# =============================================================================
# LocalOCR Extended
# Copy this file to .env, then fill in the values below.
# This file is safe to commit. Never commit your real .env.
# =============================================================================

# -----------------------------------------------------------------------------
# Required: App access
# -----------------------------------------------------------------------------
# Generate once with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# The first browser login uses the email/password below.
# INITIAL_ADMIN_TOKEN remains useful for integrations and direct API calls.
INITIAL_ADMIN_TOKEN=replace_with_a_long_random_token
INITIAL_ADMIN_NAME=Admin
INITIAL_ADMIN_EMAIL=admin@localhost
INITIAL_ADMIN_PASSWORD=replace_with_a_strong_password
SESSION_SECRET=replace_with_another_long_random_secret
SESSION_COOKIE_SECURE=0

# -----------------------------------------------------------------------------
# Required: Primary OCR provider
# -----------------------------------------------------------------------------
GEMINI_API_KEY=replace_with_your_gemini_api_key
GEMINI_MODEL=gemini-2.5-flash

# -----------------------------------------------------------------------------
# Optional: Secondary OCR fallback
# Leave blank if you do not want OpenAI fallback.
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_OCR_MODEL=gpt-4.1-mini

# -----------------------------------------------------------------------------
# Optional: Ollama fallback OCR
# Extended is designed to run in parallel with the stable grocery app.
# Default points at the shared Ollama service published by that stack.
# If you run Ollama on another machine, replace with that LAN URL.
# Example: http://192.168.1.50:11434
# -----------------------------------------------------------------------------
OLLAMA_ENDPOINT=http://host.docker.internal:11434
OLLAMA_MODEL=llava:7b
OLLAMA_TIMEOUT_SECONDS=180
# Text model for AI shopping recommendations (separate from the vision OCR model
# above). Balanced default: qwen2.5:7b (~4.7G weights, ~6G loaded — give the
# Ollama container 8G; see docker-compose.yml). Drop to qwen2.5:3b on a tight box.
OLLAMA_RECS_MODEL=qwen2.5:7b
# Cap candidates sent to the recs model per run (keeps the CPU batch small/fast).
RECS_CANDIDATE_CAP=30

# -----------------------------------------------------------------------------
# App runtime
# -----------------------------------------------------------------------------
FLASK_ENV=production
FLASK_PORT=8090
FLASK_DEBUG=0
APP_SERVICE_NAME=localocr-extended-backend
APP_DISPLAY_NAME=LocalOCR Extended
APP_SLUG=localocr_extended
DATABASE_URL=sqlite:////data/db/localocr_extended.db
BACKUP_DIR=/data/backups
BACKUP_PREFIX=localocr_extended
RECOMMENDATION_TIME=08:00
RECEIPT_RETENTION_MONTHS=12

# -----------------------------------------------------------------------------
# MQTT / Home Assistant
# Extended shares the existing grocery broker by default.
# Set MQTT_BROKER=mqtt only if you start the optional local-infra profile here.
# -----------------------------------------------------------------------------
MQTT_BROKER=host.docker.internal
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=localocr-extended
MQTT_TOPIC_PREFIX=home/localocr_extended
MQTT_DISCOVERY_ENABLED=true
HOME_ASSISTANT_DISCOVERY_PREFIX=homeassistant

# -----------------------------------------------------------------------------
# Optional operator notes for Home Assistant integration
# These are placeholders for the person deploying the app.
# They are not currently required by the backend code.
# -----------------------------------------------------------------------------
HOME_ASSISTANT_BASE_URL=http://homeassistant.local:8123
HOME_ASSISTANT_DASHBOARD_PATH=/lovelace/localocr-extended

# -----------------------------------------------------------------------------
# Optional: Telegram bot
# Needed only if you want to send receipts through Telegram.
# TELEGRAM_WEBHOOK_BASE_URL must be public HTTPS.
# Example: https://inventory.example.com
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_BASE_URL=
TELEGRAM_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Optional: local storage override
# Leave blank to use Docker volumes.
# -----------------------------------------------------------------------------
RECEIPTS_DIR=/data/receipts

# -----------------------------------------------------------------------------
# Required for encrypted credentials (admin-stored AI provider API keys via
# Settings → AI Models with credential_mode=stored_key, plus Plaid access
# tokens and any future external integrations). Generate ONCE per environment
# and never rotate without re-encrypting existing rows.
#
# Easiest (works on any OS, container already has cryptography installed):
#   docker exec localocr-extended-backend \
#       python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# Or generate on the host (see README → Configuration → Encrypted credentials
# for per-OS install instructions).
#
# Paste the 44-char base64 key after the = sign. Use a different key in dev
# vs prod. Treat it like a password — losing it makes stored secrets
# unreadable, requiring you to re-enter them in the admin UI.
# -----------------------------------------------------------------------------
FERNET_SECRET_KEY=

# -----------------------------------------------------------------------------
# Optional: Plaid — bank / credit-card transaction import
# Sign up at https://dashboard.plaid.com and create a development item.
# PLAID_ENV = sandbox | development | production
# Sandbox credentials are the fastest way to try the integration;
# development and production require Plaid approval.
# -----------------------------------------------------------------------------
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# -----------------------------------------------------------------------------
# Optional: Sign in with Google (OAuth 2.0 / OpenID Connect)
#
# ADDS Google sign-in BESIDE the existing username/password login — it does not
# replace it. Leave blank to keep Google sign-in disabled; password login is
# unaffected either way. The login UI only shows the Google button when both
# GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET are set (and GOOGLE_OAUTH_ENABLED
# is not turned off).
#
# Create credentials in Google Cloud Console → APIs & Services → Credentials →
# Create OAuth client ID → "Web application" (full click-by-click steps are in
# the Phase 4 credential-handoff guide). Then paste the values below.
#
# Authorized redirect URI to register in the console MUST exactly match:
#   <base>/auth/oauth/google/callback
# where <base> is OAUTH_REDIRECT_BASE_URL (or PUBLIC_BASE_URL, or the request
# host if neither is set). Examples:
#   dev      http://localhost:8090/auth/oauth/google/callback
#   staging  https://staging.example.com/auth/oauth/google/callback
#   prod     https://app.example.com/auth/oauth/google/callback
#
# NEVER commit real values. These are placeholders only — fill them in your
# local .env (which is git-ignored), not here.
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_ENABLED=true
# Public base URL used to build the OAuth redirect URI. Set per environment.
# Leave blank in dev to fall back to the incoming request host.
OAUTH_REDIRECT_BASE_URL=
