import SwiftUI

// Phase 1: compile-only stubs. Full implementations land in Phase 2 (§5.8, §5.1).

extension View {
    /// Standard 44pt-height list row with hover highlight + focus ring.
    func appListRow44() -> some View {
        self
    }

    /// Larger 52pt-height list row with hover highlight + focus ring.
    func appListRow52() -> some View {
        self
    }
}
