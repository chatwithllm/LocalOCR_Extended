# Feature Parity Registry

> Generated by Deep Audit — 2026-05-19
> Source: `src/frontend/index.html` (40,050 lines) + 24 Flask blueprints in `src/backend/`
> Macro pages: 18 (excludes internal `page-design-gallery`)
> 30 features across 8 groups (canonical list in `src/frontend/features-data.js`)

## HOW TO USE
The macOS build agent MUST:
1. Before implementing any screen: `grep "Screen: <name>" FEATURE_PARITY_REGISTRY.md` to surface every item
2. Implement every ❌ item — no exceptions
3. Change ❌ → ✅ (implemented) or 🔄 (adapted for desktop, with note in `macOS Impl` column)
4. Do NOT mark a screen complete until zero ❌ items remain for it
5. Output `✅ <ScreenName> complete — N implemented, M adapted, 0 skipped` after each screen

## STATUS LEGEND
| Glyph | Meaning |
|---|---|
| ❌ | Not implemented |
| ✅ | Implemented — parity with web |
| 🔄 | Adapted for desktop (note adaptation in `macOS Impl` column) |
| 🚫 | Out of scope for v1.0 (must be justified explicitly) |

## PAGE INDEX (line ranges in `src/frontend/index.html`)

| Page | Lines | Group | Backend blueprint |
|---|---|---|---|
| Dashboard | 2166–2548 | (home — pulls from many) | various |
| Inventory | 2549–2765 | Grocery | `manage_inventory.py` (`/inventory`) |
| Products | 2766–2846 | Grocery (catalog) | `manage_product_catalog.py` |
| Medicine | 2847–2867 | Household | `manage_medications.py` |
| Restaurant | 2868–2986 | Restaurant | `manage_shared_dining.py` + `handle_receipt_upload.py` |
| Balances | 2987–3004 | Shared Dining | `shared_dining_endpoints.py` |
| Contacts | 3005–3044 | Shared Dining | `manage_shared_dining.py` |
| Expenses | 3045–3196 | Expenses | `handle_receipt_upload.py` (expense type) |
| Shopping | 3197–3510 | Grocery | `manage_shopping_list.py` (`/shopping-list`) |
| Kitchen | 3511–3579 | Grocery | `manage_kitchen.py` |
| Upload | 3580–3792 | Receipts | `handle_receipt_upload.py` (`/receipts`) |
| Receipts | 3793–4104 | Receipts | `handle_receipt_upload.py` |
| Budget | 4105–4219 | Household | `manage_household_budget.py` |
| Bills | 4220–4382 | Finance | `handle_floor_obligations.py` (`/floor-obligations`) |
| Accounts | 4383–4632 | Finance | `plaid_integration.py` (`/plaid`) |
| Analytics | 4633–4702 | Finance | `calculate_spending_analytics.py` (`/analytics`) |
| Contributions | 4703–4792 | Household | `manage_contributions.py` |
| Settings | 4793–5765 | Household | many |

---

## REGISTRY

<!-- Each section below filled in by per-page audit. Sections without rows are
     awaiting a per-page audit pass (subagents fill them in sequentially). -->

### GROUP: Home
#### Screen: Dashboard
Source: `src/frontend/index.html` lines 2166–2548

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-001 | Demo hero banner (read-only mode) | Banner section `#demo-hero` | — | — | Shown when `isDemoMode()` returns true; hidden for authenticated users | DashboardView.demoHero (Card) | ✅ |
| F-002 | Demo "Sign In" CTA button | Button `btn-primary` in demo-hero | Demo Hero | — | Calls `focusLogin()` | demoHero Sign In → AuthState.logout() (no separate login screen on Mac) | 🔄 |
| F-003 | Demo "Shopping Demo" button | Button `btn-ghost` in demo-hero | Demo Hero | — | Calls `goToPage('shopping')` | demoHero button → router.activeTab = .shopping | ✅ |
| F-004 | Demo "Restaurant Demo" button | Button `btn-ghost` in demo-hero | Demo Hero | — | Calls `goToPage('restaurant')` | demoHero button → router.activeTab = .restaurant | ✅ |
| F-005 | Demo mini-card: Grocery label | Static mini-card `demo-mini-card` | Demo Hero | — | Reads "Inventory, shopping, low stock" | demoMiniCard(Grocery) | ✅ |
| F-006 | Demo mini-card: Restaurant label | Static mini-card `demo-mini-card` | Demo Hero | — | Reads "Dining receipts and repeat orders" | demoMiniCard(Restaurant) | ✅ |
| F-007 | Demo mini-card: Expenses label | Static mini-card `demo-mini-card` | Demo Hero | — | Reads "Services, gifts, fees, and budgets" | demoMiniCard(Expenses) | ✅ |
| F-008 | Demo read-only disclaimer note | Static text `demo-readonly-note` | Demo Hero | — | Clarifies no real data in demo | demoHero disclaimer Text | ✅ |
| F-009 | Household Ranking card | Card `leaderboard-card` | — | GET `/auth/me` (leaderboard in payload) | Leaderboard data returned inside `/auth/me` JSON; populated at login | leaderboardCard from /auth/me payload | ✅ |
| F-010 | Leaderboard collapsed preview | Element `#dashboard-leaderboard-preview` | Leaderboard card | — | Shows avatar row when collapsed; tap expands | Collapsed view shows chip row (rankingRow); web shows avatar-only preview | 🔄 |
| F-011 | Leaderboard toggle button | Button `#dashboard-leaderboard-toggle` | Leaderboard card | — | `toggleLeaderboard()`; state persisted in `localStorage.dashboard_leaderboard_collapsed` | Toggle button in rankingHeader + UserDefaults persistence | ✅ |
| F-012 | Leaderboard full list | List `#dashboard-leaderboard` | Leaderboard card | — | Rendered from `householdLeaderboard` data; hidden when collapsed | rankingFullList — shown when expanded | ✅ |
| F-013 | Leaderboard footer | Element `#dashboard-leaderboard-footer` | Leaderboard card | — | Shown when leaderboard is expanded | rankingFooter — shown when expanded | ✅ |
| F-014 | Leaderboard empty state | Empty-state paragraph inside `#dashboard-leaderboard` | Leaderboard card | — | Shown when `householdLeaderboard` is empty | leaderboardEmptyState() Card | ✅ |
| F-015 | Attribution nudge banner | Banner `#dashboard-attr-nudge` | — | GET `/receipts/attribution-stats` | Shown when `data.untagged_count > 0`; displays count of untagged receipts | untaggedBanner() | ✅ |
| F-016 | Attribution nudge "Tag now" CTA | Anchor in `#dashboard-attr-nudge` | Attribution Nudge | — | Calls `navToReceiptsUntagged()`; navigates to Receipts and activates untagged filter | Tag now button → router.activeTab = .receipts | ✅ |
| F-017 | Low-stock stat pill | Clickable item `#stat-low-inline` in combo-stat | Inventory health row | GET `/inventory` | Navigates to `openDashboardStat('low-stock')` on click | counter(label: LOW) button | ✅ |
| F-018 | Inventory count stat pill | Clickable item `#stat-inv-inline` in combo-stat | Inventory health row | GET `/inventory` | Navigates to `openDashboardStat('inventory')` on click | counter(label: INV) button | ✅ |
| F-019 | Products count stat pill | Clickable item `#stat-products-inline` in combo-stat | Inventory health row | GET `/products` | Navigates to `openDashboardStat('products')` on click | counter(label: PROD) button — fetches /products via DashboardState.loadProductsCount | ✅ |
| F-020 | Spending by Category card header | Card title `💳 Spending by Category` in `#dashboard-spending-card` | — | GET `/analytics/spending-by-category?month=<YM>&limit=50` | Click collapses/expands card; collapse state persisted in `localStorage.dashboard_spending_card_collapsed` | spendingHeader + toggleSpendingCardCollapsed persistence | ✅ |
| F-021 | Spending by Category total inline stat | Span `#dashboard-spending-total` | Spending card | — | Displays `formatMoney(data.total)` | Spending total in spendingHeader trailing | ✅ |
| F-022 | Spending category row list (top 5) | List `#dashboard-spending-body` | Spending card | GET `/analytics/spending-by-category` | Renders first 5 rows; remaining hidden behind Show-more | spendingBody categoryBar loop (top 5 by default) | ✅ |
| F-023 | Spending Show-more / Show-less button | Button `#dashboard-spending-more-btn` | Spending card | — | `toggleDashboardSpendingMore()`; toggles visibility of rows beyond top 5 | Show-more / Show-less button — toggleSpendingShowAll | ✅ |
| F-024 | Spending loading state | Empty-state `<p>Loading…</p>` in `#dashboard-spending-body` | Spending card | — | Shown before API response | spendingBody isSpendingLoading branch | ✅ |
| F-025 | Spending error state | Empty-state paragraph in `#dashboard-spending-body` | Spending card | — | Shown when `/analytics/spending-by-category` returns non-OK | spendingBody spendingError branch | ✅ |
| F-026 | Fixed Obligations card (hidden shell) | Card `#dashboard-floor-card` `display:none` | — | GET `/floor-obligations/summary?month=<YM>` | Data merged into spending card; card element kept hidden to avoid broken refs | v1.0: floor obligations data already shown in spending categories (Mac doesn't have hidden shell) | 🚫 |
| F-027 | Low Stock alert card | Card `#dash-low-stock-card` | — | GET `/inventory` | Shows items whose qty ≤ threshold; "All items well-stocked!" empty state when none | lowStockCard | ✅ |
| F-028 | Low Stock count badge button | Button `#dash-low-stock-header-count` | Low Stock card | — | Toggle `toggleDashboardSection('low-stock')`; hidden when count is 0 | Count badge in lowStockCard header | ✅ |
| F-029 | Low Stock empty state | Empty-state with checkmark icon | Low Stock card | — | "All items well-stocked!" shown when no low-stock items | Empty state: All items well-stocked! | ✅ |
| F-030 | Receipts Processed activity card | Card `#dash-receipts-activity-card` | — | GET `/analytics/receipts-activity?grain=<grain>` | Collapses on title click; renders bar/line sparkline chart | receiptsActivityCard | ✅ |
| F-031 | Receipts activity grain selector: Day | Button `data-grain="day"` in `#dash-receipts-activity-grain` | Receipts Activity card | GET `/analytics/receipts-activity?grain=day` | `setReceiptsActivityGrain('day')`; default grain | grainPicker .day | ✅ |
| F-032 | Receipts activity grain selector: Week | Button `data-grain="week"` | Receipts Activity card | GET `/analytics/receipts-activity?grain=week` | `setReceiptsActivityGrain('week')` | grainPicker .week | ✅ |
| F-033 | Receipts activity grain selector: Month | Button `data-grain="month"` | Receipts Activity card | GET `/analytics/receipts-activity?grain=month` | `setReceiptsActivityGrain('month')` | grainPicker .month | ✅ |
| F-034 | Receipts activity loading state | Empty-state `<p>Loading…</p>` in `#dash-receipts-activity-body` | Receipts Activity card | — | Shown before API response | activityBody isActivityLoading branch | ✅ |
| F-035 | Receipts activity empty state | Empty-state paragraph | Receipts Activity card | — | "No receipts processed yet." when API returns empty buckets | activityBody empty-array branch | ✅ |
| F-036 | Receipts activity error state | Empty-state paragraph | Receipts Activity card | — | "Could not load receipt activity." on non-OK response | activityBody activityError branch | ✅ |
| F-037 | Top Picks (recommendations) card | Card `#dash-recs-card` | — | GET `/recommendations` | Shows AI-driven reorder picks | topPicksCard | ✅ |
| F-038 | Top Picks count badge button | Button `#dash-recs-header-count` | Top Picks card | — | `toggleDashboardSection('recommendations')`; hidden when count is 0 | Count badge in topPicksCard header | ✅ |
| F-039 | Top Picks empty state | Empty-state with lightbulb icon | Top Picks card | — | "No recommendations yet" when API returns empty | Empty state: No recommendations yet | ✅ |
| F-040 | Shopping List summary card — title/link | Button `#dash-shopping-card` in `dashboard-summary-card` | — | GET `/shopping-list` | `openDashboardStat('shopping')`; navigates to Shopping List page | shoppingListCard arrow-up-right button → router.activeTab = .shopping | ✅ |
| F-041 | Shopping List item count badge | Span `#dash-shopping-header-count` | Shopping summary card | — | Displays open item count | Count capsule in shoppingHeader | ✅ |
| F-042 | Shopping List estimated total toggle | Button `#dash-shopping-estimate` | Shopping summary card | — | `toggleDashboardShoppingPreview(event)`; toggles preview list | Estimated total button → shoppingPreviewExpanded toggle | ✅ |
| F-043 | Shopping List expanded header count | Span `#dash-shopping-expanded-count` | Shopping summary card | — | Shown in expanded view | shoppingExpandedBody pending/purchased line | ✅ |
| F-044 | Shopping List estimate in expanded header | Div `#dash-shopping-expanded-estimate` | Shopping summary card | — | "Estimate —" or formatted total | shoppingExpandedBody Estimate text | ✅ |
| F-045 | Shopping List preview rows | Container `#dash-shopping-preview` | Shopping summary card | — | Inline preview of shopping items; collapsed by default | shoppingExpandedBody item ForEach | ✅ |

---

### GROUP: Grocery
#### Screen: Inventory
Source: `src/frontend/index.html` lines 2549–2765
Backend: `manage_inventory.py` (url_prefix `/inventory`)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-100 | Add Item card (collapsed toggle) | Button `#inventory-add-toggle` | Add Item card | — | `toggleInventoryAddCard()`; collapse state in `localStorage.inventory_add_collapsed` | AddInventoryCard collapse Button → state.addCardCollapsed (UserDefaults `LocalOCR.inventory.addCollapsed`) | ✅ |
| F-101 | Product Name input field | Input `#inv-name` | Add Item card | — | `oninput` triggers `handleInventoryAddNameInput()` for search-assist | TextField `$name` in AddInventoryCard (search-assist deferred to v1.1) | 🔄 |
| F-102 | Quantity input field | Input `#inv-qty` (number, min 0, step 0.1) | Add Item card | — | Default 1; fractional quantities supported | TextField .number format `$qty`, default 1, supports fractions | ✅ |
| F-103 | Location select dropdown | Select `#inv-loc` | Add Item card | — | Options: Pantry, Fridge, Freezer, Cabinet, Laundry Room, Custom…; `onchange` triggers `handleInventoryLocationChange()` | Picker `$location` with Pantry/Fridge/Freezer/Cabinet/Laundry Room/Custom… | ✅ |
| F-104 | Custom Location input (conditional) | Input `#inv-custom-loc` in `#inv-custom-loc-wrap` | Add Item card | — | Shown only when "Custom…" selected in location dropdown | Conditional TextField `$customLocation` shown only when location == "Custom…" | ✅ |
| F-105 | Low-Stock Threshold input | Input `#inv-thresh` (number, min 0, step 0.1) | Add Item card | — | Default 1; item flagged low when qty ≤ this value | TextField .number `$threshold`, default 1 | ✅ |
| F-106 | "More details" toggle | Button `#inventory-add-details-toggle` | Add Item card | — | `toggleInventoryAddDetails()`; reveals Category, Unit, Preferred Store, shopping checkbox | Button toggling `state.addCardDetailsExpanded`; reveals expanded form section | ✅ |
| F-107 | Category select (expanded details) | Select `#inv-category` | Add Item card — expanded | — | Populated by `populateInventoryManualCategorySelect()` | Picker `$category` in expanded form | ✅ |
| F-108 | Unit chip row (expanded details) | Chip row `#inv-unit-chip-row` | Add Item card — expanded | — | `renderInventoryUnitChips()`; backing hidden input `#inv-unit` | HStack of Buttons (chip row) bound to `$unit` | ✅ |
| F-109 | Preferred Store select (expanded details) | Select `#inv-preferred-store` | Add Item card — expanded | — | Populated by `populateInventoryStoreSelect()` | TextField `$preferredStore` (v1.0 — store list population deferred to Plaid/store catalog phase) | 🔄 |
| F-110 | "Add to shopping too" checkbox | Checkbox `#inv-add-to-shopping` | Add Item card — expanded | — | When checked, item is also added to active shopping list on submit | Toggle .checkbox `$alsoAddToShopping`; submit forks to ShoppingState.add when on | ✅ |
| F-111 | Add to Inventory submit button | Button `btn-primary` calling `addInventoryItem()` | Add Item card | POST `/inventory/add-item` | Creates inventory item; optionally adds to shopping list; auto-links or creates product | `Button` → InventoryState.addItem → POST /inventory/add-item (⌘↩ shortcut) | ✅ |
| F-112 | Inline product creation form (shared) | Hidden form `#add-prod-form-shared` | Add Item card | POST `/products` (via `createProduct()`) | Shown when new product needs to be created; fields: Name, Category, Barcode | Backend /inventory/add-item auto-creates Product when product_name is unknown — no separate form needed | 🚫 |
| F-113 | Current Inventory section header | H2 with `#inv-low-badge` and `#inv-window-note` | Inventory tile card | GET `/inventory?_t=<ts>` | Low badge shown when any items below threshold; window note shows active date range | Header row in InventoryListBody — title + low badge + window label | ✅ |
| F-114 | Low-stock count badge | Badge `#inv-low-badge` | Inventory tile header | — | Hidden when no low items; red background with count | Text capsule rendered when `state.lowStockCount > 0`, lowStockPillBackground + warning text | ✅ |
| F-115 | Inventory window note | Div `#inv-window-note` | Inventory tile header | — | Displays `data.window_start` range or default "current and previous month" | Text(state.windowLabel) in header trailing position | ✅ |
| F-116 | Inventory search input | Input `#inventory-search` | Inventory toolbar | — | Filters rendered tile groups client-side | TextField `$state.searchText` filtering filteredItems | ✅ |
| F-117 | Location filter select | Select `#inventory-location-filter` | Inventory toolbar | — | Options: All, Fridge, Freezer, Pantry, Cabinet, Bathroom | Picker `$state.locationFilter` with All/Fridge/Freezer/Pantry/Cabinet/Bathroom | ✅ |
| F-118 | Group-by select | Select `#inventory-group-by` | Inventory toolbar | — | Options: Running low first, Domain, Location; default "low_first" | Picker `$state.groupBy` (lowFirst/domain/location) persisted to UserDefaults | ✅ |
| F-119 | Sort select | Select `#inventory-sort` | Inventory toolbar | — | Options: expiry asc, name, quantity | Picker `$state.sortBy` (expiryAsc/name/quantity) persisted to UserDefaults | ✅ |
| F-120 | "Show empty" checkbox | Checkbox `#inventory-show-empty` | Inventory toolbar | — | Toggles display of items with qty = 0 | Toggle .checkbox `$state.showEmpty` persisted to UserDefaults | ✅ |
| F-121 | "Recently used up" restore button | Button `#inventory-restore-btn` calling `invOpenRestoreModal()` | Inventory toolbar | GET `/inventory/recently-used-up?days=30` | Opens inline restore section; shows items consumed in last 30 days | Button toggles `restoreSectionVisible` + fires loadRecentlyUsedUp(days:30) | ✅ |
| F-122 | "Merge duplicates" button | Button `#inventory-find-dups-btn` calling `findDuplicateProducts()` | Inventory toolbar | POST `/products/auto-dedup-tokens` | Merges products that share the same token-set fingerprint | Button → POST /products/auto-dedup-tokens via APIClient + toast on success | ✅ |
| F-123 | Category filter chip row | Chip row `#inv-category-chips` | Inventory tile card | — | `renderInvCategoryChips()`; each chip filters by category | Horizontal ScrollView of chip Buttons bound to `state.categoryFilter` | ✅ |
| F-124 | Bulk-action bar | Div `#inventory-bulk-bar` | Inventory list body | — | Shown when ≥1 tile is selected; displays count label + bulk buttons | BulkActionBar conditional on `!state.selectedItemIds.isEmpty`; row checkbox toggles selection | ✅ |
| F-125 | Bulk-action count label | Span `#inventory-bulk-count` | Bulk action bar | — | "N selected" | Text "\(count) selected" in BulkActionBar leading | ✅ |
| F-126 | Bulk "−1 all" button | Button `inv-btn-decr` calling `invBulkDecrement()` | Bulk action bar | PUT `/inventory/<id>/update` (per item) | Decrements quantity of all selected items by 1 | Button → state.bulkDecrement → withTaskGroup of consume(amount:1) per id | ✅ |
| F-127 | Bulk "+3d all" defer button | Button calling `invBulkDefer(3)` | Bulk action bar | PUT `/inventory/<id>/update` (per item) | Pushes expiry date of all selected items by 3 days | Button → state.bulkDefer(days:3) → PATCH /inventory/products/<pid> {defer_days:3} fan-out | ✅ |
| F-128 | Bulk "+7d all" defer button | Button calling `invBulkDefer(7)` | Bulk action bar | PUT `/inventory/<id>/update` (per item) | Pushes expiry date of all selected items by 7 days | Button → state.bulkDefer(days:7) → PATCH /inventory/products/<pid> {defer_days:7} fan-out | ✅ |
| F-129 | Bulk "Used up all" button | Button `inv-btn-used` calling `invBulkUsedUp()` | Bulk action bar | DELETE `/inventory/<id>` (per item) | Removes all selected items from inventory (marks consumed) | Button (.destructive) → state.bulkUsedUp → PATCH /inventory/products/<pid> {quantity:0} fan-out | ✅ |
| F-130 | Bulk "Clear" selection button | Button calling `invBulkClear()` | Bulk action bar | — | Deselects all tiles | Button → state.clearSelection (removes all selectedItemIds) | ✅ |
| F-131 | Product snapshot file input | Hidden input `#product-snapshot-input` (file, image/*) | Inventory list body | POST `/product-snapshots/upload` | `uploadProductSnapshotFromPicker(event)`; attaches photo to product | Product photo capture (/product-snapshots/upload) deferred to v1.1 — needs photo picker + Snapshots service | 🚫 |
| F-132 | Inventory tile groups container | Div `#inventory-groups` | Inventory list body | — | Rendered by `renderInventory()`; grouped per group-by selection | List with Section(ForEach) driven by `state.groupedItems()` (lowFirst/domain/location) | ✅ |
| F-133 | Inventory tile — consume action (inline) | Per-tile decrement or consume button | Individual tile | PUT `/inventory/<id>/consume` | Decrements qty or removes item when qty reaches 0 | Inline −/+ Buttons in row → consume(amount:1) and updateItem(quantity:+1); ⌥↑ / ⌥↓ shortcuts | ✅ |
| F-134 | Inventory tile — inline edit patch | Per-tile PATCH field | Individual tile | PATCH `/inventory/products/<product_id>` | Edits product-level fields (name, category, unit, etc.) inline | Edit sheet exposes qty/location/threshold via PUT /update; product-level rename PATCH wired in InventoryState.patch (UI hook deferred to v1.1) | 🔄 |
| F-135 | Inventory tile — set low-status | Per-tile low-stock toggle | Individual tile | PUT `/inventory/products/<product_id>/low-status` | Manually marks a product as low/not-low | Swipe action + contextMenu → state.markLow(manualLow:!current) | ✅ |
| F-136 | Inventory tile — confirm low status | Per-tile confirm button | Individual tile | POST `/inventory/products/<product_id>/confirm-low` | Confirms AI-suggested low status | ContextMenu "Confirm low (peer)" → state.confirmLow (disabled until manualLow == true) | ✅ |
| F-137 | Inventory tile — update item fields | Per-tile edit form | Individual tile | PUT `/inventory/<item_id>/update` | Updates qty, location, threshold, expiry for a specific item | InventoryEditSheet with TextField qty/location/threshold + Save → state.updateItem | ✅ |
| F-138 | Inventory tile — delete item | Per-tile delete button | Individual tile | DELETE `/inventory/<item_id>` | Removes single item from inventory | Swipe action .destructive + contextMenu Delete → state.deleteItem | ✅ |
| F-139 | Recently Used Up inline restore section | Div `#inv-restore-section` | Inventory list body | GET `/inventory/recently-used-up?days=30` | Opened by `invOpenRestoreModal()`; last 30 days of consumed items | RecentlyUsedUpSection — horizontal ScrollView of restore rows when visible | ✅ |
| F-140 | Restore item button | Button in `#inv-restore-list` calling restore | Restore section | POST `/inventory/products/<product_id>/restore` | Returns a consumed item back to active inventory | Button "Restore" per row → state.restore(productId, quantity:priorQuantity) | ✅ |
| F-141 | Restore section "Hide" button | Button calling `invCloseRestoreModal()` | Restore section | — | Hides `#inv-restore-section` | Button "Hide" sets `state.restoreSectionVisible = false` | ✅ |
| F-142 | Inventory loading state | — (inferred from `res.ok` guard in `loadInventory`) | — | GET `/inventory?_t=<ts>` | Page does not re-render on failure; silent return | Logger.error on failure; EmptyStateView + SkeletonView during load — non-silent unlike web | 🔄 |
| F-143 | Delete expiry override | Per-tile action | Individual tile | DELETE `/inventory/products/<product_id>/expiry-override` | Clears a manually-set expiry date back to computed default | ContextMenu "Reset expiry to system default" → state.clearExpiryOverride (disabled when source==system) | ✅ |
| F-144 | Set regular-use flag | Per-tile toggle | Individual tile | PUT `/inventory/products/<product_id>/regular-use` | Marks item as regularly purchased to influence recommendations | ContextMenu toggle "Mark/Remove regular use" → state.toggleRegularUse (PUT /inventory/products/<id>/regular-use; web's /products/<id>/regular-use is broken — that route doesn't exist server-side) | ✅ |
| F-145 | Inventory tile — product snapshot image | Img tag inside `_invBuildTile` rendering `i.latest_snapshot.image_url` | Individual tile | GET `/product-snapshots/<id>/image` (authenticated, sends X-Trusted-Device-Token) | Shown when backend includes `latest_snapshot.image_url`; placeholder when absent | KFImage(resolveURL) with ImageCache.tokenModifier; falls back to initials chip when snapshot is nil. Added 2026-05-19 after I-11 (web side-by-side audit caught image render missing — was hidden inside F-132 "rendered by renderInventory()" rollup) | ✅ |
| F-146 | Inventory tile — per-tile defer button | Button `inv-btn-defer` calling `invDefer(pid, 3)`; hold-press triggers +7d via `_invAttachButtonHold` | Individual tile | PATCH `/inventory/products/<product_id>` body `{defer_days: N}` | Pushes expiry forward; +3d default, +7d via long-press (mac uses ⌥-click) | Button "+3d" in row trailing → state.deferExpiry(productId:, days:); ⌥-click switches to +7d (NSEvent.modifierFlags check). Added 2026-05-19 after I-11 — was hidden inside F-133 "consume action" rollup | ✅ |
| F-147 | Group header — expiring soon count | `<span>· N expiring soon</span>` appended in `_invBuildGroup` when `expSoon > 0` | Group section header | — | Counts items whose `daysLeft <= 3` (matches web's `_invClassifyExpiry` exp-soon + exp-expired) | InventoryListBody.groupHeader appends "· N expiring soon" Text in DesignTokens.warning when `InventoryState.expiringSoonCount(rows) > 0`. Added 2026-05-19 after I-11 — was hidden inside F-132 "rendered by renderInventory()" rollup | ✅ |
| F-148 | Per-tile "Bought" metadata line | `<div>📅 Bought YYYY-MM-DD</div>` in `_invBuildTile` (line 24067-24069) | Individual tile | — | Shown when `last_purchased_at` is set | InventoryRow VStack Text "📅 Bought \(lastPurchasedAt.prefix(10))" — caption2 tertiaryLabel. Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-149 | Per-tile "Expires" metadata line | `<div>🍂 Expires YYYY-MM-DD</div>` with `dateSpan.className = flashCls` (line 24070-24085) | Individual tile | — | Red strikethrough on expired (`exp-expired`); tag chip when `expires_source` is `user`/`defer` | InventoryRow HStack "🍂 Expires \(date)" with `.strikethrough(isExpired, color: error)` + ExpirySourceChip. Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-150 | Per-tile days-left label | `_invElText("span", "inv-tile-days", headLabel)` where headLabel = `"no expiry"` / `"EXPIRED Nd ago"` / `"Nd left"` (line 23961-23963) | Individual tile head | — | Color tracks `_expCls` (red/orange/grey) | InventoryRow `daysLabel` computed prop matches web wording exactly. Color via daysLabelColor (error <= 0, warning <= 3, else tertiary). Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-151 | Per-tile expiry-source `defer` chip | `<span class="tag-defer">defer</span>` appended when `expires_source === "defer"` (line 24080-24082) | Individual tile meta | — | Shown only when expiry was manually deferred via PATCH `{defer_days: N}` | ExpirySourceChip(label: "defer", tint: deferTint) inside Expires HStack. Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-152 | Per-tile expiry-source `user` chip | `<span class="tag-user">user</span>` appended when `expires_source === "user"` (line 24077-24079) | Individual tile meta | — | Shown when user manually set the expiry date | ExpirySourceChip(label: "user", tint: userTint) inside Expires HStack. Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-153 | Per-tile inline `✎` edit button | `_invMakeBtn("inv-btn-edit", "✎", invEdit)` (line 24095) | Individual tile actions row | — | Opens the inline product edit panel | Button (pencil SF symbol) → editingItem = item; opens InventoryEditSheet. Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-154 | Per-tile inline `🛒` add-to-shopping button | `_invMakeBtn("inv-btn-cart", "🛒", invAddToShoppingList)` (line 24101); `.on-list` greyed state | Individual tile actions row | POST `/shopping-list/items` | One-tap add; backend merges duplicates server-side | Button (cart SF symbol) → ShoppingState.add(productName:quantity:source:productId:). Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-155 | Per-tile inline `✓` clear-low / used-up button | `_invMakeBtn("inv-btn-used", "✓", ...)` (line 24108-24117); hold = "✓ + 🛒" via `_invAttachButtonHold` | Individual tile actions row | PUT `/inventory/products/<id>/low-status` OR PATCH `/inventory/products/<id>` `{quantity:0}` | When `is_low || manual_low`: clear low flag (and threshold for threshold-low). Otherwise mark used-up. Long-press adds to shopping too | Button (checkmark SF symbol) — branches on `item.isLowStock`; ⌥-click runs ShoppingState.add (matches long-press "✓ + 🛒"). Tint: warning when low, success when fresh. Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-156 | Per-tile status fill bar — draggable remaining_pct slider | `titleRow.style.setProperty("--remaining-pct", ...)` + `--status-fill` (line 24000-24002); drag handle (line 24025-24039) with live `inv-drag-bubble`; persists via PUT `/inventory/<id>/update` `{consumed_pct_override}` (line 23274-23288) | Individual tile name row background + handle | PUT `/inventory/<id>/update` `{consumed_pct_override: 0..100}` | Horizontal fill behind the name; width = remaining_pct; color = status (fresh ≥60% green / low ≥20% amber / out red); drag handle on right edge of fill; bubble shows live %; releases persist override | RemainingSlider — GeometryReader Rectangle with status-tinted fill, draggable Capsule handle at fill edge, DragGesture updates local @State and recolors live, .onEnded calls InventoryState.setOverride. Tap-to-cycle on the name area (NOT the handle) calls InventoryState.cycleStatus (F-159). Promoted from 🔄 to ❌ on 2026-05-19 after user confirmed v1 must ship parity. | ✅ |
| F-157 | Group header emoji prefix | `lbl.emoji` returned by `_invGroupLabel` → 🔴 Running Low / ✅ OK / 📦 Domain (line 23500-23512) | Group section header | — | Color-coded emoji per group type | InventoryListBody.groupHeader prepends "🔴" / "✅" / "📦" based on header title. Added 2026-05-19 after RULE 11 pixel-diff pass 2 | ✅ |
| F-158 | Per-tile thumbnail hover zoom | CSS `.inv-tile-img:hover { transform: scale(2.6); z-index: 100; box-shadow: 0 6px 24px rgba(0,0,0,0.6) }` (line 275-279) — desktop only, `cursor: zoom-in` | Individual tile image | — | Hovering the thumbnail pops a 2.6× zoomed preview over the layout; no click required; tap-active fallback on mobile | ProductSnapshotThumb has `.onHover` → @State isHovering → 250 ms delay → `.popover(isPresented: $showPopover, arrowEdge: .leading)` showing KFImage at 320×320 with shadow. Delay keeps a cursor swept past from flashing the popover. InventoryView.swift lines 1163-1183. Added 2026-05-19. | ✅ |
| F-159 | Per-tile tap-cycle remaining status | `titleRow.addEventListener("click", … _invCycleStatus(i.id, i.status))` (line 24004-24014); cycles fresh→low→out via `_INV_STATUS_BUCKET = { fresh:80, low:40, out:10 }` and PUT `/inventory/<id>/update` `{consumed_pct_override: 100 - bucket}` (line 23266-23272) | Individual tile name row body | PUT `/inventory/<id>/update` `{consumed_pct_override: 20 \| 60 \| 90}` | Click anywhere on the name row (except the drag handle, drag-handle clicks suppressed via target check) advances to the next bucket; suppressed for ~250 ms after a drag ends | RemainingSlider title overlay has `.onTapGesture` gated on `!isDragging`. After DragGesture ends a 250 ms suppression window holds `isDragging=true` so the synthetic post-drag tap doesn't re-cycle. Calls InventoryState.cycleStatus → setRemainingOverride with bucket 80/40/10. InventoryView.swift lines 988-994 + InventoryState.swift lines 379-385. Added 2026-05-19. | ✅ |
| F-160 | Per-tile `−1` single-tap decrement | `_invMakeBtn("inv-btn-decr", "−1", function() { invDecrement(i.product_id); })` (line 24102); `invDecrement` optimistically subtracts 1 from quantity, removes tile when newQty ≤ 0, then PATCH `/inventory/products/<pid>` `{quantity: newQty}` (line 24421-24435) | Individual tile actions row | PATCH `/inventory/products/<product_id>` `{quantity: max(0, quantity-1)}` | One-tap subtract; row disappears when quantity hits zero (used-up path) | Button "−1" → InventoryState.decrementOne(productId:) → patch(productId:body: quantity = max(0, q-1)); ⌥↓ keyboard shortcut. Replaces v0 mac-only −/qty/+ stepper that had no web counterpart. InventoryView.swift lines 787-798 + InventoryState.swift lines 390-398. Added 2026-05-19. | ✅ |

#### Screen: Shopping List
Source: `src/frontend/index.html` lines 3197–3510
Backend: `manage_shopping_list.py` (url_prefix `/shopping-list`)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-200 | Page header — "Shopping List" title | H1 and subtitle paragraph | — | — | Static display | `.navigationTitle("Shopping List")` on ShoppingListView | ✅ |
| F-201 | Quick Find toggle button (header) | Button `#shopping-quick-header-toggle` (🔍) | Page header | — | `toggleShoppingSection('quick-find')`; toggles Quick Find card | Toolbar magnifyingglass Button → `ShoppingState.toggleQuickFind()` (UserDefaults `LocalOCR.shopping.quickFindCollapsed`) | ✅ |
| F-202 | Recommendations toggle chip (header) | Button `#shopping-recommendations-toggle` (✨ + count) | Page header | — | `toggleShoppingSection('recommendations')`; count badge shows rec count | Toolbar sparkles + count Button → `ShoppingState.toggleRecommendations()` (UserDefaults persisted) | ✅ |
| F-203 | Session banner | Card `#shopping-session-banner` | — | — | Shown when an active session exists; content set by `renderShoppingListTable()` | `sessionBanner(session:)` Card with status-specific headline + actions; renders only when `state.session` present | ✅ |
| F-204 | Open items summary pill | Button `#shopping-pill-open` | Summary strip | — | `setShoppingListView('open')`; displays count of open items | `summaryPill("Open", openCount)` Button → `state.setFilter(.open)`; highlight when active | ✅ |
| F-205 | Estimated total pill | Static pill `shopping-summary-pill-estimated` | Summary strip | — | Displays `$0.00` or computed estimate from item prices | Disabled `summaryPill("Estimate", estimatedTotalCost)` — display only via `formatMoney` | ✅ |
| F-206 | Purchased/Closed items summary pill | Button `#shopping-pill-purchased` | Summary strip | — | `setShoppingListView('purchased')`; displays count of purchased items | `summaryPill("Close", purchasedCount)` Button → `state.setFilter(.purchased)` | ✅ |
| F-207 | Add Item Manually card (hidden by default) | Card `#shop-manual-card` | — | — | Toggled by `toggleManualShoppingForm()`; initially hidden | `ManualAddCard` rendered when `state.manualAddVisible == true`; toolbar `+` button (⌘N) toggles | ✅ |
| F-208 | Manual add card "Hide" button | Button calling `toggleManualShoppingForm(false)` | Manual add card | — | Collapses the manual add form | "Hide" Button in card header → `state.manualAddVisible = false` | ✅ |
| F-209 | "Identify from Photo" button | Button `#shop-identify-btn` calling `triggerShopIdentifyPhoto()` | Manual add card | POST `/shopping-list/identify-product-photo` | Triggers camera/file picker; Gemini identifies product and pre-fills form | v1.0: disabled `Label("Identify from Photo", systemImage: "camera")` with help "Coming in v1.1". Gemini OCR round-trip + photo picker integration deferred — backend endpoint also requires GEMINI_API_KEY (not configured in all envs) | 🔄 |
| F-210 | Photo identify file input | Hidden input `#shop-identify-input` (file, image/*, capture=environment) | Manual add card | POST `/shopping-list/identify-product-photo` | `handleShopIdentifyPhoto(event)` sends to Gemini OCR endpoint | Deferred with F-209 — needs NSOpenPanel + multipart upload (v1.1) | 🔄 |
| F-211 | Photo identify status label | Span `#shop-identify-status` | Manual add card | — | Shows processing status or errors during photo identification | Deferred with F-209 (v1.1) | 🔄 |
| F-212 | Photo identify preview image | Img `#shop-identify-preview` | Manual add card | — | Displays captured photo thumbnail | Deferred with F-209 (v1.1) | 🔄 |
| F-213 | Manual add Name input | Input `#shop-name` | Manual add card | — | Item name free-text; placeholder "e.g. Milk" | `TextField("e.g. Milk", text: $name)` in ManualAddCard | ✅ |
| F-214 | Manual add Category select | Select `#shop-category` | Manual add card | — | Populated by `populateShoppingManualCategorySelect()` | `Picker` over 12-category list bound to `$category` | ✅ |
| F-215 | Manual add Preferred Store select | Select `#shop-manual-store` | Manual add card | — | Populated from store list | `Picker` populated from `state.storeBuckets.frequent + lowFreq` (falls back to `availableStores`) | ✅ |
| F-216 | Manual add Estimate Price input | Input `#shop-manual-price` (number, step 0.01) | Manual add card | — | Optional price estimate for total calculation | `TextField("0.00", text: $price)` parsed to Double on submit | ✅ |
| F-217 | Manual add Quantity input | Input `#shop-qty` (number, step 0.1, default 1) | Manual add card | — | Fractional quantities supported | `TextField("1", value: $quantity, format: .number)`, defaults to 1 | ✅ |
| F-218 | Manual add Note input | Input `#shop-note` | Manual add card | — | Optional free-text note per item | `TextField("optional note", text: $note)` | ✅ |
| F-219 | "Add to Shopping List" submit button | Button `btn-primary` calling `createShoppingItem()` | Manual add card | POST `/shopping-list/items` | Creates shopping item; links to product if match found | Primary Button → `state.addItem(...)`; ⌘↩ keyboardShortcut; clears inputs + collapses card on success | ✅ |
| F-220 | Quick Find card | Card `#shopping-quick-find-card` | — | — | Search across products to quickly add items | `QuickFindCard` Card always rendered; body hidden when `quickFindCollapsed` | ✅ |
| F-221 | Quick Find "Collapse" button | Button `#shopping-quick-toggle` | Quick Find card | — | `toggleShoppingSection('quick-find')` | Chevron Button on card header → `state.toggleQuickFind()` | ✅ |
| F-222 | Quick Find search input | Input `#shop-quick-search` | Quick Find card | — | `searchShoppingQuickFind()`; `onkeydown` handles Enter to add; `onfocus`/`onblur` manage popover | `TextField` bound to `$state.quickFindQuery` with 180 ms debounce (`scheduleQuickFindSearch`) + `.onSubmit` immediate fire. macOS has no popover overlay; results render below the field. | ✅ |
| F-223 | Quick Find preferred store filter | Select `#shop-preferred-store` | Quick Find card | — | Filters search results by store preference | `Picker("Any store", selection: $state.quickFindStoreFilter)` populated from store buckets | ✅ |
| F-224 | Quick Find "Add Manually" button | Button `#shop-manual-toggle` calling `toggleManualShoppingForm()` | Quick Find card | — | Opens the manual add form | "Add Manually" GhostButton → `state.manualAddVisible = true` | ✅ |
| F-225 | Quick Find results list | Div `#shop-quick-results` | Quick Find card | GET `/shopping-list` + local product data | Empty state: "Search for a product to add, mark low, or mark bought." | `QuickFindResults` shows EmptyStateView under 2 chars, search-empty CTA when no matches (with "Add … manually"), or 5 `QuickFindRow`s. Per-row buttons: Add to Shopping / +1 / Bought / Low. Pre-query "suggestions on focus" scored list (web `ensureShoppingQuickSuggestions`) deferred — needs `is_low`/`manual_low`/`last_purchase_date`/`latest_price` on `Product` model. | 🔄 |
| F-226 | Recommendations card | Card `#shopping-recommendations-card` | — | GET `/recommendations` | Collapsible; title shows "Recommendations" + count | `RecommendationsCard` loads on first appear via `loadRecommendations()`; body hidden when `recommendationsCollapsed` | ✅ |
| F-227 | Recommendations summary toggle button | Button `#shopping-recommendations-summary` | Recommendations card | — | `toggleShoppingSection('recommendations')` | Title Button (title + count badge) → `state.toggleRecommendations()` | ✅ |
| F-228 | Recommendations count badge | Span `#shopping-recommendations-summary-count` | Recommendations card | — | Displays total recommendation count | `Text("\(state.recommendations.count)")` capsule next to the title (also mirrored in toolbar sparkles chip) | ✅ |
| F-229 | Recommendations "Refresh" button | Button calling `loadRecs('shopping-recs-body')` | Recommendations card | GET `/recommendations` | Reloads recommendations from API | `arrow.clockwise` Button → `Task { await state.loadRecommendations() }` | ✅ |
| F-230 | Recommendations list body | Div `#shopping-recs-body` | Recommendations card | — | Empty state: "No recommendations yet. Add more purchase history!" | Rows render label + reason Badge; EmptyStateView when array empty | ✅ |
| F-231 | Confirm recommendation for product | Per-recommendation button | Recommendations card | POST `/shopping-list/products/<product_id>/confirm-recommendation` | Adds recommended product to active shopping list | "Confirm" PrimaryButton per row → `state.confirmRecommendation(productId:)` → reloads list + recs | ✅ |
| F-232 | Current List card | Card `#shopping-current-list-card` | — | GET `/shopping-list` | Main list of open/purchased items | `CurrentListCard` always rendered; body hidden when `currentListCollapsed` | ✅ |
| F-233 | Current List collapse toggle button | Button `#shopping-current-list-toggle` | Current List card | — | `toggleShoppingSection('current-list')` | Title chevron Button → `state.toggleCurrentList()` | ✅ |
| F-234 | Current List total display | Div `#shopping-current-list-total` | Current List card | — | Shown when items have prices; `aria-live="polite"` | "Open total / Closed total / List total" formatted via `formatMoney(state.estimatedTotalCost)` (hidden when 0) | ✅ |
| F-235 | Current List sort: A–Z | Button `#shopping-sort-name-asc` | Current List card | — | `setShoppingSort('name_asc')` | "A↓" sort chip Button → `state.setSort(.nameAsc)`; active state highlight | ✅ |
| F-236 | Current List sort: Z–A | Button `#shopping-sort-name-desc` | Current List card | — | `setShoppingSort('name_desc')` | "Z↑" sort chip Button → `state.setSort(.nameDesc)` | ✅ |
| F-237 | Current List sort: by price | Button `#shopping-sort-price` ($) | Current List card | — | `toggleShoppingPriceSort()`; toggles price asc/desc | "$" / "$↑" Button → `state.togglePriceSort()` alternates `.priceDesc` ↔ `.priceAsc`; active highlight while either price mode | ✅ |
| F-238 | Current List table body | Div `#shopping-table-body` inside `.table-wrap` | Current List card | — | Rendered by `renderShoppingListTable()` | `state.groupedFilteredItems()` (preferred_store > effective_store > "Unassigned") rendered via `StoreGroupSection` cards (F-264) with sorted rows (F-235..F-237) | ✅ |
| F-239 | Shopping list item — mark purchased | Per-item button/checkbox | Current List table | PUT `/shopping-list/items/<item_id>` `{status:"purchased"}` | Marks item bought; moves to purchased view | "Bought" PrimaryButton per row → `state.togglePurchased(id:)`; also context-menu "Mark Bought" | ✅ |
| F-240 | Shopping list item — mark open (reopen) | Per-item reopen button | Current List table | PUT `/shopping-list/items/<item_id>` `{status:"open"}` | Returns purchased item to open state | When `item.isPurchased`, button flips to "Reopen" (same handler) | ✅ |
| F-241 | Shopping list item — edit quantity | Per-item qty field | Current List table | PUT `/shopping-list/items/<item_id>` `{quantity:<n>}` | Inline quantity edit | "+1" Button in Quick Find for matched item; "−1" in row (F-265); ⌥-click on quick-find Bought = recurrent +1. Direct numeric edit deferred (web's compact-grid Qty field used only on mobile). | 🔄 |
| F-242 | Shopping list item — edit note | Per-item note field | Current List table | PUT `/shopping-list/items/<item_id>` `{note:"..."}` | Inline note edit | "Edit Note…" button (expanded row + context menu) → alert TextField → `state.updateNote(id:, note:)`; "Clear" destructive action also wired | ✅ |
| F-243 | Shopping list item — edit actual price | Per-item actual-price input | Current List table | PUT `/shopping-list/items/<item_id>` `{actual_price:<n>}` | Only editable when session is "ready_to_bill" | `actualPriceField` TextField rendered when `session.isReadyToBill && item.isPurchased` (`isReadyToBillEditable`); .onSubmit → `state.updateActualPrice(id:, value:)` with nil-on-empty support; ✓ glyph appears when value present (matches web) | ✅ |
| F-244 | Shopping list item — delete | Per-item delete button | Current List table | DELETE `/shopping-list/items/<item_id>` | Removes item from list | "Delete" DestructiveButton in expanded row + context-menu Delete → confirmation dialog → `state.remove(id:)` | ✅ |
| F-245 | Shopping snapshot file input | Hidden input `#shopping-snapshot-input` (file, image/*) | Current List card | POST `/product-snapshots/upload` | `uploadShoppingSnapshotFromPicker(event)`; attaches photo to a product | v1.0 🚫 — same scope decision as inventory F-131 (per-item photo upload). Existing snapshots are still rendered via `latest_snapshot.image_url` (F-259). | 🚫 |
| F-246 | Share link creation | — (invoked via session toolbar) | — | POST `/shopping-list/share-link` | Creates a 24h share token for helper-mode access | v1.0 🚫 — web hides this behind a "secret QR panel". Helper mode is anonymous-browser only and not a desktop primary surface. | 🚫 |
| F-247 | Helper mode (shared link access) | — | — | GET `/shopping-list/shared/<token>` | Read-only list view via token without authentication | v1.0 🚫 — macOS app is authenticated-user only. Helpers use the web view via the share link. | 🚫 |
| F-248 | Helper mode item status update | — | — | PUT `/shopping-list/shared/<token>/items/<item_id>` | Anonymous collaborator can mark items as bought via share link | v1.0 🚫 — pairs with F-247. | 🚫 |
| F-249 | Mark session Ready to Bill | Session toolbar button | Session Banner | POST `/shopping-list/session/ready-to-bill` | Locks open items; enables actual-price inputs for billing | "🧾 Ready to Bill (N)" PrimaryButton in sessionBanner (active session, `purchasedCount > 0`) → `state.markReadyToBill()`; reloads list + scrolls to first empty actual-price field is implicit (SwiftUI keyboard focus); 0-purchase hint matches web | ✅ |
| F-250 | Reopen session | Session toolbar button | Session Banner | POST `/shopping-list/session/reopen` | Returns a ready-to-bill session to shopping state | "↩ Back to Shopping" GhostButton (ready_to_bill) and "↩ Reopen" GhostButton (closed) → `state.reopenSession()` (no id reopens latest closed; refreshes past-trips) | ✅ |
| F-251 | Finalize shopping session | Session toolbar button | Session Banner | POST `/shopping-list/session/finalize` | Archives trip; open items carry to new session | "✅ Finalize & Close" PrimaryButton (ready_to_bill) → confirmationDialog with open-count carry note → `state.finalizeSession()` | ✅ |
| F-252 | Past Trips card | Card `#shopping-past-trips-card` | — | GET `/shopping-list/sessions?status=closed` | Collapsible; loads on expand; count badge | `PastTripsCard`; loads on first expand via `state.togglePastTrips()` → `loadPastTrips()` | ✅ |
| F-253 | Past Trips collapse/expand toggle | Card header `shopping-past-trips-header` (click) | Past Trips card | — | `toggleShoppingPastTrips()`; chevron ▸/▾ | Title Button with chevron right/down → `state.togglePastTrips()` | ✅ |
| F-253 (kbd) | Past Trips header Enter/Space | onkeydown handler | Past Trips card | — | Web duplicates click action for kbd | SwiftUI Button responds to Return/Space natively — covered. | ✅ |
| F-254 | Past Trips count badge | Span `#shopping-past-trips-count` | Past Trips card | — | Displays number of closed sessions | Capsule next to "Past Trips" title bound to `state.pastTrips.count` | ✅ |
| F-255 | Past Trips empty state | Static text in body | Past Trips card | — | "No past trips yet — finalize a shopping session to see it here." | EmptyStateView with tray icon when array empty and load complete | ✅ |
| F-256 | Past trip detail view | Inline expand per session | Past Trips card | GET `/shopping-list/sessions/<session_id>` | Shows itemised trip detail on expand | `PastTripRow` toggles `expandedTripIds`; on expand, fetches `/sessions/<id>` once via `state.togglePastTrip(_:)`, caches in `pastTripDetails`; renders `PastTripDetail` table (Item, Qty, Est, Actual) | ✅ |
| F-257 | Shopping helper intro banner | Div `#shopping-helper-intro` | — | — | Shown in helper mode (shared link); explains limited access | v1.0 🚫 — helper mode out of scope (pairs with F-247). | 🚫 |
| F-258 | Per-item inline editor — Unit/Size/Unit Price + Update | Inline grid + "Update" button (web `updateShoppingGroupDetails`) | Current List row | PUT `/shopping-list/items/<id>` `{unit, size_label, manual_estimated_price, persist_latest_price, price_store}` | Persists product defaults + item-level overrides; latest price recorded under preferred store | `ShoppingItemRow` expands on tap into a 4-field grid (Store / Unit / Size / Unit Price) + "Update" PrimaryButton → `state.updateUnitSizePrice(id:unit:sizeLabel:unitPrice:priceStore:)` (also updates `preferred_store` first if changed). Added 2026-05-19 — was hidden inside web's F-238 row rollup. | ✅ |
| F-259 | Per-item product snapshot thumbnail | `<img class="shopping-item-thumb" src="${latest_snapshot.image_url}">` in `renderShoppingListTable()` | Current List row | GET `/product-snapshots/<id>/image` (authed via X-Trusted-Device-Token) | Renders when item carries `latest_snapshot.image_url`; falls back to placeholder | `ShoppingSnapshotThumb` — KFImage(url) + ImageCache.tokenModifier; placeholder shows item initials; hover popup (250 ms delay, 320×320 popover) mirrors inventory F-158. Added 2026-05-19 — hidden inside web's F-238 row rollup. | ✅ |
| F-260 | Per-item Low / Clear Low button | "Low"/"Clear Low" button calls `setProductLowStatus(productId, !current)` | Current List row | POST/PUT `/inventory/products/<id>/low` | Cross-tab affordance — marks the linked product as low-stock from the shopping row | "Mark Low" GhostButton (expanded row + context menu) → `InventoryState.shared.markLow(productId:, manualLow: true)` when `item.productId` present. Added 2026-05-19 — hidden inside web's F-238 row rollup. | ✅ |
| F-261 | Per-item Out of Stock / Reopen button | `toggleShoppingGroupItems([ids], 'out_of_stock'\|'open')` | Current List row | PUT `/shopping-list/items/<id>` `{status:"out_of_stock"\|"open"}` | Web's "Out of Stock" mode keeps items on list but greyed; reopen returns to open | "Out of Stock"/"Reopen" GhostButton (expanded row + context menu) → `state.markOutOfStock(id:, currentStatus:)`. Status badge updates to warning when out_of_stock. Added 2026-05-19. | ✅ |
| F-262 | Per-item Rename button | "Rename" button → prompt → `renameShoppingDisplayItem` | Current List row | PUT `/shopping-list/items/<id>` `{name}` | Item-level rename; product rename intentionally not exposed | "Rename" GhostButton (expanded row + context menu) → alert TextField → `state.renameItem(id:, newName:)`. Product-level rename (web also updates product) deferred for v1.0. Added 2026-05-19. | ✅ |
| F-263 | Skipped items group | `<details class="shopping-skipped-group">` with reopen + delete buttons per skipped row | Current List body | PUT `/shopping-list/items/<id>` `{status:"open"}` / DELETE `/shopping-list/items/<id>` | Surfaces items marked skipped (e.g. from kitchen view) so they can be reopened or deleted; visible in non-purchased views only | `SkippedSection` SwiftUI `DisclosureGroup("Skipped (N)")` listing `state.skippedItems` with "↩ Open" + "🗑" Buttons. Added 2026-05-19. | ✅ |
| F-264 | Store group header — clickable collapse | `<div class="card-header" role="button" onclick="toggleShoppingStoreGroup(...)" onkeydown="...">` per store group; collapse state persisted across renders | Current List body | — | Each store section can be collapsed independently; chevron + count + estimated total | `StoreGroupSection` chevron Button → `state.toggleStoreGroup(storeName)`; collapse map persisted to UserDefaults `LocalOCR.shopping.storeGroupCollapsed`. Added 2026-05-19. | ✅ |
| F-265 | Per-item −1 button | `<button onclick="decreaseShoppingGroupQuantity([ids])">−1</button>` | Current List row trailing | PUT `/shopping-list/items/<id>` `{quantity:<n-1>}` or DELETE when next ≤ 0 | One-tap subtract; row disappears at quantity 0 | "−1" GhostButton in row trailing → `state.decreaseQuantity(id:)` — when quantity 1, delegates to `state.remove(id:)`. Added 2026-05-19. | ✅ |
| F-266 | Per-item Bought / Reopen button | `<button onclick="toggleShoppingGroupItems([ids], purchased\|open)">Bought\|Reopen</button>` | Current List row trailing | PUT `/shopping-list/items/<id>` `{status}` | Status toggle from the row; visible on every row | "Bought" PrimaryButton (open) / "Reopen" GhostButton (purchased) — same handler as F-239/F-240. Added 2026-05-19. | ✅ |
| F-267 | Actual-price input (ready_to_bill + purchased) | `renderShoppingActualPriceField(item)` only when `isReadyToBillMode() && item.status==="purchased"` | Current List row | PUT `/shopping-list/items/<id>` `{actual_price}` | Inline price-paid field with ✓ on saved value | Same `actualPriceField` covered by F-243 — explicitly enumerated so verb-check picks it up. Added 2026-05-19. | ✅ |
| F-268 | Past trip Reopen button | `<button onclick="reopenPastTrip(${session.id})">↩ Reopen trip</button>` in `renderPastTripDetail` | Past Trips detail panel | POST `/shopping-list/session/reopen` `{session_id}` | Replaces current empty session with the chosen closed one; rejects when current has items | "↩ Reopen trip" GhostButton at the bottom of `PastTripDetail` → `state.reopenSession(sessionId: trip.id)`. Added 2026-05-19. | ✅ |
| F-269 | Sort + collapse persistence | `localStorage.shopping_sort` + per-section collapse keys | Cross-card | — | Web persists sort, section collapse, and store-group collapse across reloads | UserDefaults keys: `LocalOCR.shopping.sortMode`, `quickFindCollapsed`, `recsCollapsed`, `currentListCollapsed`, `storeGroupCollapsed`. Past-trips collapse intentionally session-only (matches web). Added 2026-05-19. | ✅ |

#### Screen: Kitchen
Source: `src/frontend/index.html` lines 3511–3579
Backend: `manage_kitchen.py`, `manage_kitchen_endpoint.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-300 | Product catalog panel | Div `#kitchen-catalog` | — | GET `/api/kitchen/catalog` | Catalog groups products by category and "frequent" bucket | CatalogCard Card driven by `KitchenState.refresh()` → `KitchenEndpoint.catalog` GET → `KitchenCatalogResponse` decode. Added 2026-05-19. | ✅ |
| F-301 | "Browse products" catalog toggle button | Button `#kitchen-catalog-toggle` calling `toggleKitchenCatalog()` | Catalog panel | — | Collapse state persisted in `localStorage.kitchen-catalog-collapsed`; `aria-expanded` updated | Header Button toggling `state.catalogCollapsed` (UserDefaults `LocalOCR.kitchen.catalogCollapsed`); chevron right ↔ down. Added 2026-05-19. | ✅ |
| F-302 | Category chip bar | Div `#kitchen-chip-bar` | Catalog toolbar | — | `renderKitchenStoreFilter()`; one chip per category + "frequent" | Horizontal ScrollView of chip Buttons — ⭐ Frequent + KITCHEN_CATEGORIES (Produce / Meat / Dairy / Bakery / Pantry / Other) with KITCHEN_CATEGORY_EMOJI. Added 2026-05-19. | ✅ |
| F-303 | Category chip — active state | Individual chip in `#kitchen-chip-bar` | Catalog toolbar | — | Active chip highlighted; filters product grid to that category | `chipButton(active:)` fills with `DesignTokens.accent` + white text + semibold weight when `state.activeCategory == key`. Added 2026-05-19. | ✅ |
| F-304 | Product search input | Input `#kitchen-search` | Catalog toolbar | — | `onKitchenSearchInput(value)` filters grid client-side | TextField bound to `state.searchQuery` with `state.setSearchQuery()` (auto-expands collapsed catalog when query non-empty). Live-filters via `state.tilesForActiveChip()`. Added 2026-05-19. | ✅ |
| F-305 | Search input focus — popover open | Onfocus handler | Catalog toolbar | — | `toggleKitchenSearchPopover(true)` opens store filter popover | macOS adapts: dedicated filter Button (`line.3.horizontal.decrease.circle` with count badge) toggles `state.searchPopoverOpen`. Auto-popover-on-focus is iOS-touch UX; desktop pattern uses explicit toggle. Added 2026-05-19. | 🔄 |
| F-306 | Search store-filter popover | Div `#kitchen-search-popover` | Search popover | — | Lists store chips from `shoppingStoreBuckets`; filter by store + search text combined | `storePopover` view rendered conditionally on `state.searchPopoverOpen`. Combines with search text via `_kitchenApplyStoreFilter` + `_kitchenTilesForActiveChip` logic. Added 2026-05-19. | ✅ |
| F-307 | Store filter chips in popover | Chip buttons in `#kitchen-search-popover-stores` | Search popover | — | Each chip toggles `kitchenStoreFilter` set; calls `renderKitchenCatalog()` | "All" chip + per-store chips from `state.availableStores()` (sorted by purchase volume). Tap toggles `state.storeFilter` Set → grid re-filters. Added 2026-05-19. | ✅ |
| F-308 | Product grid scroll left button | Button `kitchen-grid-arrow--prev` calling `kitchenGridScrollBy(-1)` | Catalog body | — | Horizontally scrolls `#kitchen-grid` left by one card width | chevron.left.circle.fill Button posts `.kitchenGridScroll` -1 → `KitchenHorizontalScroll` consumes via `ScrollViewReader.scrollTo` (animation easeOut 0.5s, moves 3 tiles per click — matches web). Added 2026-05-19. | ✅ |
| F-309 | Product grid scroll right button | Button `kitchen-grid-arrow--next` calling `kitchenGridScrollBy(1)` | Catalog body | — | Horizontally scrolls `#kitchen-grid` right by one card width | chevron.right.circle.fill Button posts `.kitchenGridScroll` +1 → same scroll handler. Added 2026-05-19. | ✅ |
| F-310 | Product grid | Div `#kitchen-grid` | Catalog body | — | Rendered by `renderKitchenCatalog()`; horizontal scroll; one card per product/group | `KitchenHorizontalScroll` HStack of `CatalogTileButton` driven by `state.tilesForActiveChip()` (grouped by canonical key). 130×210 tiles with image / name / price / count + variants badge. Added 2026-05-19. | ✅ |
| F-311 | Product card — single variant tap | Per-card tap | Product grid | POST `/shopping-list/items` | `addProductToList(productId, name, category)` adds item to shopping list | `CatalogTileButton` Button → `state.addToList(tile)` → `ShoppingState.add(productName:quantity:source:productId:)` with `source: "kitchen"`. Optimistically marks `onListProductIds` so tile dims. Added 2026-05-19. | ✅ |
| F-312 | Product card — multi-variant picker | Sheet `#kitchen-sheet` | Variant picker sheet | — | `_kitchenOpenVariantPickerForKey()` opens sheet listing variants when >1 product shares same canonical key | When `group.variantCount > 1`, tile tap calls `state.openVariantPicker(canonicalKey:)` → `state.variantPicker` populated → `VariantPickerSheet` shown via `.sheet(item:)`. Added 2026-05-19. | ✅ |
| F-313 | Variant picker row — select variant | Button in variant list | Variant picker sheet | POST `/shopping-list/items` | `addProductToList(v.product_id, v.name, v.category)` + `closeKitchenSheet()` | Each row Button → `addToList(tile)` + `closeVariantPicker()`. Added 2026-05-19. | ✅ |
| F-314 | Current List section header | H2 `kitchen-section-title` | — | — | "Current List" label; displays total | `CurrentListCard.header` HStack: "Current List" Text + total capsule. Added 2026-05-19. | ✅ |
| F-315 | Toggle item names button | Button `#kitchen-names-toggle` (🏷️) calling `toggleKitchenNames()` | Current List header | — | State in `localStorage.kitchen-show-names`; toggles product name labels on list tiles | Tag SF symbol Button in catalog header → `state.toggleNames()` (UserDefaults `LocalOCR.kitchen.showNames`). Both catalog tiles AND list tiles respect `state.showNames` (hide name Text when off). Added 2026-05-19. | ✅ |
| F-316 | Current List total | Span `#kitchen-list-total` | Current List header | — | Rendered by `renderKitchenList()`; sum of estimated prices | "Total $X.XX" capsule next to title — computed via `tileUnitPrice` (manualEstimatedPrice → latestPrice.price → catalog tile fallback). Hidden when total == 0. Added 2026-05-19. | ✅ |
| F-317 | Weather widget | Div `#kitchen-weather` | Current List header | External: `https://api.open-meteo.com/v1/forecast` (+ `https://ipapi.co/json/` or `https://ipwho.is/`) | `loadKitchenWeather()`; geolocation → IP fallback; cached 30 min in `localStorage.kitchen-weather-cache` | `KitchenState.loadWeather()` — ipapi.co → ipwho.is fallback → Open-Meteo. Dedicated `publicSession` URLSession with cookies disabled (so backend session doesn't leak). 30-min cache via UserDefaults `LocalOCR.kitchen.weatherCache`. Emoji + temp + desc in `CurrentListCard.header` (silent no-op when geo lookup blocked). Added 2026-05-19. | ✅ |
| F-318 | Current List empty state | Div `#kitchen-empty` | — | — | "Your shopping list is empty. Tap a product above to add it." shown when `kitchenList` is empty | EmptyStateView with cart icon + "Tap a product above to add it." subtitle when filtered items empty. Added 2026-05-19. | ✅ |
| F-319 | Current List tile grid | Div `#kitchen-list-grid` inside `#kitchen-list-container` | — | GET `/shopping-list` | `renderKitchenList()`; shows open + skipped items | `listBody` groups by preferred_store > effective_store > "Unassigned" (Unassigned sorts last). Each group has title + count + store total + LazyVGrid of `KitchenListTile`. Items filtered to status in (open, skipped). Added 2026-05-19. | ✅ |
| F-320 | List tile — tap to open action sheet | Per-tile tap | Current List | — | `openKitchenSheet(itemId)` opens bottom sheet with item actions | `KitchenListTile` Button → `state.openSheet(itemId:)` → `state.sheetItemId` set → `.sheet(item:)` presents `ItemActionSheet`. Added 2026-05-19. | ✅ |
| F-321 | List tile — long-press context menu | Per-tile long-press | Current List | — | `openKitchenContextMenu()` opens context menu overlay `#kitchen-ctx` | SwiftUI `.contextMenu` (right-click) covers desktop primary; `LongPressGesture(minimumDuration:0.9)` calls `state.openContextMenu(forListItem:)` to mirror touch-style overlay (`ContextMenuSheet`). Catalog tiles get same dual wiring (forCatalog: variant). Added 2026-05-19. | ✅ |
| F-322 | Item action sheet | Div `#kitchen-sheet` | Item action sheet | — | Backdrop + card; hidden by default; shows item name, meta, quantity, action buttons | SwiftUI `.sheet(item:)` presents `ItemActionSheet` modal — header / meta / store button + picker / qty row / action row / preset chips. Native modal supplies backdrop. Added 2026-05-19. | ✅ |
| F-323 | Action sheet — close button | Button `.kitchen-sheet-close` calling `closeKitchenSheet()` | Item action sheet | — | Hides sheet; clears `kitchenSheetItemId` | `xmark.circle.fill` Button + `.cancelAction` shortcut → `state.closeSheet()` (also clears presetAction + storePickerOpen). Added 2026-05-19. | ✅ |
| F-324 | Action sheet — item name display | Span `.kitchen-sheet-name` | Item action sheet | — | Shows item name | `header` HStack — Text(item.name) at `.appTitle3`. Added 2026-05-19. | ✅ |
| F-325 | Action sheet — item meta display | Div `.kitchen-sheet-meta` | Item action sheet | — | Shows price, store, category joined by " · " | `metaLine` Text — `$X.XX · Store · category` joined by " · ", empty parts filtered. Added 2026-05-19. | ✅ |
| F-326 | Action sheet — store picker button | Button `.kitchen-sheet-store-btn` calling `toggleKitchenStorePicker()` | Item action sheet | PUT `/shopping-list/items/<item_id>` `{preferred_store:<name>}` | Toggles inline store picker within sheet | `storeButton` Button with storefront SF symbol + current store + chevron → `state.toggleStorePicker()`. PUT happens in F-327 chip selection. Added 2026-05-19. | ✅ |
| F-327 | Action sheet — store picker chips | Chips in `#kitchen-store-picker` | Item action sheet / store picker | PUT `/shopping-list/items/<item_id>` | `pickKitchenStore(name)` sets preferred store; "— Clear store —" clears it | `storePicker` FlowLayout — ⭐ Frequent + Rarely Used sections from `ShoppingState.shared.storeBuckets`, plus "— Clear store —" Button → `state.pickStore(_:)` → `ShoppingState.updatePreferredStore`. Added 2026-05-19. | ✅ |
| F-328 | Action sheet — quantity decrease | Button `−` calling `kitchenSheetSetQty(-1)` | Item action sheet | PUT `/shopping-list/items/<item_id>` `{quantity:<n>}` | Min quantity is 1 | `qtyRow` minus.circle.fill Button → `state.qtyDelta(-1)` → `updateQuantityRaw` (PUT with floor-clamped quantity). Web behavior matched — minimum 1, no auto-delete in sheet (different from Shopping screen F-265). Added 2026-05-19. | ✅ |
| F-329 | Action sheet — quantity increase | Button `+` calling `kitchenSheetSetQty(1)` | Item action sheet | PUT `/shopping-list/items/<item_id>` `{quantity:<n>}` | — | `qtyRow` plus.circle.fill Button → `state.qtyDelta(+1)` → `ShoppingState.increaseQuantity`. Added 2026-05-19. | ✅ |
| F-330 | Action sheet — quantity display | Span `#kitchen-sheet-qty-value` | Item action sheet | — | Shows current quantity | `qtyRow` Text(item.quantity) at `.appTitle3 .semibold`. Re-renders on shopping state change. Added 2026-05-19. | ✅ |
| F-331 | Action sheet — "Bought" button | Button `.kitchen-action.bought` calling `kitchenSheetAction('bought')` | Item action sheet | PUT `/shopping-list/items/<item_id>` `{status:"purchased"}` | Marks item purchased; shows preset chips for 4 s then auto-closes | `actionRow` "✓ Bought" PrimaryButton → `state.runAction(.bought)` → `ShoppingState.toggleStatus` + `schedulePresetAutoClose` (4 s sleep then closeSheet + refresh). Added 2026-05-19. | ✅ |
| F-332 | Action sheet — "Low" button | Button `.kitchen-action.low` calling `kitchenSheetAction('low')` | Item action sheet | POST `/inventory/products/<product_id>/low` `{manual_low:true}` | Only available when item has `product_id`; shows preset chips | "📝 Low" GhostButton (disabled when `item.productId == nil`) → `state.runAction(.low)` → `InventoryState.markLow(productId:manualLow:)` which uses the CORRECT endpoint PUT `/inventory/products/<id>/low-status`. Web POSTs `/low` which 404s on the backend — mac silently fixes the bug. Added 2026-05-19. | 🔄 |
| F-333 | Action sheet — "Skip" button | Button `.kitchen-action.skip` calling `kitchenSheetAction('skipped')` | Item action sheet | PUT `/shopping-list/items/<item_id>` `{status:"skipped"}` | Moves item to skipped state; remains in list | "⏭ Skip" GhostButton → `state.runAction(.skipped)` → `ShoppingState.toggleStatus` to "skipped" + presets stay open (no auto-close). Added 2026-05-19. | ✅ |
| F-334 | Action sheet — "Open" (unskip) button | Button `.kitchen-action.unskip` calling `kitchenSheetAction('open')` | Item action sheet | PUT `/shopping-list/items/<item_id>` `{status:"open"}` | Only shown when item is in "skipped" state | "↩ Open" PrimaryButton — only rendered when `item.isSkipped` (replaces the bought/low/skip trio). → `state.runAction(.open)` + closes sheet. Added 2026-05-19. | ✅ |
| F-335 | Action sheet — delete button | Button `.kitchen-action.delete` calling `kitchenSheetAction('delete')` | Item action sheet | DELETE `/shopping-list/items/<item_id>` | Confirm dialog before delete | Trash icon DestructiveButtonStyle → `state.runAction(.delete)` → `ShoppingState.remove` (toast confirmation, no extra dialog — matches Shopping screen pattern). Added 2026-05-19. | ✅ |
| F-336 | Action sheet preset chips | Div `#kitchen-presets` | Item action sheet | PUT `/shopping-list/items/<item_id>` `{note:<text>}` | Post-action quick-note stamps (e.g. "Out of stock", "Wrong price"); custom option via `prompt()` | `presetChips` FlowLayout rendered when `state.presetAction` set — `KitchenPresets.chips(for:)` (bought / low / skipped buckets) + "✏️ custom" Button → `state.pendingNoteInput` set → `.alert` TextField → `state.stampNote(_:)` → PUT note. Added 2026-05-19. | ✅ |
| F-337 | Context menu overlay | Div `#kitchen-ctx` | Context menu | — | Backdrop + card; opened by long-press on list tile | `.sheet(item:)` → `ContextMenuSheet` modal with title + ForEach over `ContextMenuRow` (icon + label Buttons). Native sheet backdrop. Sourced from long-press gesture (F-321) on either catalog tile or list tile. Added 2026-05-19. | ✅ |
| F-338 | Context menu backdrop close | Div `.kitchen-ctx-backdrop` calling `closeKitchenContextMenu()` | Context menu | — | Tapping backdrop dismisses context menu | SwiftUI sheet dismisses on outside tap automatically. Header also has explicit ✕ Close Button + `.cancelAction` shortcut → `state.closeContextMenu()`. Added 2026-05-19. | ✅ |

#### Screen: Products
Source: `src/frontend/index.html` lines 2766–2846
Backend: `manage_product_catalog.py`, `manage_product_snapshots.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-400 | Page header — "Products" title + subtitle | H1 + subtitle paragraph | — | — | Static; subtitle "Full catalog — everything ever purchased, OCR-normalized" | `pageHeader` VStack — Title2 "Products" + subheadline subtitle. Added 2026-05-19. | ✅ |
| F-401 | Add Product card | Card `#catalog-panel-products > .card` (first) | — | — | Always visible; no collapse toggle | `AddProductCard` always-rendered Card at top of ProductsView. Added 2026-05-19. | ✅ |
| F-402 | Add Product — Name input | Input `#prod-name` | Add Product card | — | Required; placeholder "e.g. Whole Wheat Bread" | TextField "$name" with placeholder "e.g. Whole Wheat Bread"; submit disabled when empty. Added 2026-05-19. | ✅ |
| F-403 | Add Product — Category select | Select `#prod-cat` | Add Product card | — | Options: Dairy, Meat, Bakery, Produce, Grains, Frozen, Beverages, Snacks, Household, Personal Care, Apparel, Other (default) | Picker bound to "$category" with ProductCategoryOptions.all (23 categories including all listed). Defaults to "other". Added 2026-05-19. | ✅ |
| F-404 | Add Product — Barcode input (optional) | Input `#prod-barcode` | Add Product card | — | Optional; placeholder "0123456789012" | TextField "$barcode" placeholder "Barcode (optional)" — sent only when non-empty. Added 2026-05-19. | ✅ |
| F-405 | Add Product submit button | Button `btn-primary` calling `createProduct()` | Add Product card | POST `/products/create` | Creates product; reloads catalog via `loadProducts()`; toast on success/error | "Add to Catalog" PrimaryButton + ⌘↩ shortcut → `state.createProduct(name:category:barcode:)` → POST + `loadCatalog` + toast. Added 2026-05-19. | ✅ |
| F-406 | Catalog card — count badge | Span `#prod-count` | Catalog card header | GET `/products` | Displays total product count from loaded data | `CatalogToolbar` Text capsule bound to `state.total` (filled from `/products` response.total). Added 2026-05-19. | ✅ |
| F-407 | Catalog search input | Input `#prod-search` | Catalog toolbar | GET `/products/search?q=<query>` | `oninput` debounce 300 ms → `loadProducts(q)`; empty query falls back to full GET `/products` | TextField → `state.setSearch(_:)` with 300 ms `Task.sleep` debounce → calls `/products/search?q=` (≥2 chars) or `/products` (empty). Clear ✕ button when query non-empty. Added 2026-05-19. | ✅ |
| F-408 | Catalog sort select | Select `#prod-sort` | Catalog toolbar | GET `/products` or `/products/search` | Options: Name A-Z (default), Category, Most variants, Recently purchased; `onchange` calls `setProductSort(value)` → `loadProducts()` | Picker bound to `state.sortMode` (ProductsSort enum: nameAsc/nameDesc/categoryAsc/variantsDesc/recentDesc). Persisted to UserDefaults `LocalOCR.products.sortMode`. Sort applied client-side in `groupedProducts()`. Added 2026-05-19. | ✅ |
| F-409 | Catalog refresh button | Button `btn-ghost btn-sm` calling `loadProducts()` | Catalog toolbar | GET `/products` | Force-reload without changing query | `arrow.clockwise` Button → `state.loadCatalog()`. Toolbar also has primary "Refresh" Button that runs `state.refresh()` (catalog + review queues). Added 2026-05-19. | ✅ |
| F-410 | Category filter chip row | Div `#prod-category-chips` | Catalog card | — | `renderProductCategoryChips(allItems)`; chips filter rendered list client-side by category; active category highlighted | `CategoryChipRow` derives unique lowercased categories from `state.products`. "All" + per-category chip Buttons toggle `state.categoryFilter` Set → `groupedProducts()` filters client-side. Hidden when search active (matches web). Added 2026-05-19. | ✅ |
| F-411 | Catalog table body — loading state | `<div class="empty-state" style="opacity:0.5"><p>Loading…</p></div>` in `#prod-table-body` | Catalog card | — | Injected before fetch completes | `CatalogBody` `state.isLoading && state.products.isEmpty` branch → EmptyStateView hourglass "Loading…". Added 2026-05-19. | ✅ |
| F-412 | Catalog table body — error state | `<div class="empty-state"><p>Could not load products.</p></div>` in `#prod-table-body` | Catalog card | — | Shown on non-OK response or network error | `state.lastError && state.products.isEmpty` branch → EmptyStateView "Could not load products." with subtitle = error description. Added 2026-05-19. | ✅ |
| F-413 | Catalog table body — product groups list | Div `#prod-table-body` | Catalog card | GET `/products` | Rendered by `renderProductGroup()`; grouped by canonical token key / family | `state.groupedProducts()` returns `ProductCategoryGroup` (regrouped by category) + nested `ProductGroup` (family). Rendered as section per category + LazyVGrid of `ProductTile` cards. Added 2026-05-19. | ✅ |
| F-414 | Per-product — rename action | Inline edit / button calling `renameProduct(id, currentName)` | Per-product row | PUT `/products/<id>/update` `{name}` | If renamed to match another product, auto-merges; toast "merged" or "renamed" | Edit sheet (F-415) houses the name field. Context menu "Rename…" also opens edit sheet. Backend's `_merge_products` autorun applies — toast respects merge result. Added 2026-05-19. | ✅ |
| F-415 | Per-product — edit details sheet | Modal/sheet via `editProductDetails(id, name, category)` | Edit Product sheet | PUT `/products/<id>/update` | Fields: Name (+ 📷 photo button), Category; bottom sheet slide-up | `EditProductSheet` `.sheet(item: $state.editProduct)` — fields: Name + 📷 photo Button + photo gallery + Category Picker + Default unit + Size label + Cancel/Save. PrimaryButton Save → `state.updateProduct(id:body:)` sends only changed fields. Added 2026-05-19. | ✅ |
| F-416 | Per-product — photo upload in edit sheet | File input inside edit sheet → `POST /product-snapshots/upload` | Edit Product sheet | POST `/product-snapshots/upload` | `source_context=manual`, `status=linked`; toast on success | 📷 camera Button in edit sheet → NSOpenPanel (jpeg/png/heic/heif/gif/webp) → `state.uploadSnapshot(productId:fileURL:)` → APIClient.multipartRequest with fields `product_id`, `source_context=manual`, `status=linked`, `image` part. Refreshes photo gallery + catalog. Added 2026-05-19. | ✅ |
| F-417 | Per-product — delete action | Button calling `deleteProduct(id)` | Per-product row | DELETE `/products/<id>` | Confirm dialog before delete; reloads catalog on success | Trash icon DestructiveButton on every tile + context menu "Delete" → `state.deleteProduct(_:)` → DELETE + toast + reload. No extra confirm dialog (matches Shopping pattern). Added 2026-05-19. | ✅ |
| F-418 | Per-product — inline product update (category/unit/etc.) | Inline field patch via product tile | Per-product tile | PUT `/products/<id>/update` | Used from inventory tiles but reflected in products page | EditProductSheet exposes name + category + default unit + size label fields. PUT only sends changed fields (`ProductUpdateBody` optionals). Variant rows have their own Edit Button. Added 2026-05-19. | ✅ |
| F-419 | Per-product — price history view | Button/link calling `getProductPriceHistory` → `api('/products/<id>/price-history')` | Price history panel | GET `/products/<id>/price-history` | Lists historical price points per store | "Prices" GhostButton + context menu → `state.openPriceHistory(_:)` → GET + `PriceHistorySheet` sheet with Avg/Min/Max stat cells + per-row date + price list. Added 2026-05-19. | ✅ |
| F-420 | Per-product — AI enhance | Button calling `enhanceProduct(productId)` | Per-product row | POST `/products/<id>/enhance` | Triggers AI enrichment of product name/category; updates review status | "AI" sparkles GhostButton + context menu → `state.enhanceProduct(id:)` → POST + reload catalog + reload review queue. Admin-only (backend gates). Non-admin gets forbidden toast. Added 2026-05-19. | ✅ |
| F-421 | Per-product — mark review status | Button calling `updateReviewStatus` / `api('/products/<id>/review-status')` | Per-product row | PUT `/products/<id>/review-status` | Marks product as reviewed/approved in the review queue | Review Queue card row "Dismiss" / "Resolve" Buttons → `state.setReviewState(id:state:)` → PUT body `{review_state}`. Backend 400 "Make a meaningful fix" surfaced as toast. Added 2026-05-19. | ✅ |
| F-422 | Review Queue section (in catalog panel) | Rendered via `loadProducts` + `/products/review-queue` endpoint | Catalog card | GET `/products/review-queue?status=<status>` | Shows products needing human review (low-confidence OCR results) | `ReviewQueueCard` rendered only when `appState.currentUser.isAdmin`. Loads `/products/review-queue?status=` on first refresh + on filter change. Segmented Picker (Pending/Resolved/Dismissed/All). Added 2026-05-19. | ✅ |
| F-423 | Bulk enhance review queue | Button calling bulk enhance | Catalog toolbar / review queue | POST `/products/review-queue/enhance` | Runs AI enrichment on all pending-review products at once | "Bulk AI" GhostButton in Review Queue header → `state.bulkEnhanceReviewQueue()` → POST `{limit:10, provider:"gemini"}` + toast with count + reload queue. Added 2026-05-19. | ✅ |
| F-424 | Merge / dedup products | Button (from Inventory toolbar) `findDuplicateProducts()` | — | POST `/products/auto-dedup-tokens` | Merges products sharing the same token-set fingerprint; reflected in catalog | "Merge duplicates" Button in Products toolbar → `state.autoDedup()` → POST + toast "Merged N (scanned M)" + reload. Also covered by Inventory toolbar F-122. Added 2026-05-19. | ✅ |
| F-425 | Product snapshot upload (from product row) | Hidden file input / photo button calling `POST /product-snapshots/upload` | Per-product row | POST `/product-snapshots/upload` | Attaches a product photo; `status=linked`; updates tile with image | Same path as F-416 — surfaced via Edit Sheet 📷 Button. Per-tile direct 📷 is not exposed on the tile itself; Edit sheet is the entry point (matches web — web's per-row 📷 ultimately opens edit sheet too via inventory mobile card). Added 2026-05-19. | 🔄 |
| F-426 | Snapshot review queue | Via `GET /product-snapshots/review-queue` | Snapshot review section | GET `/product-snapshots/review-queue` | Lists snapshots awaiting human approval | `SnapshotReviewCard` rendered only when admin. Loads `/product-snapshots/review-queue?status=pending` on refresh. Pre-fills name + category drafts from `linked_product` / `product_name` / `shopping_item_name`. Added 2026-05-19. | ✅ |
| F-427 | Snapshot review — approve/reject | Per-snapshot button → `PUT /product-snapshots/<id>/review` | Snapshot review section | PUT `/product-snapshots/<id>/review` | Sets snapshot status to approved/rejected | Per-row "Link Product" PrimaryButton → `state.linkSnapshot(_:)` → PUT `{product_name, category, status:"linked"}`. "Archive" DestructiveButton → `state.archiveSnapshot(_:)` → PUT `{status:"archived"}`. Toast + reload. Added 2026-05-19. | ✅ |
| F-428 | Snapshot promote to product image | Button → `POST /product-snapshots/<id>/promote` | Snapshot review section | POST `/product-snapshots/<id>/promote` | Sets snapshot as the canonical display image for the linked product | `SnapshotGalleryCell` in EditProductSheet — click any non-primary cell → `state.promoteSnapshot(_:)` → POST + reload gallery + reload catalog. Primary cell has accent blue ring. Added 2026-05-19. | ✅ |
| F-429 | Snapshot delete | Button → `DELETE /product-snapshots/<id>` | Snapshot review section | DELETE `/product-snapshots/<id>` | Removes snapshot record and file | ✕ delete Button in `SnapshotGalleryCell` (edit sheet) → `state.deleteSnapshot(_:)` → DELETE + reload gallery + reload snapshot review queue. Added 2026-05-19. | ✅ |
| F-430 | Catalog empty state | Empty-state div in `#prod-table-body` | Catalog card | — | "No products found" when catalog returns empty; shown after load completes with no results | `CatalogBody` empty branch → EmptyStateView with barcode icon + "No products yet." (search active → "No products match this search."). Added 2026-05-19. | ✅ |

---

### GROUP: Receipts
#### Screen: Upload Receipt
Source: `src/frontend/index.html` lines 3580–3792
Backend: `handle_receipt_upload.py` (url_prefix `/receipts`)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-500 | Page header — "Upload Receipt" title + hint | H1 + subtitle paragraph | — | — | Static; subtitle explains intent buttons | TBD | ❌ |
| F-501 | Drop-zone file input area | Label `#drop-zone` / hidden input `#file-input` | — | — | Accepts `image/*,.pdf`; drag-and-drop + click; `multiple` attribute allows batch; `onchange` calls `addFilesToBatch(this)` | TBD | ❌ |
| F-502 | Drop-zone icon | Div `.drop-zone__icon` 📸 | Drop zone | — | Decorative; always visible | TBD | ❌ |
| F-503 | Drop-zone title text | Div `.drop-zone__title` | Drop zone | — | "Click or drag & drop receipts" | TBD | ❌ |
| F-504 | Drop-zone hint text | Div `.drop-zone__hint` | Drop zone | — | "JPG · PNG · WEBP · PDF · up to 10 MB · multiple OK" | TBD | ❌ |
| F-505 | Drop-zone image preview | Img `#preview-img` + meta `#preview-meta` in `#preview-wrap` | Drop zone | — | Shows thumbnail and filename/size after file selected; hidden when no file | TBD | ❌ |
| F-506 | Batch controls panel | Div `#batch-controls` (hidden until files added) | — | — | Revealed when `addFilesToBatch` processes files; shows count + list | TBD | ❌ |
| F-507 | Batch select-all checkbox | Checkbox `#batch-select-all` | Batch controls | — | `onchange` calls `toggleSelectAllBatch(checked)`; checks/unchecks all batch items | TBD | ❌ |
| F-508 | Batch file count label | Span `#batch-controls__count` | Batch controls | — | "N files" reflecting current batch size | TBD | ❌ |
| F-509 | Batch "Clear all" button | Button `#batch-clear-btn` calling `clearBatch()` | Batch controls | — | Removes all files from batch; hides `#batch-controls`; disables upload button | TBD | ❌ |
| F-510 | Batch file list | Ul `#batch-list` | Batch controls | — | One `<li>` per file; each has checkbox, filename, remove button | TBD | ❌ |
| F-511 | Receipt Type section | Div `.upload-section` with label "Receipt Type" | — | — | Static section label | TBD | ❌ |
| F-512 | Receipt Type — Auto button (default active) | Button `upload-intent-btn` `data-intent="auto"` | Receipt Type section | — | `setUploadIntent('auto', this)`; default active state; sets hidden input `#upload-receipt-intent` | TBD | ❌ |
| F-513 | Receipt Type — Grocery button | Button `upload-intent-btn` `data-intent="grocery"` | Receipt Type section | — | `setUploadIntent('grocery', this)` | TBD | ❌ |
| F-514 | Receipt Type — Restaurant button | Button `upload-intent-btn` `data-intent="restaurant"` | Receipt Type section | — | `setUploadIntent('restaurant', this)` | TBD | ❌ |
| F-515 | Receipt Type — General Expense button | Button `upload-intent-btn` `data-intent="general_expense"` | Receipt Type section | — | `setUploadIntent('general_expense', this)` | TBD | ❌ |
| F-516 | Receipt Type hidden input | Input `#upload-receipt-intent` (hidden, value="auto") | Receipt Type section | — | Carries selected intent to `uploadReceipt()`; persists across intent toggles | TBD | ❌ |
| F-517 | Receipt Type desktop hint | Span `.upload-hint-desktop` | Receipt Type section | — | "Use Restaurant or General Expense when you already know…"; desktop-only visibility class | TBD | ❌ |
| F-518 | OCR Model section label | Label `for="upload-ai-model"` "OCR Model" | — | — | Static section label | TBD | ❌ |
| F-519 | OCR Model select dropdown | Select `#upload-ai-model` | OCR Model section | — | `onchange` calls `changeAiModelSelection(value)`; default option "Default OCR fallback"; populated from AI model list | TBD | ❌ |
| F-520 | OCR Model "Browse" button | Button calling `toggleAiModelBrowser()` | OCR Model section | — | Opens/closes `#upload-ai-model-browser` panel listing all available models | TBD | ❌ |
| F-521 | OCR Model metadata hint | Span `#upload-ai-model-meta` | OCR Model section | — | "Using server default OCR fallback." or selected model display name | TBD | ❌ |
| F-522 | OCR Model browser panel | Div `#upload-ai-model-browser` (display:none initially) | OCR Model section | — | Lists available models; each has "Use This Model" button calling `changeAiModelSelection(model.id)` | TBD | ❌ |
| F-523 | Upload / Auto Detect button | Button `#upload-btn` `btn-primary` calling `uploadReceipt()` | — | POST `/receipts/upload` | Disabled until file(s) selected; label "🚀 Auto Detect Receipt"; triggers batch upload loop | TBD | ❌ |
| F-524 | Stop batch button | Button `#upload-stop-btn` `btn-danger` calling `requestBatchStop()` | — | — | Hidden until upload starts; aborts in-flight fetch and halts batch loop | TBD | ❌ |
| F-525 | Upload status span | Span `#upload-status` (display:none) | — | — | `aria-hidden`; used for screen-reader or legacy status text during upload | TBD | ❌ |
| F-526 | Scan progress bar | Div `#upload-scan-progress` (hidden until upload starts) | — | — | `data-variant="indeterminate"`; shows animated bar during OCR; hidden when idle | TBD | ❌ |
| F-527 | Scan progress phase label | Span `#upload-scan-phase` | Scan progress | — | Text phases: "Ready" → "Uploading" → "Processing" → etc. | TBD | ❌ |
| F-528 | Scan progress meta label | Span `#upload-scan-meta` | Scan progress | — | Displays file name or batch progress "2 / 5" | TBD | ❌ |
| F-529 | Scan progress model chip | Span `#upload-scan-model` (hidden until model known) | Scan progress | — | Shows which OCR model processed the receipt | TBD | ❌ |
| F-530 | Scan progress retry button | Button `#upload-scan-retry` (display:none) | Scan progress | POST `/receipts/upload` | Shown on error; calls `uploadReceipt()` again | TBD | ❌ |
| F-531 | OCR Result card | Card `#ocr-result-card` (display:none initially) | — | — | Revealed after successful upload; contains `#ocr-result` with extracted line items | TBD | ❌ |
| F-532 | OCR Result card header | Span `.card-title` "✅ Extracted Items" | OCR Result card | — | Static title; shown only when result card is visible | TBD | ❌ |
| F-533 | OCR Result body | Div `#ocr-result` | OCR Result card | — | Rendered by upload-result enriched renderer; shows extracted items, store, totals for review | TBD | ❌ |
| F-534 | Batch upload — per-file duplicate detection | Server-side check during upload | — | POST `/receipts/upload` (server side) | Server computes file hash; returns duplicate info if file already uploaded | TBD | ❌ |
| F-535 | Batch upload — multiple-file sequential loop | JS upload loop in `uploadReceipt()` | — | POST `/receipts/upload` (N times) | Iterates selected files in batch; updates scan progress per file; stops on `requestBatchStop()` | TBD | ❌ |

#### Screen: Receipts List
Source: `src/frontend/index.html` lines 3793–4104
Backend: `handle_receipt_upload.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-600 | Page header — "Receipts" title + subtitle | H1 + paragraph | — | — | Subtitle "Review receipts sent from Telegram or uploaded directly" | TBD | ❌ |
| F-601 | Filters toggle button (header) | Button `#receipt-filters-toggle` 🧰 calling `toggleReceiptFilters()` | Page header | — | `aria-label="Toggle receipt filters"`; shows/hides the filter card | TBD | ❌ |
| F-602 | Filter card | Card `#receipt-filter-card` | — | — | `toggleReceiptFilters()` controls visibility; persists collapsed state | TBD | ❌ |
| F-603 | Filter card — "Review Refunds" button | Button calling `openRefundReceipts()` | Filter card header | GET `/receipts?...` | Applies refund filter and loads receipts | TBD | ❌ |
| F-604 | Filter card — Apply button | Button `btn-primary` calling `loadReceipts()` | Filter card header | GET `/receipts?<filter params>` | Submits current filter values | TBD | ❌ |
| F-605 | Filter card — Reset button | Button calling `resetReceiptFilters()` | Filter card header | — | Clears all filter inputs and reloads | TBD | ❌ |
| F-606 | Filter — Search text input | Input `#receipt-search-input` type=search | Filter body | — | `oninput` calls `onReceiptSearchInput()`; debounced client-side filter | TBD | ❌ |
| F-607 | Filter — Attribution chips | Div `#receipt-attribution-chips` | Filter body | — | `renderReceiptAttributionFilterChips()`; one chip per household member; clicking sets attribution filter | TBD | ❌ |
| F-608 | Filter — Untagged only chip | Button `#receipts-filter-untagged-btn` calling `toggleReceiptsUntaggedFilter()` | Filter body | — | `aria-pressed` toggled; shows only receipts without any attribution | TBD | ❌ |
| F-609 | Filter — Store select | Select `#receipt-filter-store` | Filter body | GET `/receipts` (options from `data.filters`) | Options populated from API response; "All stores" default | TBD | ❌ |
| F-610 | Filter — Source select | Select `#receipt-filter-source` | Filter body | — | Options: All sources, Upload, Telegram | TBD | ❌ |
| F-611 | Filter — Receipt Type select | Select `#receipt-filter-type` | Filter body | GET `/receipts` (options from `data.filters`) | Options populated from API response | TBD | ❌ |
| F-612 | Filter — Transaction select | Select `#receipt-filter-transaction` | Filter body | GET `/receipts` (options from `data.filters`) | Options populated from API response | TBD | ❌ |
| F-613 | Filter — Status select | Select `#receipt-filter-status` | Filter body | GET `/receipts` (options from `data.filters`) | Options populated from API response | TBD | ❌ |
| F-614 | Filter — Purchased from date | Input `#receipt-filter-purchase-from` type=date | Filter body | GET `/receipts?purchase_from=<date>` | Filter start date for purchase date range | TBD | ❌ |
| F-615 | Filter — Purchased to date | Input `#receipt-filter-purchase-to` type=date | Filter body | GET `/receipts?purchase_to=<date>` | Filter end date for purchase date range | TBD | ❌ |
| F-616 | Filter — Uploaded from date | Input `#receipt-filter-upload-from` type=date | Filter body | GET `/receipts?upload_from=<date>` | Filter start date for upload date range | TBD | ❌ |
| F-617 | Filter — Uploaded to date | Input `#receipt-filter-upload-to` type=date | Filter body | GET `/receipts?upload_to=<date>` | Filter end date for upload date range | TBD | ❌ |
| F-618 | Receipts By Store panel (desktop) | Div `.receipt-filter-store-block.desktop-only` inside filter body | Filter body | GET `/receipts` (summary in response) | Rendered by `renderReceiptSummaries(data.summary)` | TBD | ❌ |
| F-619 | Receipts By Store — sort select (desktop) | Select `#receipt-store-sort` calling `setReceiptStoreSort(value)` | Store panel desktop | — | Options: Most receipts (default), Store A-Z | TBD | ❌ |
| F-620 | Summary section card (collapsible) | Card `#receipts-summary-section` | — | GET `/receipts` | Collapsed by default; `toggleReceiptsSummary()` on header click; caret ▸/▾; state persisted | TBD | ❌ |
| F-621 | Summary section toggle (header click) | Div `.card-header` in `#receipts-summary-section` | Summary section | — | `toggleReceiptsSummary()`; updates `#receipts-summary-caret` | TBD | ❌ |
| F-622 | Summary — Total Receipts stat tile | Stat card `.accent` `#receipt-total-count` | Summary section | GET `/receipts` | "matching filters" sub-label | TBD | ❌ |
| F-623 | Summary — Refund Receipts stat tile | Stat card `.warning` `#receipt-refund-count` | Summary section | GET `/receipts` | Sub-text in `#receipt-refund-sub` | TBD | ❌ |
| F-624 | Summary — Refund Total stat tile | Stat card `#receipt-refund-total` | Summary section | GET `/receipts` | "returned amount" sub-label | TBD | ❌ |
| F-625 | Summary — Total Items stat tile | Stat card `#receipt-total-items` | Summary section | GET `/receipts` | "line-item quantity" sub-label | TBD | ❌ |
| F-626 | Summary — Unique Items stat tile | Stat card `#receipt-unique-items` | Summary section | GET `/receipts` | "distinct products" sub-label | TBD | ❌ |
| F-627 | Summary — Most Bought Items stat tile | Stat card `#receipt-most-bought-count` + `#receipt-most-bought-list` | Summary section | GET `/receipts` | Shows top product names | TBD | ❌ |
| F-628 | Summary — Refund review strip | Div `#receipt-refund-review-strip` (display:none when empty) | Summary section | GET `/receipts` | Rendered when refund receipts present | TBD | ❌ |
| F-629 | Summary — Purchases By Month chart | Div `#receipt-summary-months` | Summary section | GET `/receipts` | Bar/list chart by month; empty state "No purchase summary yet." | TBD | ❌ |
| F-630 | Summary — Receipts By Store (mobile) | Card `.mobile-only` `#receipt-summary-stores-mobile` | Summary section | GET `/receipts` | Mobile-only sibling of desktop store panel; sort via `#receipt-store-sort-mobile` | TBD | ❌ |
| F-631 | Summary — Receipts By Store mobile sort | Select `#receipt-store-sort-mobile` calling `setReceiptStoreSort(value)` | Summary section (mobile) | — | Same options as desktop: Most receipts, Store A-Z | TBD | ❌ |
| F-632 | Dedup card | Card `#receipts-dedup-card` | — | — | Always visible; collapsed-like (content is empty-state until scan runs) | TBD | ❌ |
| F-633 | Dedup — "Scan for duplicates" button | Button `#receipts-dedup-scan-btn` calling `runDedupScan()` | Dedup card | GET `/receipts/dedup-scan` | Scans last 180 days; renders pair list with Merge/Swap/Not-a-dup buttons | TBD | ❌ |
| F-634 | Dedup — pair Merge button | Button calling `mergeReceiptsPair(keepId, dropId)` | Dedup card | POST `/receipts/merge` | Drops one receipt; reparents linked Plaid transaction to keeper | TBD | ❌ |
| F-635 | Dedup — pair Swap button | Button calling `swapDedupPair(keepId, dropId)` | Dedup card | — | Swaps which row is Keep vs. Drop before merge | TBD | ❌ |
| F-636 | Dedup — pair "Not a dup" button | Button calling `dismissDedupPair(keepId, dropId)` | Dedup card | POST `/receipts/dedup-dismiss` | Flags pair as confirmed non-duplicate; suppresses from future scans | TBD | ❌ |
| F-637 | Manual merge by ID — Keep ID input | Input `#dedup-keep-id` type=number | Dedup card | — | Manual receipt ID entry | TBD | ❌ |
| F-638 | Manual merge by ID — Drop ID input | Input `#dedup-drop-id` type=number | Dedup card | — | Manual receipt ID entry | TBD | ❌ |
| F-639 | Manual merge by ID — Merge button | Button calling `manualMergeReceipts()` | Dedup card | POST `/receipts/merge` | Uses `#dedup-keep-id` and `#dedup-drop-id` values | TBD | ❌ |
| F-640 | Recent Receipts list card | Card in `.receipt-detail-grid` | — | GET `/receipts` | Left column of detail grid; main receipt table | TBD | ❌ |
| F-641 | Recent Receipts — sort select | Select `#receipt-sort` calling `setReceiptSort(value)` | Receipts list card | GET `/receipts` | Options: Newest purchase (default), Oldest, Highest total, Store A-Z, Status | TBD | ❌ |
| F-642 | Recent Receipts — refresh button | Button `btn-ghost btn-sm` calling `loadReceipts()` | Receipts list card | GET `/receipts` | Force-reload | TBD | ❌ |
| F-643 | Recent Receipts table body | Div `#receipts-table-body` | Receipts list card | GET `/receipts` | Rendered by `renderReceiptRows()`; one row per receipt; "failed" receipts filtered unless status filter set to "failed" | TBD | ❌ |
| F-644 | Receipts list — loading state | Empty-state "Could not load receipts." in `#receipts-table-body` | Receipts list card | — | Shown on non-OK response | TBD | ❌ |
| F-645 | Receipts list — empty state | Empty-state "No receipts match the selected attribution chips." or similar | Receipts list card | — | Shown when filtered result set is empty | TBD | ❌ |
| F-646 | Receipt Detail card — empty state | Div `#receipt-detail-body` with icon 🧾 and prompt text | Receipt detail card | — | "Select a receipt to inspect the extracted items and image." | TBD | ❌ |
| F-647 | Receipt row — click to view detail | Per-row click calling `viewReceipt(id)` | Receipts table | GET `/receipts/<id>` | Populates `#receipt-detail-body` with full receipt data | TBD | ❌ |
| F-648 | Receipt detail — image display | Img tag in detail body | Receipt detail card | GET `/receipts/<id>/image` | Shows uploaded receipt image; rotate buttons on either side | TBD | ❌ |
| F-649 | Receipt detail — rotate left | Button calling `rotateReceipt(id, 'left')` | Receipt detail card | PUT `/receipts/<id>/rotate` `{direction:"left"}` | Rotates stored image 90° left; reloads detail | TBD | ❌ |
| F-650 | Receipt detail — rotate right | Button calling `rotateReceipt(id, 'right')` | Receipt detail card | PUT `/receipts/<id>/rotate` `{direction:"right"}` | Rotates stored image 90° right; reloads detail | TBD | ❌ |
| F-651 | Receipt detail — editable fields (store, date, total, type) | Inline editor rendered in `#receipt-detail-body` | Receipt detail card | PUT `/receipts/<id>/update` | `collectReceiptEditorData(receiptId)` gathers all fields before submit | TBD | ❌ |
| F-652 | Receipt detail — attribution picker (whole receipt) | Button `#receipt-attribution-<id>` calling `toggleAttributionPicker()` | Receipt detail card | PUT `/receipts/<id>/attribution` | Multi-select household members; supports shared split | TBD | ❌ |
| F-653 | Receipt detail — per-item attribution picker | Button `.receipt-item-attribution` per line item | Receipt detail card | PUT `/receipts/<id>/items/<item_id>/attribution` | Item-level attribution overrides receipt-level | TBD | ❌ |
| F-654 | Receipt detail — bill status select | Select `#receipt-bill-status-select-<id>` calling `updateReceiptBillStatus()` | Receipt detail card | PUT `/receipts/<id>/bill-status` | Options: upcoming, paid, autopay; "paid" reveals paid-date input | TBD | ❌ |
| F-655 | Receipt detail — approve (review receipts) | Button calling `approveReceipt(id)` | Receipt detail card | POST `/receipts/<id>/approve` | For receipts in "review" status; parses JSON editor value and saves as purchase | TBD | ❌ |
| F-656 | Receipt detail — re-run OCR | Button `data-reprocess-btn` calling `reprocessReceipt(id)` | Receipt detail card | POST `/receipts/<id>/reprocess` | Re-submits image to OCR; optionally uses selected model via `model_id`; shows spinner | TBD | ❌ |
| F-657 | Receipt detail — delete | Button calling `deleteReceipt(id)` | Receipt detail card | DELETE `/receipts/<id>` | Confirm dialog; also removes purchase data and inventory added from it | TBD | ❌ |
| F-658 | Receipt item snapshot upload | Hidden input `#receipt-item-snapshot-input` calling `uploadReceiptItemSnapshotFromPicker(event)` | Receipt detail card | POST `/product-snapshots/upload` | Attaches a product photo to a specific receipt line item | TBD | ❌ |
| F-659 | Bulk attribution toolbar | Rendered by `_renderReceiptsBulkTagToolbar()` | Receipts list | POST `/receipts/bulk-attribution` | Shown when ≥1 receipt is selected; lets user attribute multiple receipts at once | TBD | ❌ |
| F-660 | Auto-link Plaid receipts (on load) | Background call `_autoLinkPlaidReceiptsOnce()` | — | POST `/receipts/auto-link-plaid` | Idempotent; runs once per session on `loadReceipts()`; merges pre-existing Plaid+Upload pairs | TBD | ❌ |

---

### GROUP: Restaurant
#### Screen: Restaurant Workspace
Source: `src/frontend/index.html` lines 2868–2986
Backend: `manage_shared_dining.py`, `handle_receipt_upload.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-700 | Page header — "Restaurant" title + subtitle | H1 + subtitle paragraph | — | — | Subtitle "Track dining-out receipts, repeat orders, and restaurant spend without touching grocery inventory." | `RestaurantsView.pageHeader` — appTitle2 + subheadline copy verbatim. Added 2026-05-20. | ✅ |
| F-701 | Visits stat card | Stat card `.accent` `#restaurant-visit-count` | Stats grid | GET `/analytics/restaurant-summary` | Sub-text from `#restaurant-visit-sub`; shows refund count if any | `StatsGrid.statCard` "Visits" tinted accent; value from `summary.visitCount`; sub conditional on `refundCount > 0`. Added 2026-05-20. | ✅ |
| F-702 | Dining Spend stat card | Stat card `.success` `#restaurant-total-spend` | Stats grid | GET `/analytics/restaurant-summary` + GET `/analytics/spending?period=monthly&domain=restaurant` | `formatMoney(summaryData.total_spend \|\| spendData.grand_total)`; "current window" sub-label | "Dining Spend" success-tinted; `summary.totalSpend ?? spending.grandTotal`. Sub "current window". Added 2026-05-20. | ✅ |
| F-703 | Average Ticket stat card | Stat card `#restaurant-average-ticket` | Stats grid | GET `/analytics/restaurant-summary` | Per-visit average; `formatMoney(summaryData.average_ticket)` | "Average Ticket" `summary.averageTicket` formatted, sub "per visit". Added 2026-05-20. | ✅ |
| F-704 | Top Restaurant stat card | Stat card `#restaurant-top-store` + `#restaurant-top-store-sub` | Stats grid | GET `/analytics/restaurant-summary` | First entry of `summaryData.top_restaurants`; sub shows visits, refunds, net total | "Top Restaurant" — `summary.topRestaurants.first.store` + composed sub "N visit(s) · N refund(s) · Net $X". Added 2026-05-20. | ✅ |
| F-705 | Stats grid — empty state for stat cards | Default values: 0 / $0.00 / — | Stats grid | — | Shown before `loadRestaurant()` resolves | All cards default to 0/$0.00/— via `Self.money(nil)` + "No visits yet" until refresh resolves. Added 2026-05-20. | ✅ |
| F-706 | Dining Budget card | Card `.dining-budget-card` | — | GET `/budget/status?month=<YM>&domain=restaurant` | Loads budget for selected month on page enter | `DiningBudgetCard` triggered by `state.loadBudget()` inside `refresh()`. Added 2026-05-20. | ✅ |
| F-707 | Dining Budget — month picker | Input `#restaurant-budget-month` type=month | Dining Budget card | GET `/budget/status?month=<YM>&domain=restaurant` | `onchange` calls `loadRestaurantBudget()`; auto-initialised to current month if empty | TextField "YYYY-MM" → `state.setBudgetMonth(_:)` (regex-validated) → reload. Initialized to `currentYearMonth()` via UserDefaults `LocalOCR.restaurant.budgetMonth`. Web's `<input type=month>` adapted to TextField; macOS DatePicker swap deferred. Added 2026-05-20. | 🔄 |
| F-708 | Dining Budget — Monthly Target input | Input `#restaurant-budget-amount` type=number step=0.01 | Dining Budget card | — | Placeholder "e.g. 300"; editable; value pre-filled from `b.budget_amount` | TextField "$state.budgetAmountInput" placeholder "e.g. 300"; pre-filled from `budget.budgetAmount` (formatted `%.2f`). Added 2026-05-20. | ✅ |
| F-709 | Dining Budget — Save button | Button `#restaurant-budget-save` calling `saveRestaurantBudget()` | Dining Budget card | POST `/budget/set-monthly` `{month, domain:"restaurant", budget_amount}` | Admin-only; toast on success/error; reloads budget summary | PrimaryButton "Save" disabled when `!isAdmin` + toast "Only admins can update budgets" on demote. `state.saveBudget()` → POST + reload. Added 2026-05-20. | ✅ |
| F-710 | Dining Budget — progress summary | Div `#restaurant-budget-status` | Dining Budget card | GET `/budget/status?month=<YM>&domain=restaurant` | Renders spend amount, progress bar, % used, visits, remaining; bar class: "ok" / "warn" / "danger" (70% / 90% thresholds) | `statusBody` — "$spent of $cap" header, progress bar with success/warning/error tint at 70/90% thresholds, "N% used · N visit(s) · N refund(s)" + "$X left/over" trailing colored green/red. Added 2026-05-20. | ✅ |
| F-711 | Dining Budget — progress bar | Div `.progress-bar` with dynamic class and width% | Budget status | — | Width capped at 100%; class ok/warn/danger | `ProgressBar` GeometryReader Rectangle; width = `geo.size.width * (pct/100)` clamped [0, geo.width]; tint via `barColor(for:)` (success<70, warning<90, error≥90). Added 2026-05-20. | ✅ |
| F-712 | Dining Budget — no budget set state | Empty-state "No restaurant budget set for this month yet." in `#restaurant-budget-status` | Dining Budget card | — | Shown when `/budget/status` returns non-OK | When `budget == nil` or `budgetAmount == 0` → EmptyStateView creditcard icon + verbatim copy. Added 2026-05-20. | ✅ |
| F-713 | Receipt Review card | Card `.restaurant-review-card` | — | GET `/analytics/restaurant-summary?months=<N>` | Main receipts list for the selected period | `ReceiptReviewCard` rendered from `summary.recentReceipts.prefix(8)`. Added 2026-05-20. | ✅ |
| F-714 | Receipt Review — period select | Select `#restaurant-period` calling `loadRestaurant()` | Receipt Review card | GET `/analytics/restaurant-summary?months=<N>` | Options: 3, 6 (default), 12 months | Segmented Picker (3 mo / 6 mo / 12 mo) in toolbar bound to `state.months` (UserDefaults persisted) → `setMonths(_:)` → reload. Added 2026-05-20. | ✅ |
| F-715 | Receipt Review — refresh button | Button `.restaurant-refresh-btn` calling `loadRestaurant()` | Receipt Review card | GET `/analytics/restaurant-summary?months=<N>` | `aria-label="Refresh"` | Toolbar `arrow.clockwise` Button → `state.refresh()` (full parallel reload via withTaskGroup). Added 2026-05-20. | ✅ |
| F-716 | Receipt Review — body (receipt rows) | Div `#restaurant-body` | Receipt Review card | GET `/analytics/restaurant-summary` | `renderCompactReceiptRows(receipts.slice(0, 6 or 8))`; includes delete=false | `ReceiptRow` VStack — store + date + transaction type + total. Refund rows tinted warning. Tap → Router.activeTab = .receipts + loadDetail. Added 2026-05-20. | ✅ |
| F-717 | Receipt Review — empty state | Empty-state "No restaurant receipts yet. Upload one and it will land here." | Receipt Review card | — | Shown when `summaryData.recent_receipts` is empty | EmptyStateView fork.knife icon + verbatim copy. Added 2026-05-20. | ✅ |
| F-718 | Receipt Review — error state | Empty-state "Could not load restaurant receipts." | Receipt Review card | — | Shown when `/analytics/restaurant-summary` returns non-OK | When `lastError != nil && summary == nil` → EmptyStateView exclamationmark.triangle + error subtitle. Added 2026-05-20. | ✅ |
| F-719 | Receipt Review — "Open All" button | Button `btn-ghost btn-sm` calling `openRestaurantReceipts()` | Receipt Review card | — | Navigates to Receipts page with restaurant filter applied | "Open All" GhostButton → `state.openReceiptsTab()` → Router.activeTab = .receipts + ReceiptsState.loadList. Receipts page filtering by restaurant domain not yet exposed in mac ReceiptsView; covers navigation only. Added 2026-05-20. | 🔄 |
| F-720 | Top Restaurants card | Card `#restaurant-top-restaurants` | Analytics grid | GET `/analytics/restaurant-summary` | Left column of bottom grid; ranked list of restaurants | `TopRestaurantsCard` left column (max width 320pt) — Card + VStack of TopRestaurantRow. Added 2026-05-20. | ✅ |
| F-721 | Top Restaurants — list rows | Ul `.summary-row-list` in `#restaurant-top-restaurants` | Top Restaurants card | — | Each row: store name, visits, refunds, avg ticket, total; `onclick="openStoreReceipts(store)"` | TopRestaurantRow Button — store name + meta ("N visit(s) · N refund(s) · Avg $X") + total + chevron.right. Added 2026-05-20. | ✅ |
| F-722 | Top Restaurants — row tap (deep-link to receipts) | Button `.summary-row.is-tappable` per restaurant | Top Restaurants card | — | `openStoreReceipts(item.store)` navigates to Receipts filtered by that store | Tap → Router.activeTab = .receipts + ReceiptsState.loadList + toast "Showing receipts — filter by <store> in Receipts page". Per-store filter not exposed in mac ReceiptsView yet; nav covers deep-link intent. Added 2026-05-20. | 🔄 |
| F-723 | Top Restaurants — empty state | Empty-state "No restaurant history yet." in `#restaurant-top-restaurants` | Top Restaurants card | — | Shown when `summaryData.top_restaurants` is empty | `AnalyticsGrid.emptyTopRestaurants` EmptyStateView storefront + verbatim copy. Added 2026-05-20. | ✅ |
| F-724 | Top Ordered Items card | Card `#restaurant-top-items` | Analytics grid | GET `/analytics/restaurant-summary` | Right column of bottom grid; most frequently ordered menu items | `TopItemsCard` right column expands to fill — Card + header row + LazyVStack of rows. Added 2026-05-20. | ✅ |
| F-725 | Top Ordered Items — table (desktop) | `<table>` with Item / Qty / Total / Avg columns | Top Ordered Items card | — | Rendered in non-compact mode; `renderCompactTopItemCards` for compact/mobile | HStack header (Item / Qty / Total / Avg) + Divider + HStack rows aligned to columns (60/90/80pt). Monospaced digits for Qty. Added 2026-05-20. | ✅ |
| F-726 | Top Ordered Items — compact cards (mobile) | `renderCompactTopItemCards(topItems, "Avg")` | Top Ordered Items card | — | Compact card layout for mobile viewport | Mac always uses table layout — no mobile breakpoint. Added 2026-05-20. | 🔄 |
| F-727 | Top Ordered Items — empty state | Empty-state "No restaurant line items yet." in `#restaurant-top-items` | Top Ordered Items card | — | Shown when `summaryData.top_items` is empty | `AnalyticsGrid.emptyTopItems` EmptyStateView fork.knife + verbatim copy. Added 2026-05-20. | ✅ |
| F-728 | Analytics grid layout | Two-column CSS grid (minmax(260px,1fr) / minmax(0,2fr)) | — | — | Contains Top Restaurants + Top Ordered Items cards | `AnalyticsGrid` HStack top-aligned — `TopRestaurantsCard` (maxWidth 320) + `TopItemsCard` filling remaining width. Mirrors web 1fr/2fr ratio. Added 2026-05-20. | ✅ |
| F-729 | Spending by period data (background) | JS array `periodRows` from `spendData.spending_by_period` | — | GET `/analytics/spending?period=monthly&domain=restaurant&months=<N>` | Fetched in parallel with summary; supports future chart rendering | `state.loadSpending()` runs in parallel via `withTaskGroup`. Only `grandTotal` decoded for now (used as F-702 fallback). `spending_by_period` map decoded as opaque; chart UI deferred to v1.1. Added 2026-05-20. | 🔄 |

---

### GROUP: Shared Dining
#### Screen: Balances
Source: `src/frontend/index.html` lines 2987–3004
Backend: `shared_dining_endpoints.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-800 | Page header: "Balances" title | `<h1>Balances</h1>` in `#page-balances` | — | — | Static heading | `BalancesView.header` Text "Balances" at appTitle2. Added 2026-05-20. | ✅ |
| F-801 | Page subtitle | `<p>Outstanding debts across all shared receipts</p>` | — | — | Static subtitle | Verbatim subheadline text under title. Added 2026-05-20. | ✅ |
| F-802 | Refresh button | Ghost btn with 🔄, `onclick="loadBalances()"` in page header | — | — | Reloads balances table | Toolbar `arrow.clockwise` Button → `state.loadBalances()`. Added 2026-05-20. | ✅ |
| F-803 | "Who Owes What" card container | `.card` with `.card-title` "Who Owes What" | — | — | Wraps the balances table | `BalancesCard` Card wrapper with "Who Owes What" header + count of contacts on the right. Added 2026-05-20. | ✅ |
| F-804 | Balances loading state | `#balances-body` initialises with `<p>Loading…</p>` | — | GET `/shared-dining/balances` | Shown while fetch in-flight | `isLoading && balances.isEmpty` branch → EmptyStateView hourglass "Loading…". Added 2026-05-20. | ✅ |
| F-805 | Balances empty state | `#balances-body` renders "No outstanding balances — all settled! 🎉" | — | GET `/shared-dining/balances` | Shown when API returns `[]` | EmptyStateView checkmark.seal + verbatim copy. Added 2026-05-20. | ✅ |
| F-806 | Balances error state | `#balances-body` renders "Could not load balances." | — | GET `/shared-dining/balances` | Shown when `res.ok` is false | EmptyStateView exclamationmark.triangle + subtitle from `lastError`. Added 2026-05-20. | ✅ |
| F-807 | Balances table | `<table class="balances-table">` inside `#balances-body` | — | GET `/shared-dining/balances` | Rendered when rows exist | VStack of `BalanceHeaderRow` + `BalanceRowView` separated by Divider. Decode `[BalanceRow]` direct array. Added 2026-05-20. | ✅ |
| F-808 | Table column: Contact | `<th>Contact</th>` + `<td>` with `r.name` | Balances table | — | Contact display name | Text(row.name) frame-flex left-align with line-limit 1. Added 2026-05-20. | ✅ |
| F-809 | Table column: Direction | `<th>Direction</th>` + `<td>` "Owes you" / "You owe" | Balances table | — | Derived from `r.net_amount > 0` | `row.owesYou ? "Owes you" : "You owe"` tinted success/error. Added 2026-05-20. | ✅ |
| F-810 | Table column: Amount | `<th>Amount</th>` + coloured `<td>` with `formatMoney(Math.abs(r.net_amount))` | Balances table | — | Green = owed to you, red = you owe | `String(format: "$%.2f", abs(row.netAmount))` monospacedDigit, tinted success/error. Added 2026-05-20. | ✅ |
| F-811 | Amount colour badge (owed) | `.balances-amount--owed` class (green) | Balances table | — | Applied when `net_amount > 0` | `DesignTokens.success` foreground when `netAmount > 0` (mirrors `.balances-amount--owed`). Added 2026-05-20. | ✅ |
| F-812 | Amount colour badge (owe) | `.balances-amount--owe` class (red) | Balances table | — | Applied when `net_amount < 0` | `DesignTokens.error` foreground when `netAmount < 0`. Added 2026-05-20. | ✅ |
| F-813 | "Settle all" per-contact action button | `<button class="balances-settle-btn">Settle all</button>` per row | Balances table | — | Calls `settleAllWithContact(contactId, name)` | Per-row DestructiveButton "Settle all" → `state.pendingSettle = row` to open confirmation. Added 2026-05-20. | ✅ |
| F-814 | Settle-all confirmation dialog | `confirm('Mark all debts with … as settled?')` native dialog | — | — | Destructive action guard | `.confirmationDialog("Mark all debts with <name> as settled?")` bound to `state.pendingSettle` with destructive Settle all + Cancel. Added 2026-05-20. | ✅ |
| F-815 | Settle-all API call | POST `/shared-dining/contacts/:id/settle-all` | — | POST `/shared-dining/contacts/{contactId}/settle-all` | Marks all debts for contact as settled | `state.settleAll(_:)` → POST `/shared-dining/contacts/<id>/settle-all` + DemoModeGate guard + decode `SettleAllResponse`. Added 2026-05-20. | ✅ |
| F-816 | Settle-all success toast | `toast('Settled N debt(s) with …', 'success')` | — | — | Count pluralised; table auto-reloads | ToastQueue.push "Settled N debt(s) with <name>" pluralised; auto-reload via `loadBalances()`. Added 2026-05-20. | ✅ |
| F-817 | Settle-all error toast | `toast('Could not settle debts', 'error')` | — | — | Shown when POST is not OK | catch block toasts `errorDescription ?? "Could not settle debts"`. Demo mode → "Demo mode — changes not saved." Added 2026-05-20. | ✅ |

#### Screen: Contacts
Source: `src/frontend/index.html` lines 3005–3044
Backend: `manage_shared_dining.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-900 | Page header: "Dining Contacts" title | `<h1>Dining Contacts</h1>` in `#page-contacts` | — | — | Static heading | `ContactsView.header` Text "Dining Contacts" appTitle2. Added 2026-05-20. | ✅ |
| F-901 | Page subtitle | `<p>Saved contacts for splitting restaurant receipts</p>` | — | — | Static subtitle | Verbatim subheadline under title. Added 2026-05-20. | ✅ |
| F-902 | Refresh button | Ghost btn 🔄 `onclick="loadContacts()"` in page header | — | — | Reloads contacts list | Toolbar `arrow.clockwise` Button → `state.loadContacts()`. Added 2026-05-20. | ✅ |
| F-903 | Add Contact card | `.card` "Add Contact" with form fields | — | — | Always visible above the list | `AddContactCard` Card always rendered above SavedContactsCard. Added 2026-05-20. | ✅ |
| F-904 | Add Contact: Name field (required) | `#contact-name-input` text input, label "Name *" | Add Contact card | — | Required; `saveContact()` toasts error if blank | TextField "$draftName" with "Name *" caption label; Submit disabled until non-empty; `createContact` re-checks + toast. Added 2026-05-20. | ✅ |
| F-905 | Add Contact: Phone field (optional) | `#contact-phone-input` tel input, placeholder "+1 555 000 0000" | Add Contact card | — | Optional; sent as `null` if blank | TextField placeholder "+1 555 000 0000" → sent as `nil` when blank via `nilIfEmptyContact`. Added 2026-05-20. | ✅ |
| F-906 | Add Contact: Email field (optional) | `#contact-email-input` email input, placeholder "name@example.com" | Add Contact card | — | Optional; sent as `null` if blank | TextField placeholder "name@example.com" → `nil` when blank. Added 2026-05-20. | ✅ |
| F-907 | "Add Contact" submit button | `<button class="btn btn-primary">Add Contact</button>` `onclick="saveContact()"` | Add Contact card | POST `/shared-dining/contacts` | Full-width; submits name/phone/email JSON | PrimaryButton + ⌘↩ shortcut → `state.createContact(name:phone:email:)` → POST `/shared-dining/contacts` with `CreateContactBody`. Added 2026-05-20. | ✅ |
| F-908 | Add contact validation — missing name toast | `toast('Name is required', 'error')` | — | — | Guards `saveContact()` before API call | `state.createContact` guards trimmed name; toasts "Name is required" verbatim before any network call. Added 2026-05-20. | ✅ |
| F-909 | Add contact error toast | `toast(err.error \|\| 'Could not save contact', 'error')` | — | — | Shown when POST not OK | catch toasts `errorDescription ?? "Could not save contact"`. Demo mode → "Demo mode — changes not saved." Added 2026-05-20. | ✅ |
| F-910 | Add contact success toast | `toast(name + ' added', 'success')` | — | — | Fields cleared; `loadContacts()` called | "\(name) added" success toast + automatic `loadContacts()` reload. Added 2026-05-20. | ✅ |
| F-911 | Field clear after successful add | All three input fields cleared via `.value = ''` | — | — | Follows successful POST | View binds three `@State` strings; on `createContact` returning true → all three set to "". Added 2026-05-20. | ✅ |
| F-912 | Saved Contacts card | `.card` "Saved Contacts" wrapping `#contacts-body` | — | — | Always visible below the form card | `SavedContactsCard` Card always rendered. Title + count chip in header. Added 2026-05-20. | ✅ |
| F-913 | Contacts loading state | `#contacts-body` shows `<p>Loading…</p>` | — | GET `/shared-dining/contacts` | Shown while fetch in-flight | First-paint shows empty state until response (no separate loading branch — refresh swap is instant). Added 2026-05-20. | 🔄 |
| F-914 | Contacts empty state | "No saved contacts yet. Add one above." | — | GET `/shared-dining/contacts` | Shown when API returns `[]` | EmptyStateView person.crop.circle.badge.plus + verbatim "No saved contacts yet." + "Add one above." subtitle. Added 2026-05-20. | ✅ |
| F-915 | Contacts error state | "Could not load contacts." | — | GET `/shared-dining/contacts` | Shown when `res.ok` is false | `loadContacts` swallows errors as `logger.warning`; contacts array stays empty so the F-914 empty state shows instead. Web's "Could not load" copy not surfaced separately. Added 2026-05-20. | 🔄 |
| F-916 | Contacts list container | `.contacts-list` div rendered per contact | Saved Contacts card | GET `/shared-dining/contacts` | Populated from `_diningContacts` | LazyVGrid(adaptive minimum 220) of `ContactCard`. Added 2026-05-20. | ✅ |
| F-917 | Contact card | `.contacts-card` article per contact | Contacts list | — | One card per entry in `_diningContacts` | `ContactCard` HStack with avatar + name + meta. Added 2026-05-20. | ✅ |
| F-918 | Contact avatar initial | `.contacts-card__avatar` showing first letter of name (uppercase) | Contact card | — | Derived from `c.name.charAt(0).toUpperCase()` | Text `String(contact.name.prefix(1)).uppercased()` in 36×36 Circle filled with `DesignTokens.accent`. Added 2026-05-20. | ✅ |
| F-919 | Contact name label | `.contacts-card__name` showing full name | Contact card | — | Escaped HTML of `c.name` | Text(contact.name) at .appCallout.semibold lineLimit 1. Added 2026-05-20. | ✅ |
| F-920 | Contact meta (phone + email) | `.contacts-card__meta` concatenating phone + email with ` · ` | Contact card | — | Only shown when at least one exists | `[contact.phone, contact.email].compactMap.filter(non-empty).joined(" · ")` rendered only when non-empty. Added 2026-05-20. | ✅ |
| F-921 | `_diningContacts` in-memory cache | `var _diningContacts = []` module-level array | — | — | Reused by `_spRender` for split-panel contact dropdown | `SharedDiningState.contacts` published array — survives tab switches; receipt-split-panel dropdown will reuse the same source when v1.1 receipt-split feature lands. Added 2026-05-20. | ✅ |

---

### GROUP: Expenses
#### Screen: Expenses
Source: `src/frontend/index.html` lines 3045–3196
Backend: `handle_receipt_upload.py` (expense receipt_intent)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1000 | Page header: "Expenses" title | `<h1>Expenses</h1>` in `#page-expenses` | — | — | Static heading | `ExpensesView.header` appTitle2. Added 2026-05-20. | ✅ |
| F-1001 | Page subtitle | Static `<p>` describing one-off spending | — | — | "services, gifts, fees, and retail receipts" | Verbatim subheadline copy. Added 2026-05-20. | ✅ |
| F-1002 | Stat card: Expense Receipts count | `#expense-receipt-count` inside `accent` stat-card | — | GET `/analytics/expense-summary` | Populated from `summaryData.purchase_count` | `StatsGridExpense` accent-tinted stat card bound to `summary.purchaseCount`. Added 2026-05-20. | ✅ |
| F-1003 | Stat card sub: refund count in window | `#expense-receipt-sub` — "N refund(s) in window" or "purchase receipts" | — | — | Shows refund count when > 0 | `receiptSub` conditional on `refundCount > 0` — matches web wording. Added 2026-05-20. | ✅ |
| F-1004 | Stat card: Total Spend | `#expense-total-spend` inside `success` stat-card | — | GET `/analytics/expense-summary` | `summaryData.total_spend` or `spendData.grand_total` | Success-tinted card `summary.totalSpend ?? spending.grandTotal`. Added 2026-05-20. | ✅ |
| F-1005 | Stat card: Average Ticket | `#expense-average-ticket` | — | GET `/analytics/expense-summary` | `summaryData.average_ticket` | "Average Ticket" `summary.averageTicket`. Added 2026-05-20. | ✅ |
| F-1006 | Stat card: Top Merchant (name) | `#expense-top-store` | — | GET `/analytics/expense-summary` | First entry of `summaryData.top_merchants[]` | "Top Merchant" `topMerchants.first.store` (warning tint). Added 2026-05-20. | ✅ |
| F-1007 | Stat card: Top Merchant sub (receipt count + refunds + net) | `#expense-top-store-sub` | — | — | Formatted string; "No expenses yet" when absent | `topMerchantSub` composed "N receipt(s) · N refund(s) · Net $X" / "No expenses yet". Added 2026-05-20. | ✅ |
| F-1008 | Expense Budget card | `.dining-budget-card` with card-title "Expense Budget" | — | — | Left column of upper 2-col grid | `ExpenseBudgetCard` Card. Mac stacks vertically (no two-col split with detail). Added 2026-05-20. | ✅ |
| F-1009 | Budget month picker | `#expense-budget-month` `<input type="month">` `onchange="loadExpenseBudget()"` | Expense Budget card | GET `/budget/status?month=…&domain=general_expense` | Auto-initialised to current month if blank | TextField "$state.budgetMonth" — regex validated; persisted via UserDefaults `LocalOCR.expense.budgetMonth`. DatePicker swap deferred v1.1. Added 2026-05-20. | 🔄 |
| F-1010 | Budget monthly target input | `#expense-budget-amount` number input | Expense Budget card | — | Admin only to save; populated from `b.budget_amount` | TextField "$state.budgetAmountInput" placeholder "e.g. 200"; pre-filled from response amount. Added 2026-05-20. | ✅ |
| F-1011 | Budget Save button | `#expense-budget-save` `btn-primary` `onclick="saveExpenseBudget()"` | Expense Budget card | POST `/budget/set-monthly` | Only admins can save; toasts error otherwise | PrimaryButton "Save" disabled when `!isAdmin`; `state.saveBudget()` → POST `domain=general_expense`. Added 2026-05-20. | ✅ |
| F-1012 | Budget admin guard | `toast('Only admins can update budgets', 'error')` | — | — | Checked before POST | Verbatim toast on non-admin save attempt. Added 2026-05-20. | ✅ |
| F-1013 | Budget status display | `#expense-budget-status` progress bar + spend/cap/remaining summary | Expense Budget card | GET `/budget/status?month=…&domain=general_expense` | Colour: `ok` / `warn` (≥70%) / `danger` (≥90%) | `statusBody` — "$spent of $cap" + progress bar + "N% used · receipts · refunds · $X left/over". Added 2026-05-20. | ✅ |
| F-1014 | Budget progress bar | `.progress-bar` with `ok`/`warn`/`danger` class, width = `min(pct, 100)%` | Expense Budget card | — | Visual spending indicator | `ProgressBar` clamped 0-100%; tint via barColor 70/90% thresholds (success/warning/error). Added 2026-05-20. | ✅ |
| F-1015 | Budget "no budget set" empty state | "No general expense budget set for this month yet." | Expense Budget card | — | Shown when budget status endpoint returns non-OK | EmptyStateView creditcard + verbatim copy when `budget == nil || budgetAmount == 0`. Added 2026-05-20. | ✅ |
| F-1016 | Selected Receipt detail card | Card "Selected Receipt" with `#expense-detail-body` | — | — | Right column of upper 2-col grid | Detail panel deferred — mac instead deep-links to the Receipts tab (Router.activeTab + loadDetail) when a row is tapped. Inline detail card v1.1. Added 2026-05-20. | 🔄 |
| F-1017 | Detail card empty state | "Select an expense receipt to inspect totals and reference line items." | Selected Receipt card | — | Shown before any receipt is selected | Same as F-1016 — deferred. Added 2026-05-20. | 🔄 |
| F-1018 | Receipt detail: store name | `.expense-detail__store` | Selected Receipt card | GET `/receipts/:id` | Derived from `data.store \|\| receipt.store` | Shown in Receipts tab after deep-link. Added 2026-05-20. | 🔄 |
| F-1019 | Receipt detail: date + time + type meta | `.expense-detail__meta` | Selected Receipt card | — | Format: "Date · Time · TransactionType" | Same — covered via Receipts tab. Added 2026-05-20. | 🔄 |
| F-1020 | Receipt detail: "Open" button | `<button onclick="jumpToReceipt(id)">Open</button>` | Selected Receipt card | — | Links through to Receipts list | Mac tile is itself the "Open" affordance — tap row → Router.activeTab=.receipts + activeDetailDestination=.receipt(id). Added 2026-05-20. | 🔄 |
| F-1021 | Receipt detail: total amount | `.expense-detail__total-amount` with optional `--refund` modifier | Selected Receipt card | — | Formatted; refund class when `transaction_type === 'refund'` | Refund rows in the list itself are tinted warning; full detail covered via Receipts tab. Added 2026-05-20. | 🔄 |
| F-1022 | Receipt detail: subtotal / tax / tip breakdown | `.expense-detail__total-meta` spans | Selected Receipt card | — | Only shown when value > 0 | Covered via Receipts tab detail view (v1.1 will inline the panel here). Added 2026-05-20. | 🔄 |
| F-1023 | Receipt detail: line items list | `<ul class="summary-row-list">` with qty, unit price, line total | Selected Receipt card | — | From `data.items[]`; empty state when none | Covered via Receipts tab. Added 2026-05-20. | 🔄 |
| F-1024 | Receipt detail: line items empty state | "No reference line items saved yet." | Selected Receipt card | — | Shown when `items.length === 0` | Covered via Receipts tab. Added 2026-05-20. | 🔄 |
| F-1025 | Expense Receipts table/list card | `.restaurant-review-card` "Expense Receipts" with `#expenses-body` | — | — | Main receipt browser | `RecentExpensesCard` Card with "Recent General Expenses" header. Added 2026-05-20. | ✅ |
| F-1026 | Period filter select | `#expense-period` select (3 / 6 / 12 months), default 6 | Expense Receipts card | GET `/analytics/expense-summary?months=N` | `onchange="loadExpenses()"` | Toolbar segmented Picker (3/6/12 mo) → `state.setMonths(_:)` → refresh. UserDefaults persisted. Added 2026-05-20. | ✅ |
| F-1027 | Refresh button in receipts toolbar | Ghost btn 🔄 `onclick="loadExpenses()"` | Expense Receipts card | — | Reloads with current period | Toolbar `arrow.clockwise` Button → `state.refresh()`. Added 2026-05-20. | ✅ |
| F-1028 | Expenses empty state | "No general expense receipts yet. Upload one with the `General Expense` intent…" | `#expenses-body` | — | Shown when `receipts.length === 0` | EmptyStateView doc.text + verbatim copy when `recentReceipts.isEmpty`. Added 2026-05-20. | ✅ |
| F-1029 | Monthly Expense Spend sub-card | `.card` "Monthly Expense Spend" inside `#expenses-body` | — | GET `/analytics/spending?period=monthly&domain=general_expense&months=N` | Shows desktop table or mobile month-chip grid | `state.loadSpending()` runs in parallel; `spending_by_period` map is decoded opaquely (only `grand_total` consumed). Monthly table UI deferred v1.1. Added 2026-05-20. | 🔄 |
| F-1030 | Monthly spend: desktop table | `<table>` with Month / Net / Purchases / Refunds / Receipts columns | Monthly Expense Spend card | — | Rendered when `!isCompactMobileReceiptView()` | Same v1.1 deferral as F-1029. Added 2026-05-20. | 🔄 |
| F-1031 | Monthly spend: mobile month-chip grid | `.month-chip-grid` with individual `.month-chip` per period | Monthly Expense Spend card | — | Rendered when `isCompactMobileReceiptView()` | Mac always uses desktop layout. Added 2026-05-20. | 🔄 |
| F-1032 | Month chip: quick-peek on long-press | `data-quick-action` JSON attribute → `showQuickPeek()` overlay | Month chip | — | Payload: title, amount, stats, topMerchants, trend delta | Covered by F-1031 deferral. Added 2026-05-20. | 🔄 |
| F-1033 | Monthly spend empty state | "No expense spending data yet." | Monthly Expense Spend card | — | Shown when `periodRows.length === 0` | Covered by F-1029 deferral. Added 2026-05-20. | 🔄 |
| F-1034 | Recent General Expenses sub-card | `.card` "Recent General Expenses" — up to 12 recent receipts | `#expenses-body` | — | Calls `renderCompactReceiptRows(receipts.slice(0,12))` | `RecentExpensesCard` shows `recentReceipts.prefix(12)`. Added 2026-05-20. | ✅ |
| F-1035 | Click receipt row → detail panel | `onClick: (id) => viewExpenseReceiptDetail(id)` | Recent General Expenses | GET `/receipts/:id` | Updates `#expense-detail-body`; scrolls on mobile | Tap → Router.activeTab=.receipts + activeDetailDestination=.receipt(id) + ReceiptsState.loadDetail. Inline detail panel deferred. Added 2026-05-20. | 🔄 |
| F-1036 | Error state when summary fetch fails | "Could not load general expenses." in `#expenses-body` | — | — | Shown when `!summaryRes.ok` | `lastError && summary == nil` → EmptyStateView exclamationmark.triangle + verbatim copy. Added 2026-05-20. | ✅ |
| F-1037 | Top Merchants card | `.card` "Top Merchants" with `#expense-top-merchants` | — | GET `/analytics/expense-summary` | Lower-left of bottom analytics grid | `TopMerchantsCard` (max width 320) — Card + VStack of TopMerchantRow. Added 2026-05-20. | ✅ |
| F-1038 | Top Merchants list | `<ul class="summary-row-list">` with tappable `.summary-row` per merchant | Top Merchants card | — | `onclick="openStoreReceipts(store)"` | TopMerchantRow Button → Router.activeTab=.receipts + ReceiptsState.loadList + toast hint. Per-merchant filter not surfaced on mac ReceiptsView yet. Added 2026-05-20. | 🔄 |
| F-1039 | Top Merchants empty state | "No merchant history yet." | Top Merchants card | — | Shown when `top_merchants.length === 0` | `emptyMerchants` EmptyStateView storefront + verbatim copy. Added 2026-05-20. | ✅ |
| F-1040 | Merchant row: visits, refunds, avg-ticket meta | `.summary-row__meta` built from `item.visits`, `item.refunds`, `item.average_ticket` | Top Merchants list | — | Refund and avg-ticket text conditional | TopMerchantRow `meta` computed "N receipt(s) · N refund(s) · Avg $X". Added 2026-05-20. | ✅ |
| F-1041 | Top Reference Items card | `.card` "Top Reference Items" with `#expense-top-items` | — | GET `/analytics/expense-summary` | Lower-right of bottom analytics grid | `TopExpenseItemsCard` right column. Added 2026-05-20. | ✅ |
| F-1042 | Top Reference Items: desktop table | `<table>` Item / Qty / Total / Avg columns | Top Reference Items card | — | Rendered when `!isCompactMobileReceiptView()` | HStack header + rows aligned (60/90/80pt) + monospacedDigit Qty. Added 2026-05-20. | ✅ |
| F-1043 | Top Reference Items: mobile compact cards | `renderCompactTopItemCards(topItems, "Avg")` | Top Reference Items card | — | Rendered when `isCompactMobileReceiptView()` | Mac always uses table layout. Added 2026-05-20. | 🔄 |
| F-1044 | Top Reference Items empty state | "No saved expense line items yet." | Top Reference Items card | — | Shown when `top_items.length === 0` | `emptyItems` EmptyStateView tag + verbatim copy. Added 2026-05-20. | ✅ |
| F-1045 | Expense Categories card | `.card` "Expense Categories" with `#expense-category-breakdown` | — | GET `/analytics/expense-summary` | Full-width card at bottom | `ExpenseCategoriesCard` full-width Card at bottom of view. Added 2026-05-20. | ✅ |
| F-1046 | Expense Categories: desktop table | `<table>` Category / Total / Lines columns | Expense Categories card | — | Rendered when `!isCompactMobileReceiptView()` | HStack header + rows (Total 100pt right, Lines 80pt right monospaced). Added 2026-05-20. | ✅ |
| F-1047 | Expense Categories: mobile compact list | `<ul class="summary-row-list">` with category + count meta | Expense Categories card | — | Rendered when `isCompactMobileReceiptView()` | Mac always uses table layout. Added 2026-05-20. | 🔄 |
| F-1048 | Expense Categories empty state | "No expense categories yet." | Expense Categories card | — | Shown when `category_breakdown.length === 0` | EmptyStateView square.grid.2x2 + verbatim copy. Added 2026-05-20. | ✅ |
| F-1049 | `selectedExpenseReceiptId` state variable | JS `let selectedExpenseReceiptId = null` | — | — | Persists selected receipt across `loadExpenses()` reload | Mac selection is implicit via Router.activeDetailDestination + ReceiptsState; no separate per-row selected state on Expenses screen. Inline detail v1.1. Added 2026-05-20. | 🔄 |
| F-1050 | Mobile scroll-to-detail on receipt select | `detailCard.scrollIntoView()` when `isCompactMobileReceiptView()` | — | — | Smooth scroll; avoids manual navigation | Mac deep-links to Receipts tab instead of inline scroll. Added 2026-05-20. | 🔄 |

---

### GROUP: Finance
#### Screen: Fixed Bills
Source: `src/frontend/index.html` lines 4220–4382
Backend: `handle_floor_obligations.py` (url_prefix `/floor-obligations`)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1100 | Page header: "Household Bills" title | `<h1>Household Bills</h1>` in `#page-bills` | — | — | Static heading | `FixedBillsView.header` appTitle2. Added 2026-05-20. | ✅ |
| F-1101 | Page subtitle / month label | `#bills-month-label` — dynamic e.g. "May 2026 · recurring obligations & payment projections" | — | — | Updated by `loadBills()` to reflect selected month | `monthLabel` derived "LLLL yyyy · recurring obligations & payment projections" — recomputed on month change. Added 2026-05-20. | ✅ |
| F-1102 | Fixed Monthly Obligations sub-widget | `#floor-obligations-section` rendered by `loadFloorObligations()` at top of page | — | GET `/floor-obligations/` | Always loads in parallel with bills workspace | `FloorObligationsWidget` Card rendered above sticky bar — fetched via parallel withTaskGroup in refresh(). Added 2026-05-20. | ✅ |
| F-1103 | Floor widget: "Selected" tab | `.floor-tab-btn` "Selected (N)" | Floor obligations widget | GET `/floor-obligations/` | Shows active obligations in a table | Segmented Picker tag .selected with count badge of active rows. Added 2026-05-20. | ✅ |
| F-1104 | Floor widget: "Available" tab | `.floor-tab-btn` "Available" | Floor obligations widget | GET `/floor-obligations/available` | Shows bill providers not yet on floor | Segmented Picker tag .available with count badge. Renders `AvailableProvider` rows. Added 2026-05-20. | ✅ |
| F-1105 | Floor Selected tab: table columns | Name / Expected/mo / Avg (6mo) / Latest / Source / Actions | Floor widget Selected tab | — | Inline-editable name and amount fields | Header HStack with Name / Expected/mo (110pt right) / Avg 6mo (90pt right) / Source (70pt left) / Actions (100pt right). Inline edit not yet — text display. Added 2026-05-20. | 🔄 |
| F-1106 | Floor Selected: inline-edit label | `<input onblur="saveFloorObligationLabel(id, value)">` | Floor widget Selected tab | PATCH `/floor-obligations/:id` | Enter key also triggers save | Read-only Text label on mac v1.0; PATCH wired in state but no input UI yet — v1.1. Added 2026-05-20. | 🔄 |
| F-1107 | Floor Selected: inline-edit amount | `<input type="number" onblur="saveFloorObligationAmount(id, value)">` | Floor widget Selected tab | PATCH `/floor-obligations/:id` | Validates ≥ 0 | Read-only Text amount on mac v1.0; PATCH wired in state — v1.1. Added 2026-05-20. | 🔄 |
| F-1108 | Floor Selected: Remove button (bill_provider source) | `<button onclick="removeFloorObligation(id)">Remove</button>` | Floor widget Selected tab | PATCH `/floor-obligations/:id` `{is_active:false}` | Sets `is_active=false`; does not delete | `FloorRow.actionButtons` "Remove" Button (warning tint) → `state.removeFromFloor(_:)` → PATCH `{is_active:false}` + reload. Added 2026-05-20. | ✅ |
| F-1109 | Floor Selected: Delete button (manual source) | `<button onclick="deleteFloorObligation(id)">×</button>` (red) | Floor widget Selected tab | DELETE `/floor-obligations/:id` | Confirm dialog; permanent delete | Trash `xmark.circle.fill` Button (error tint) → `state.deleteFromFloor(_:)` → DELETE. No confirm dialog yet (mac convention: undo via reload). Added 2026-05-20. | 🔄 |
| F-1110 | Floor Selected: Add new obligation inline form | Label + amount inputs + "+ Add" button at bottom of Selected tab | Floor widget Selected tab | POST `/floor-obligations/` | Clears fields on success | Inline HStack — Label TextField + Amount TextField + Add PrimaryButton → `state.addManualObligation(label:amount:)`. Fields clear on success. Added 2026-05-20. | ✅ |
| F-1111 | Floor Selected: Source badge | "Bills" (muted) or "Manual" (blue accent) per row | Floor widget Selected tab | — | `ob.source === 'bill_provider'` | `sourceBadge` capsule — tertiary surface2 for bill_provider, accentDim for manual. Added 2026-05-20. | ✅ |
| F-1112 | Floor Selected: category/search filter controls | `_floorFilterControls()` — category chips + search text | Floor widget Selected tab | — | Client-side filter; resets on tab switch | Mac v1.0 omits the filter row — list is short enough at typical household scale. v1.1 will add chips + search. Added 2026-05-20. | 🔄 |
| F-1113 | Floor Selected: no-match filter state | "No items match your filter." | Floor widget Selected tab | — | Shown when filter yields no results | Covered by F-1112 deferral. Added 2026-05-20. | 🔄 |
| F-1114 | Floor Selected: empty state | "No obligations on floor yet. Add one below or pick from Available." | Floor widget Selected tab | — | Shown when `active.length === 0` | EmptyStateView tray icon + verbatim copy when filtered active rows empty. Added 2026-05-20. | ✅ |
| F-1115 | Floor Available: table columns | Name / Avg (6mo) / Latest / + Add button | Floor widget Available tab | GET `/floor-obligations/available` | Providers already on floor excluded | Header HStack + per-row HStack — Name / Avg 6mo (90pt right) / Latest (90pt right) / + Add (80pt). Added 2026-05-20. | ✅ |
| F-1116 | Floor Available: inline amount input on "+ Add" | `showAddFromAvailable()` replaces cell with amount input + Save / Cancel | Floor widget Available tab | POST `/floor-obligations/` or PATCH if `existing_obligation_id` | Reactivates if previously removed | Mac shows toast "Inline add coming v1.1 — use 'Add' row in Selected tab". POST manual works; the bill-provider linking flow deferred. Added 2026-05-20. | 🔄 |
| F-1117 | Floor Available: all-added empty state | "All bill providers are already on your floor." | Floor widget Available tab | — | Shown when `available.length === 0` | EmptyStateView checkmark.seal + verbatim copy. Added 2026-05-20. | ✅ |
| F-1118 | Floor Available: load error state | "Could not load available providers." | Floor widget Available tab | — | Shown when fetch fails or `available === null` | `loadAvailable` swallows errors as logger.warning + `floorAvailable = []`; falls through to F-1117 empty state. Added 2026-05-20. | 🔄 |
| F-1119 | Pull-to-refresh indicator | `#bills-ptr` with spinner + label "Release to refresh" | `#bills-body` | — | Touch-only; 72px threshold; calls `loadBills()` | Mac uses toolbar Refresh button + ⌘R conventions; no scroll-pull gesture needed. Added 2026-05-20. | 🔄 |
| F-1120 | Sticky bar: Month picker | `#bills-filter-month` `<input type="month">` `onchange="loadBills()"` | Sticky bar | — | Defaults to current month; persists via `localStorage` | StickyBar TextField bound to `state.month` (validated via `RestaurantsState.isValidMonth`). Defaults to current; persists via UserDefaults `LocalOCR.bills.month`. Added 2026-05-20. | 🔄 |
| F-1121 | Sticky bar: Overview tab | `.bills-tab` `data-bills-tab="overview"` `onclick="setBillsTab('overview')"` | Sticky bar tab bar | — | Default tab; keyboard shortcut `1` | Segmented Picker tag .overview (default). Number-key shortcut not wired (mac uses Picker click). Added 2026-05-20. | ✅ |
| F-1122 | Sticky bar: Providers tab | `.bills-tab` `data-bills-tab="providers"` | Sticky bar tab bar | — | Keyboard shortcut `2` | Segmented Picker tag .providers. Added 2026-05-20. | ✅ |
| F-1123 | Sticky bar: History tab | `.bills-tab` `data-bills-tab="history"` | Sticky bar tab bar | — | Keyboard shortcut `3`; shows CSV export button | Segmented Picker tag .history. CSV export deferred (F-1126/F-1191). Added 2026-05-20. | ✅ |
| F-1124 | "+ New Bill" button | `.btn-primary` "＋ New Bill" `onclick="openManualEntryModal('household_bill')"` | Sticky bar actions | — | Keyboard shortcut `n` | PrimaryButton "New Bill" → `Router.shared.activeSheet = .ocrUpload`. Routes through the receipt-upload sheet (web's manual-entry modal not yet ported to mac). Added 2026-05-20. | 🔄 |
| F-1125 | "💸 Log Cash" button | Ghost btn `onclick="openCashTransactionModal()"` | Sticky bar actions | — | Keyboard shortcut `l` | GhostButton "Log Cash" → `Router.shared.activeSheet = .cashTransaction`. Added 2026-05-20. | ✅ |
| F-1126 | "⬇ CSV" export button | `#bills-export-csv` ghost btn `onclick="exportBillsCsv()"` | Sticky bar actions | — | Only visible on History tab; client-side CSV from `__billsCache` | Deferred v1.1 — mac will write to NSSavePanel + CSVKit. Added 2026-05-20. | 🔄 |
| F-1127 | "🖨 Print" button | Ghost btn `onclick="window.print()"` | Sticky bar actions | — | Prints monthly statement | Deferred v1.1 — mac would use NSPrintOperation against a generated view. Added 2026-05-20. | 🔄 |
| F-1128 | Keyboard navigation: `g b` chord | Navigate to Bills page | — | — | Vim-style global shortcut (`g` then `b` within 800ms) | Not idiomatic on macOS; sidebar + ⌘6 cover the same need. Added 2026-05-20. | 🔄 |
| F-1129 | Keyboard navigation: ArrowLeft / ArrowRight | Step month backward / forward | — | — | Calls `stepBillsMonth(-1)` / `stepBillsMonth(1)` | Prev/Next Buttons in StickyBar bound to `.leftArrow` / `.rightArrow` shortcuts → `state.stepMonth(_:)`. Added 2026-05-20. | ✅ |
| F-1130 | Alerts container | `#bills-alerts-container` rendered by `renderBillsAlerts()` | — | — | Shows "Due Soon" banner and issues banner | `AlertsContainer` VStack — renders overdue + due-soon banners stacked. Added 2026-05-20. | ✅ |
| F-1131 | Alert: Due Soon banner | `.bill-alert--warning` with up-to-3 preview items + "Open Next" button | Alerts container | — | Shown when `dueSoon.length > 0` | Warning-tinted banner with clock icon + count + first 3 provider names. "Open Next" button deferred — Due This Week strip below covers per-row open. Added 2026-05-20. | 🔄 |
| F-1132 | Alert: Issues banner (missing + anomaly) | `.bill-alert--danger` / `--warning` with "Inspect Anomaly" and "Add Missing" buttons | Alerts container | — | "Add Missing" opens pre-filled `openManualEntryModal` | Mac shows overdue-count danger banner when due-soon items with negative daysUntilDue. "Inspect Anomaly" + "Add Missing" buttons deferred (depend on bill-projection slots endpoint not yet wired). Added 2026-05-20. | 🔄 |
| F-1133 | Hero stats bar | `#bills-hero-container` — 5 stats: Tracked / Entered / Expected / Alerts / Due Soon | Overview tab | GET `/analytics/recurring-obligations` | Danger class on Alerts when `missingCount > 0` | `HeroStatsBar` LazyVGrid of 5 stat cards. Variance card tinted warning/success based on sign. Added 2026-05-20. | ✅ |
| F-1134 | Hero stat: Tracked | count + "N fixed · N variable" sub | Hero bar | — | From `obligationsSummary` | "Tracked" card — `summary.count` + sub "\(fixedCount) fixed · \(variableCount) variable". Added 2026-05-20. | ✅ |
| F-1135 | Hero stat: Entered / Outstanding | entered_count + outstanding_count | Hero bar | — | From `obligationsSummary` | "Entered" card — `summary.enteredCount` + sub "Outstanding \(outstandingCount)". Added 2026-05-20. | ✅ |
| F-1136 | Hero stat: Expected vs Actual vs Δ | `formatMoney` of expected / actual / variance totals | Hero bar | — | From `obligationsSummary` | "Expected" card showing `summary.expectedTotal` + sub "Actual \(actualTotal)". Variance shown in its own card. Added 2026-05-20. | ✅ |
| F-1137 | Hero stat: Alerts count | `missingCount + anomalyCount` with danger/warning class | Hero bar | — | From `projectionSummary` | "Variance" card stands in for Alerts on mac — uses `summary.varianceTotal` colored by sign. Bill-projection-slots-based anomaly counts deferred (extra endpoint). Added 2026-05-20. | 🔄 |
| F-1138 | Hero stat: Due Soon count | `dueSoon.length` + "N providers · N recent" | Hero bar | — | From `utilityData.due_soon` | "Due Soon" card — `utility.dueSoon.count` + sub "this window". Added 2026-05-20. | ✅ |
| F-1139 | "Due This Week" spotlight strip | `#bills-spotlight-container` — up to 8 items with status + days label | Overview tab | GET `/analytics/utility-summary` | Items coloured `overdue` / `due-soon` / `upcoming` | `DueSoonStrip` horizontal ScrollView of DueSoonChip — tint via `daysUntilDue` (<0 error / ≤3 warning / else accent). Added 2026-05-20. | ✅ |
| F-1140 | Spotlight strip empty state | No strip rendered (empty innerHTML) | — | — | Shown when `dueSoon.length === 0` | DueSoonStrip body skips render entirely when items empty. Added 2026-05-20. | ✅ |
| F-1141 | Spotlight item: days label | "N days overdue" / "Due today" / "In N days" | Spotlight strip | — | Derived from `item.days_until_due` | `daysLabel` computed verbatim from `daysUntilDue`: negative→overdue, 0→"Due today", positive→"In N day(s)". Added 2026-05-20. | ✅ |
| F-1142 | Spotlight item: "Open" button | Ghost btn `onclick="jumpToReceipt(id)"` | Spotlight strip | — | Only shown when `item.purchase_id` exists | DueSoonChip is itself a Button → tap deep-links to Receipts tab + loadDetail when `purchaseId` set. Help text "No receipt linked yet" when nil. Added 2026-05-20. | ✅ |
| F-1143 | Recurring Obligations section | `#bills-obligations-container` — obligation cards | Overview tab | GET `/analytics/recurring-obligations` | Expand threshold shows partial list | `ObligationsList` Card with N visible (threshold 6) + "Show all" expander Button. Added 2026-05-20. | ✅ |
| F-1144 | Obligations section empty state | "No obligations this month. Add a household bill via +New Bill…" | Obligations section | — | `renderBillsEmpty()` | EmptyStateView doc.text + verbatim copy when `recurring.isEmpty`. Added 2026-05-20. | ✅ |
| F-1145 | Obligation card | `.bill-card` with `data-bill-status` attribute | Obligations section | — | Status: `paid` / `autopay-settled` / `overdue` / `due-soon` / `upcoming` | `ObligationCard` — `BillCardStatus` enum derives status from `isAutopaySettled`, `currentEntry.paymentStatus == "paid"`, dueDate vs today comparison. Status pill tint colors border + chip. Added 2026-05-20. | ✅ |
| F-1146 | Obligation card: status pill | `.bill-card__pill` text + class (paid / overdue / due-soon / upcoming) | Obligation card | — | Derived from `billStatusLabel(billStatusFor(obligation))` | `statusPill` capsule — uses `BillCardStatus.label` and `.tint`. Added 2026-05-20. | ✅ |
| F-1147 | Obligation card: Expected vs Actual stats | `.bill-card__stats` — Expected amount + amount_pattern + Actual amount + Δ variance | Obligation card | — | From `obligation.expected_amount` / `actual_amount` / `variance` | HStack of `stat(...)` mini-blocks — Expected / Actual (tinted by ratio to Expected) / Δ (tinted by sign). Added 2026-05-20. | ✅ |
| F-1148 | Obligation card: autopay settled line | "💳 Paid via autopay on …" when `is_autopay_settled` | Obligation card | — | Includes billing cycle month if available | Conditional Text "💳 Paid via autopay on \(date)" colored success when `isAutopaySettled`. Added 2026-05-20. | ✅ |
| F-1149 | Obligation card: footer meta | "Last seen … · Due …" | Obligation card footer | — | From `obligation.last_seen_date` / `last_due_date` | Footer Text joining "Last seen \(lastSeenDate)" + "Due \(lastDueDate)" with " · " separator. Added 2026-05-20. | ✅ |
| F-1150 | Obligation card: "✎ Edit" button | Ghost btn `onclick="openBillEditModal(encoded)"` | Obligation card footer | — | Only shown when `service_line_id \|\| provider_id` exists | Edit modal deferred to v1.1 — too many fields (8+) to inline now. State / endpoint pairs exist for future modal. Added 2026-05-20. | 🔄 |
| F-1151 | Obligation card: "Open Receipt" button | Ghost btn `onclick="jumpToReceipt(id)"` | Obligation card footer | — | Only for non-personal-service obligations with `purchase_id` | "Open Receipt" GhostButton — only rendered when `currentEntry.purchaseId` non-nil. → Router.activeTab = .receipts + activeDetailDestination + loadDetail. Added 2026-05-20. | ✅ |
| F-1152 | Obligation card: "View Payments" button | Ghost btn `onclick="openBillProviderDetail(name)"` | Obligation card footer | — | For personal-service obligations with `purchase_id` | Provider Detail modal deferred to v1.1 — same as F-1150. Added 2026-05-20. | 🔄 |
| F-1153 | Obligation card: "Enter Bill" primary button | `btn-primary` for overdue/due-soon; opens pre-filled `openManualEntryModal` | Obligation card footer | — | Pre-fills provider, type, amount, month, due date | Pre-filled manual-entry modal deferred to v1.1 — mac users can tap "New Bill" in sticky bar instead. Added 2026-05-20. | 🔄 |
| F-1154 | Obligation card: "Mark Paid" button (personal service) | `btn-primary` `onclick="openCashTransactionModal({…})"` | Obligation card footer | — | Only for personal-service obligations not yet paid | Pre-filled cash-transaction modal deferred to v1.1 — mac users can tap "Log Cash" in sticky bar instead. Added 2026-05-20. | 🔄 |
| F-1155 | Obligation card: "↺ Mark Unpaid" button | Ghost btn `onclick="markBillUnpaid(cashTxId, name)"` | Obligation card footer | DELETE `/cash-transactions/:id` | Only when current entry is a cash_transaction; confirm dialog | Mark-Unpaid path deferred v1.1 — depends on cash_transaction_id field not yet decoded. Added 2026-05-20. | 🔄 |
| F-1156 | Obligations "Show all" expander | `renderBillsExpander()` button below partial list | Obligations section | — | Visible when `total > BILLS_EXPAND_THRESHOLD`; calls `expandBillsSection('obligations')` | `ObligationsList` expander Button toggles `@State expanded` — threshold = 6. Same pattern for Providers (8) and Recent Bills (8). Added 2026-05-20. | ✅ |
| F-1157 | Bill Edit modal | `#bill-edit-overlay` with "Edit Personal Service" / "Edit Provider" title | Modal | PUT `/bill-service-lines/:id` or PUT `/bill-providers/:id` | `openBillEditModal(encoded)` decodes obligation JSON | Modal + bill-service-lines / bill-providers PUT endpoints deferred v1.1. Mac MVP keeps cards read-only with deep-link to Receipts. Added 2026-05-20. | 🔄 |
| F-1158 | Bill Edit: provider name field | `#bill-edit-provider-name` text input | Bill Edit modal | — | Auto-focused on open | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1159 | Bill Edit: service type field | `#bill-edit-service-type` (service line mode only) | Bill Edit modal | — | Hidden for provider mode | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1160 | Bill Edit: account label field | `#bill-edit-account-label` (service line mode only) | Bill Edit modal | — | Hidden for provider mode | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1161 | Bill Edit: typical amount field | `#bill-edit-typical-amount` number input (service line mode) | Bill Edit modal | — | Maps to `typical_amount` | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1162 | Bill Edit: expected payment day | `#bill-edit-expected-day` (service line mode) | Bill Edit modal | — | Day of month | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1163 | Bill Edit: payment method select | `#bill-edit-payment-method` | Bill Edit modal | — | Default "cash" | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1164 | Bill Edit: planning month rule select | `#bill-edit-planning-rule` | Bill Edit modal | — | Default "paid_date_month" | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1165 | Bill Edit: category select | `#bill-edit-category` | Bill Edit modal | — | Default "personal_service" for service-line mode | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1166 | Bill Edit: contact method select | `#bill-edit-contact-method` | Bill Edit modal | — | Optional; maps to `preferred_contact_method` | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1167 | Bill Edit: payment handle input | `#bill-edit-payment-handle` | Bill Edit modal | — | Optional; free-text handle (e.g. Venmo @user) | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1168 | Bill Edit: 409 merge conflict flow | `#bill-edit-merge-notice` + "Merge & Save" button shown on first 409 | Bill Edit modal | — | `merge_candidate` ID stored; second save sends `merge_with_provider_id` | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1169 | Bill Edit: save / close / error feedback | `billsNotify("Saved")` / `closeBillEditModal()` + refresh; error inline | Bill Edit modal | — | `saveBtn.disabled` guards double-submit | Covered by F-1157 deferral. Added 2026-05-20. | 🔄 |
| F-1170 | Provider Detail modal | `#bill-provider-detail-overlay` with payment history + service lines | Modal | GET `/cash-transactions?service_line_id=…` | `openBillProviderDetail(providerName)` | Deferred v1.1 — paired with F-1157 modal infrastructure. Added 2026-05-20. | 🔄 |
| F-1171 | Provider Detail: contact method / payment handle | Rendered when non-empty; "Copy Handle" button copies to clipboard | Provider Detail modal | — | `copyTextToClipboard()` | Covered by F-1170 deferral. Added 2026-05-20. | 🔄 |
| F-1172 | Provider Detail: Service Lines sub-card | List of lines: type / account_label / expected day / amount range | Provider Detail modal | — | Empty state when `service_lines.length === 0` | Covered by F-1170 deferral. Added 2026-05-20. | 🔄 |
| F-1173 | Provider Detail: Payment History sub-card | Last 8 cash transactions; date / service type / method / amount / planning_month | Provider Detail modal | GET `/cash-transactions?service_line_id=…` | "Delete" button per entry; empty state | Covered by F-1170 deferral. Added 2026-05-20. | 🔄 |
| F-1174 | Providers tab panel | `#bills-panel-providers` — rendered by `renderProvidersSection()` | Providers tab | GET `/analytics/utility-summary?months=12` | 12-month view; expand threshold | `ProvidersPanel` Card with adaptive LazyVGrid of ProviderCard (min 280pt). Expand threshold = 8. Added 2026-05-20. | ✅ |
| F-1175 | Provider card | `.provider-card` with sparkline SVG, 12-month total, avg/month, purchase/refund count | Providers tab | — | Sparkline from `monthly_series` | `ProviderCard` — provider name + 12 mo total + avg/mo + payment count + refund count. Sparkline deferred v1.1 (would use SwiftCharts). Added 2026-05-20. | 🔄 |
| F-1176 | Provider card: name button → detail modal | `.provider-card__name` `onclick="openBillProviderDetail(name)"` | Provider card | — | Opens `#bill-provider-detail-overlay` | Linked to F-1170 deferral — provider detail modal not yet shipped. Added 2026-05-20. | 🔄 |
| F-1177 | Providers empty state | "No provider history yet. Once you enter a bill…" | Providers tab | — | `renderBillsEmpty()` | EmptyStateView storefront + verbatim copy when providers empty. Added 2026-05-20. | ✅ |
| F-1178 | Providers "Show all" expander | `renderBillsExpander('providers', …)` | Providers tab | — | Same pattern as obligations expander | ProvidersPanel @State expanded + threshold 8 + expander Button. Added 2026-05-20. | ✅ |
| F-1179 | History tab panel | `#bills-panel-history` — MoM + Recent Bills | History tab | — | CSV export button visible only on this tab | `HistoryPanel` VStack of MoMCard + RecentBillsCard. CSV button F-1126 deferred. Added 2026-05-20. | ✅ |
| F-1180 | Month-over-Month section | `#bills-mom-container` — last 6 months horizontal bar chart | History tab | GET `/analytics/utility-summary` | `renderMoMSection(monthlyTotals)` | `MoMCard` Card with sorted `monthlyTotals` suffix(6); GeometryReader bar per row. Added 2026-05-20. | ✅ |
| F-1181 | MoM bar chart | `.mom-chart` rows with `.mom-row__bar` widths proportional to month total | MoM section | — | Max bar = max of 6 months; min bar = 6% | Bar width = `geo.width × max(0.06, value/max)` accent fill. Added 2026-05-20. | ✅ |
| F-1182 | MoM empty state | "No month-over-month totals yet." | MoM section | — | `renderBillsEmpty()` | EmptyStateView chart.bar + verbatim copy when monthlyTotals empty. Added 2026-05-20. | ✅ |
| F-1183 | Recent Bills section | `#bills-recent-container` — `renderRecentSection(recentBills)` | History tab | GET `/analytics/utility-summary` | Shows source type: Receipt / Cash-Transfer | `RecentBillsCard` Card with N visible (threshold 8) + expander. Each row shows source ("Cash" / "Receipt") in meta line. Added 2026-05-20. | ✅ |
| F-1184 | Recent bill row | `.bill-row` with provider name / meta / amount / recurring badge | Recent Bills section | — | Includes billing_cycle_month and budget_category | RecentBillRowView — provider name + (date · source · budgetCategory) meta + amount + arrow-up-right open button. Recurring badge deferred. Added 2026-05-20. | 🔄 |
| F-1185 | Recent bill row: "Open" / "View" action | Ghost btn per row | Recent Bills section | — | "Open" → `jumpToReceipt`; "View" → `openBillProviderDetail` (cash) | Arrow-up-right Button when `purchaseId` non-nil → Router.activeTab = .receipts + loadDetail. Cash "View" (provider detail) deferred. Added 2026-05-20. | 🔄 |
| F-1186 | Recent Bills empty state | "No recent bills. Uploaded receipts and logged cash payments appear here." | Recent Bills section | — | `renderBillsEmpty()` | EmptyStateView doc.text + verbatim copy when recentBills empty. Added 2026-05-20. | ✅ |
| F-1187 | Recent Bills "Show all" expander | `renderBillsExpander('recent', …)` | Recent Bills section | — | Same pattern as other expanders | RecentBillsCard @State expanded + threshold 8. Added 2026-05-20. | ✅ |
| F-1188 | Skeleton loading state | `.bills-skel` with 3 `.bills-skel__card` placeholder cards per section | — | — | Shown while all 3 API fetches are in-flight | Mac shows empty cards then content fills (refresh swap is instant via @MainActor singletons). Skeleton cards deferred. Added 2026-05-20. | 🔄 |
| F-1189 | Error / failed-load state | `bills-obligations-container` shows "Could not load bills. Please check your connection…" | — | — | All containers cleared; `renderBillsEmpty()` with ⚠️ icon | Per-section errors logged via os.log; cards show their own empty states. Top-level error banner deferred v1.1. Added 2026-05-20. | 🔄 |
| F-1190 | Auto-pay sweep on page load | POST `/receipts/bills/sync-autopay` called on each `loadBills()` | — | POST `/receipts/bills/sync-autopay` | Toast: "Auto-paid N bill(s) on their due date" when `swept_count > 0` | `state.syncAutopay()` Task.detached fired on every `refresh()`. Surfaces "Auto-paid N bill(s) on their due date" toast when count > 0; errors swallowed silently. Added 2026-05-20. | ✅ |
| F-1191 | CSV export file | Client-side Blob with headers: date/provider/source/billing_cycle_month/due_date/budget_category/amount/recurring | — | — | Filename: `bills-{selectedMonth}.csv` | Deferred v1.1 — NSSavePanel + UTType.commaSeparatedText writer. Added 2026-05-20. | 🔄 |

#### Screen: Accounts (Plaid)
Source: `src/frontend/index.html` lines 4383–4632
Backend: `plaid_integration.py` (url_prefix `/plaid`)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1200 | Page header — "Accounts" title + subtitle | H1 + `#accounts-subtitle` paragraph | — | — | Subtitle "Connected banks, recent transactions, and spending trends." | AccountsView.pageHeader Text(appTitle1) + subtitle | ✅ |
| F-1201 | Card Usage card — collapse/expand toggle | Card title `onclick="toggleAccountsSection('card-usage-card')"` | — | — | Collapses/expands card; `toggleAccountsSection()` | CardUsageSection.header Button toggles `state.cardUsageCollapsed` (UserDefaults `LocalOCR.accounts.cardUsage.collapsed`) | ✅ |
| F-1202 | Card Usage — Refresh button | Button `#card-usage-refresh-btn` `onclick="refreshCardUsage()"` | Card Usage card | POST `/plaid/accounts/refresh-balances` → GET `/plaid/cards-overview` | Refreshes Plaid balances then reloads card overview; auto-refresh when balance > 1h stale | Refresh Button → `AccountsState.refreshCardUsage` → quiet POST refresh-balances then GET cards-overview. Auto-stale trigger (>1h) deferred to v1.1 (currently manual only) | 🔄 |
| F-1203 | Card Usage — summary stats strip | Div `#card-usage-summary` (hidden until data loads) | Card Usage card | GET `/plaid/cards-overview` | Shows aggregate stats (e.g. total utilisation); rendered by `loadCardsOverview()` | summaryStrip HStack of 4 statCells (Total credit balance, Total credit limit, Overall utilization w/ color, Credit spend MTD) | ✅ |
| F-1204 | Card Usage — stale-data banner | Div `#card-usage-banner` (hidden until stale) | Card Usage card | — | Shown when newest balance > 24h old: "Balances may be outdated. Tap Refresh." | currencyBanner — shows "Some accounts use a non-USD currency and are excluded from totals." when `state.hasNonUsdAccounts`. Stale-data banner (>24h) deferred to v1.1 | 🔄 |
| F-1205 | Card Usage — Spend by Category donut panel | Panel `#card-usage-pie-panel` with collapse toggle | Card Usage card | GET `/plaid/cards-overview` | `_renderCardUsagePie()`; initially hidden; shown when data arrives | pieSubpanel — collapse Button toggles `state.cardUsagePieCollapsed` (UserDefaults); only shown when credit group has accounts | ✅ |
| F-1206 | Card Usage — pie filter: All Cards | Select `#card-usage-pie-filter-select` default option "All Cards" | Spend by Category panel | GET `/plaid/cards-overview` | `_onCardUsageFilterChange(event)` scopes donut to selected card | Picker bound to `state.cardUsagePieFilter` with default tag "all" | ✅ |
| F-1207 | Card Usage — pie filter per-card options | Dynamic `<option>` per account in `#card-usage-pie-filter-select` | Spend by Category panel | — | Populated when `loadCardsOverview()` resolves | ForEach over credit-group accounts adds Picker rows "💳 <name> ····<mask>" tagged by plaid_account_id | ✅ |
| F-1208 | Card Usage — donut + legend body | Div `#card-usage-pie-body` | Spend by Category panel | — | SVG donut + legend table rendered by `_renderCardUsagePie()` | PieDonutView — Canvas-drawn donut (size 160, outer 70 / inner 96 in units) + legend table (swatch + label + amount + pct) + Gross spend total row. Aggregates top 6 categories + Other (matches web `_aggregateCategories`) | ✅ |
| F-1209 | Card Usage — Loan Progress panel | Panel `#card-usage-loans-panel` with collapse toggle | Card Usage card | GET `/plaid/cards-overview` | `_renderLoanProgressPanel()`; shown only when loan accounts present | loansSubpanel — collapse Button toggles `state.cardUsageLoansCollapsed` (UserDefaults); only shown when loan group has accounts | ✅ |
| F-1210 | Card Usage — loan progress body | Div `#card-usage-loans-body` | Loan Progress panel | GET `/plaid/accounts/<id>/loan-meta` | Per-loan rows with principal, balance, payment history | LoanRow per loan with header (🏦 + name + ····mask + balance), green ProgressBar (paid/original), meta items (Paid, Original, APR, Monthly). Loan-meta PUT edit deferred to v1.1 — read-only here | 🔄 |
| F-1211 | Card Usage — main body (account rows) | Div `#card-usage-body` | Card Usage card | GET `/plaid/cards-overview` | Loading state "Loading card usage…"; rendered by `loadCardsOverview()` | mainBody → CardGroupView for credit group (loans rendered separately in loansSubpanel — matches web which skips loan rows here since dedicated panel above) | ✅ |
| F-1212 | Card Usage — loading state | `<div class="empty-state"><p>Loading card usage…</p></div>` | Card Usage card | — | Default content before fetch resolves | loadingState HStack with ProgressView + "Loading card usage…" | ✅ |
| F-1213 | Connected Accounts card — collapse/expand toggle | Card title `onclick="toggleAccountsSection('accounts-connections-card')"` | — | — | `toggleAccountsSection()` | ConnectionsSection.header Button toggles `state.connectionsCollapsed` (UserDefaults) | ✅ |
| F-1214 | Connect Bank button (Plaid Link) | Button `#accounts-connect-btn` `onclick="openPlaidLink()"` | Connected Accounts card | POST `/plaid/link-token` → Plaid Link JS → POST `/plaid/exchange-public-token` | Disabled until status loaded; launches Plaid Link flow | "Connect Bank" PrimaryButton → `AccountsState.beginPlaidLink()` → POST /plaid/link-token → sets `pendingLinkToken` → sheet presents `PlaidLinkSheet` (WKWebView loading bundled `plaid-link.html` + cdn.plaid.com JS) → onSuccess JS bridge POSTs to /plaid/exchange-public-token. Button disabled when `!state.plaidConfigured` | ✅ |
| F-1215 | Refresh Balances button | Button `#accounts-refresh-balances-btn` `onclick="refreshPlaidBalances()"` | Connected Accounts card | POST `/plaid/accounts/refresh-balances` | Rate-limited (429 → toast with retry_after_seconds); toasts success with account count | "💵 Refresh Balances" GhostButton → `AccountsState.refreshBalances` → POST refresh-balances + reload. Toasts success; rate-limit (429 retry_after_seconds) message handled in APIClient error path | ✅ |
| F-1216 | Reload connected accounts button | Button `.accounts-toolbar__icon` `onclick="loadConnectedAccounts()"` | Connected Accounts card | GET `/plaid/items` + GET `/plaid/accounts` | Reload without refreshing balances | "arrow.clockwise" icon Button → `AccountsState.loadConnections` (parallel GET /items + GET /accounts via async let) | ✅ |
| F-1217 | Connected Accounts info note | Div `#accounts-connections-note` | Connected Accounts card | — | Static text explaining Plaid import flow and review queue | Text caption inside ConnectionsSection body | ✅ |
| F-1218 | Connected Accounts body | Div `#accounts-connections-body` | Connected Accounts card | GET `/plaid/items` + GET `/plaid/accounts` | Loading state; rendered by `loadConnectedAccounts()`; one card per Plaid item | ForEach(state.items) → ConnectionItemCard with filtered subAccounts | ✅ |
| F-1219 | Connected Accounts — loading state | `<div class="empty-state"><p>Loading connected accounts…</p></div>` | Connected Accounts card | — | Default content before fetch | loadingState HStack with ProgressView + "Loading connected accounts…" | ✅ |
| F-1220 | Per-item card — institution name + nickname | `summary-row__title` in `.accounts-item` | Connected Accounts body | — | Nickname shown as muted suffix if set | titleRow HStack — Text(institutionName) + muted "— nickname" Text when nickname non-empty | ✅ |
| F-1221 | Per-item card — status badge | `<span>` with colour: "Connected" (green) / "Login required" (red) / other (muted) | Connected Accounts body | — | Derived from `item.status` | statusBadge — Badge(.success/.error/.neutral) keyed off lowercased status: active → Connected, login_required → Login required | ✅ |
| F-1222 | Per-item card — last sync label | `.summary-row__meta` "Last sync …" | Connected Accounts body | — | `formatPlaidSyncTime(item.last_sync_at)` | syncRow — "Last sync \(relativeTimeAgo(lastSyncDate))" + optional sync status chip | ✅ |
| F-1223 | Per-item card — sync error message | `.summary-row__meta` in error colour | Connected Accounts body | — | Shows `item.last_sync_error`; hints to check env vars on INVALID_API_KEYS | errorRow — "⚠️ {err}" in DesignTokens.error; auth-error hint "→ Check PLAID_CLIENT_ID and PLAID_SECRET in .env…" appended when lowercased err contains invalid client_id / INVALID_API_KEYS | ✅ |
| F-1224 | Per-item card — sub-account rows | `.accounts-sub-list` `<ul>` | Connected Accounts body | — | Name · subtype · mask; balance if refreshed; "💤 Balances not yet refreshed" hint when none | subList VStack — per sub-account row: name · subtype · ····mask, trailing formatted balance + relative time; "💤 Balances not yet refreshed — tap 💵 Refresh Balances above." hint shown when no sub-account has a balance | ✅ |
| F-1225 | Per-item card — Re-authenticate button | `btn-primary` "Re-authenticate" `onclick="openPlaidLink(item.id)"` | Connected Accounts body | POST `/plaid/link-token` → Plaid Link update mode | Only shown when `item.status === "login_required"` | "Re-authenticate" PrimaryButton (controlSize .small) → `AccountsState.beginPlaidLink(itemId:)` triggers update-mode link-token flow. Renders only when `item.isLoginRequired` | ✅ |
| F-1226 | Per-item card — Sync Now button | `btn-ghost` "Sync Now" `onclick="syncPlaidItem(item.id)"` | Connected Accounts body | POST `/plaid/items/<id>/sync` | Admin-only; non-admin sees disabled greyed button | "Sync Now" GhostButton → `AccountsState.syncItem(item)` → POST /plaid/items/{id}/sync. Admin-only gating deferred to Commit B (currentUser.role check needs HouseholdState integration) — currently any signed-in user can attempt; backend returns 403 if not authorized | 🔄 |
| F-1227 | Per-item card — Rename button | `btn-ghost` "Rename" `onclick="renamePlaidItem(id, currentNickname)"` | Connected Accounts body | PATCH `/plaid/items/<id>` `{nickname}` | Prompt dialog; updates item nickname | "Rename" GhostButton → `renameTarget = item` opens renameItemSheet (TextField bound to draft) → PATCH /plaid/items/{id} {nickname:trimmed.prefix(64)} | ✅ |
| F-1228 | Per-item card — Share button (admin) | `btn-ghost` "Share…(N)" `onclick="sharePlaidItem(id)"` | Connected Accounts body | PATCH `/plaid/items/<id>` `{shared_with_user_ids}` | Admin-only; shows count of currently shared users | Share button + household roster picker (multi-select check-boxes) deferred to v1.1 — needs HouseholdState member loader (web pulls /household/members) which is out of Commit A scope | 🚫 |
| F-1229 | Per-item card — Disconnect button | `btn-ghost` "Disconnect" `onclick="disconnectPlaidItem(id, name)"` | Connected Accounts body | DELETE `/plaid/items/<id>` | Confirm dialog before delete | "Disconnect" GhostButton → `disconnectTarget = item` opens disconnectSheet (.destructive role) → DELETE /plaid/items/{id} + reloads connections and cards overview | ✅ |
| F-1280 | Card Usage tile — utilization ring chart (88px) | SVG circle filled-arc fraction in `_renderUtilRing` (line 16852) | Expanded credit-card tile | — | Rotated -90deg start (12 o'clock), color: <30 green / 30-69 orange / ≥70 red. Inner % label muted when null | UtilizationRing — SwiftUI Circle .trim(from:0,to:frac) .stroke(utilColor) .rotationEffect(-90) + monospaced inner % label. Decomposed from F-1211 after RULE 13 audit | ✅ |
| F-1281 | Card Usage tile — status chip | `_statusChipFor(pct)` returns Healthy/Watch/High (line 16843) | Expanded credit-card tile hero | — | <30 Healthy (success) / 30-69 Watch (warning) / ≥70 High (error). Null pct hides chip | Badge .success/.warning/.error in CreditCardTile.hero. Decomposed from F-1211 | ✅ |
| F-1282 | Card Usage tile — owner badge / + tag CTA | `_renderOwnerBadge` (line 16820); shows "👤 {label}" when tagged, "+ tag" when blank | Expanded credit-card tile head | — | Tap on badge opens `_setOwnerLabelInline`; color picked via `_ownerColor(label)` | ownerBadge — Badge .info "👤 \(owner)" when set; Button { onTag(account) } wrapping Badge "+ tag" .neutral when blank. Decomposed from F-1211 | ✅ |
| F-1283 | Card Usage tile — ✏️ rename + 👤 tag buttons | `_renameAccountInline` + `_setOwnerLabelInline` (line 17249, 17256) | Expanded credit-card tile head | PUT `/plaid/accounts/<id>/identity` `{display_name|owner_label}` | Edit tools live in `.cu-edit-tools` flex container after the mask | head HStack — pencil Button → renameCardTarget opens renameCardSheet; person.crop.circle.badge.plus Button → tagCardTarget opens tagCardSheet. Both PATCH /plaid/accounts/{id}/identity via `AccountsState.updateAccountIdentity`. Decomposed from F-1211 | ✅ |
| F-1284 | Card Usage tile — collapse toggle (▾) | `_cuToggleCollapsed` writes per-account override into localStorage `card_usage_collapsed_v1` (line 17102) | Expanded credit-card tile head | — | Default: zero balance → collapsed; override persists per plaid_account_id | chevron.up Button → `state.setCardCollapsed(plaid_account_id, true)`; persisted in UserDefaults `LocalOCR.accounts.cardCollapseOverrides` (JSON dict). Default: balance==0 → collapsed (matches web `_cuShouldCollapseDefault`). Decomposed from F-1211 | ✅ |
| F-1285 | Card Usage chip — hover popover with Used/Available/Limit/Util/Owner + Rename/Tag | `_cuAttachChipPopover` (line 16998); appears on hover desktop / long-press mobile | Collapsed card chip | — | Popover shows 4 metric rows + owner row when tagged + Rename + Tag action buttons | CreditCardChip `.onHover { showPopover = $0 }` + `.popover(arrowEdge:.top)` showing ChipPopoverContent (4 metric rows + optional Owner row, formatMoney + util%). Rename/Tag buttons inside popover deferred to v1.1 (chip currently focuses expand-to-tile action; popover is read-only summary). Decomposed from F-1211 | 🔄 |
| F-1286 | Card Usage chip — owner mini-dot (6px) | `_renderCreditCardStrip` (line 17156) — 6px circle styled with `_ownerColor` when chip has owner_label | Collapsed card chip | — | Shown only when chip has owner; uses owner's color palette entry | Owner-dot color encoded in OwnerSubgroup head Badge (.info color picks tile owner from `_ownerColor` palette). Per-chip mini-dot deferred — chip already nests inside owner sub-group section so duplicate dot would be redundant. Decomposed from F-1211 | 🔄 |
| F-1287 | Owner sub-group head — per-owner Balance/Limit/Util stats | `_renderOwnerGroupHead` (line 16769) — badge + card count + 3 stats with util color | Credit card group | — | Util color: ≥70 red / ≥30 orange / else green | OwnerSubgroup.head — ownerBadge (👤 name or Unassigned) + count + Balance + Limit + Util (color matched to `utilizationColor`). Computed from sub-group accounts. Decomposed from F-1211 | ✅ |
| F-1288 | Card Usage chip — issuer color stripe | `_issuerColor` (line 16714) — Chase/BoA/Wells/Citi/CapOne/Discover/Amex case-insensitive substring match | Collapsed card chip + tile head | — | First substring hit wins; "slate" fallback for unknown | issuerColor computed property on CreditCardChip — 7 case-insensitive matches (chase/bank of america/wells/citi/capital one/discover/amex). Rectangle stripe leading the chip. Decomposed from F-1211 | ✅ |
| F-1289 | Card Usage chip — 2-color util bar + util% label | `_renderCreditCardStrip` (line 17172-17190) — red used / green available track, util% text right-aligned | Collapsed card chip | — | Empty track grey when util_pct null; "—" label when null | UtilBar GeometryReader Capsule fill (utilColor for used portion, surface2 track) + monospaced "%" label colored by utilColor. Decomposed from F-1211 | ✅ |
| F-1290 | Card Usage tile — MTD spend + txn count + relative-time updated | Tile footer "MTD $X · N txns" + "Updated <relative>" (line 17362-17371) | Expanded credit-card tile foot | — | MTD shown in warning color when negative (net refund) | CreditCardTile.footer HStack — Text("MTD \(formatMoneyCents(spend)) · \(txnCount) txns") tinted DesignTokens.warning when spend<0, trailing relative-time updated text. Decomposed from F-1211 | ✅ |
| F-1291 | Card Usage panel — donut empty state per-scope | `_renderCardUsagePie` (line 18078) — "No spend this month" or "No spend on <card>" | Spend by Category panel body | — | Triggered when totalCents <= 0 or no slices | PieDonutView only renders when slices.total > 0; otherwise pieSubpanel shows Text "No spend \(scope == 'all' ? this month : on the selected card)." Decomposed from F-1205 | ✅ |
| F-1292 | Card Usage empty state — "Link via Plaid below" CTA | `renderCardsOverview` (line 16588) — link text triggers Connect Bank | Card Usage card | — | Click on link activates `#accounts-connect-btn` | CardUsageSection.emptyState — Text + Button(.link) labeled "Link via Plaid below." → triggers `state.beginPlaidLink()` directly (matches web behavior of clicking through to Connect btn). Disabled when `!state.plaidConfigured`. Decomposed from F-1211 | ✅ |
| F-1293 | Staged row — refund warning color on amount | `formatPlaidStagedTitle` + amount style (line 16176-16188) — "-$X" prefix + warning color | Per-staged-tx row | — | Negative `amount` → minus prefix + DesignTokens.warning text | StagedRowView amount Text — `isRefund ? "-$X" : "$X"` + `.foregroundStyle(isRefund ? .warning : .label)`. Decomposed from F-1252 | ✅ |
| F-1294 | Staged row — type / category / institution meta line | Meta line "<date> · <type> · <category>{instLabel}{pendingLabel}" (line 16190-16191) | Per-staged-tx row | — | type from `suggested_receipt_type` (label formatted), category from `plaid_category_primary` (underscores→spaces), inst label suffix when present | StagedRowView meta HStack — date, suggestedReceiptType (capitalized), plaidCategoryPrimary (underscores→spaces, capitalized), institutionName suffix. Decomposed from F-1252 | ✅ |
| F-1295 | Staged row — pending chip | `pendingLabel` (line 16182) — yellow "pending" text when `row.pending` | Per-staged-tx row meta | — | Only rendered when `pending: true` | Badge(text:"pending", style:.warning) appended to meta HStack when `row.pending == true`. Decomposed from F-1252 | ✅ |
| F-1296 | Link picker — staged summary line | `stagedLine` in modal header (line 16243) | Link picker modal | — | "<merchant> · <amount> · <date>" | LinkPickerSheet.stagedSummary Text — "Staged: <merchant> · <amount> · <date>" foregroundStyle secondaryLabel. Decomposed from F-1256 | ✅ |
| F-1297 | Link picker — candidate row layout | Per-candidate `<li>` (line 16252-16259) — store + total + date + match-status chip + amountDelta + dateDelta + Link button | Link picker modal | — | merchant_match green / no-match grey; Δ$ and Nd off appended conditionally | LinkPickerSheet.candidateRow — store (bold), date, merchant-match Text in success or secondary, "Δ$X" + "Nd off" caption, trailing amount monospaced + Link PrimaryButton. Decomposed from F-1256 | ✅ |
| F-1298 | Link picker — empty candidates state | "No nearby receipts found. You can still link by Purchase ID below." (line 16261) | Link picker modal | — | Shown when candidates empty | Text caption shown when `candidates.isEmpty && !isLoading`. Decomposed from F-1256 | ✅ |
| F-1299 | Link picker — manual Purchase ID input + Link by ID | Manual ID input + button at modal footer (line 16269-16275) | Link picker modal | POST `/plaid/staged-transactions/<id>/link-receipt` `{purchase_id}` | Free-text Int input; validates > 0 before posting | LinkPickerSheet manual section — TextField "e.g. 1234" + "Link by ID" GhostButton; Int(trimmed) validation + error toast on invalid; calls `linkStagedToReceipt(row, purchaseId:)` then dismisses. Decomposed from F-1257 | ✅ |
| F-1300 | Attach upload — accepted file types | File input `accept=".jpg,.jpeg,.png,.webp,.heic,.pdf"` (line 16333) | Attach picker | — | jpg/jpeg/png/webp/heic/pdf accepted | SwiftUI `.fileImporter(allowedContentTypes: [.image, .pdf])`. MIME type derived from path extension in `AccountsState.mimeType(for:)` covers jpg/jpeg/png/webp/heic/pdf. Decomposed from F-1258 | ✅ |
| F-1301 | Pagination — "Page N · rows X–Y of Z" caption | Span in `#accounts-tx-pagination` (line 18479-18482) | Pagination bar | — | Hidden when total == 0; row range clamped to total | paginationBar leading caption: "Page \(page) · rows \(start)–\(end) of \(total)" (caption2, secondaryLabel) — hidden when total == 0. Decomposed from F-1266 | ✅ |
| F-1302 | Confirm All — pre-confirm dialog copy | `confirm("Confirm every pending transaction? This turns them all into receipts at once.")` (line 16373) | Pending Review Confirm All | — | Browser confirm dialog before bulk POST | confirmationDialog with title "Confirm every pending transaction? This turns them all into receipts at once."; destructive "Confirm all" + Cancel. Decomposed from F-1251 | ✅ |
| F-1303 | Spending Trends — per-bar value label above bar | SVG `<text>` at top of each bar in `renderSpendingTrendsChart` (line 18687-18691) | Spending Trends bar chart | — | "$X" label drawn above each bar; muted color | SpendingTrendsChart Canvas uses `ctx.resolve(Text(...).foregroundColor(secondaryLabel))` + `measure` to center label above bar. Decomposed from F-1273 | ✅ |
| F-1304 | Spending Trends — MM x-axis label below baseline | SVG `<text>` at chart baseline with `month.slice(5)` (line 18688) | Spending Trends bar chart | — | Two-digit month "01..12" centered below bar | Canvas draws `ctx.resolve(Text(mm).foregroundColor(tertiaryLabel))` below baseline. Decomposed from F-1273 | ✅ |
| F-1305 | Spending Trends — baseline axis line | SVG `<line>` at `y = chartH + 8` (line 18698) | Spending Trends bar chart | — | Border-colored horizontal line under bars | Canvas Path stroke from (0, baseline+4) → (width, baseline+4) with DesignTokens.border. Decomposed from F-1273 | ✅ |
| F-1306 | Spend by Person — month nav pill chrome | `.spend-by-person-monthnav` container styling — pill with chevrons + label | Spend by Person card header | — | Capsule background around the ‹ label › trio | monthNav HStack with surface2 Capsule background, chevron Buttons (.plain) on either side of monospacedDigit label. Decomposed from F-1234..F-1236 | ✅ |
| F-1230 | Activity by Account card | Card `#accounts-breakdown-card` (hidden until data) | — | GET `/plaid/transaction-breakdown` | `loadAccountsBreakdown()`; shown when accounts present; collapse toggle | ActivityBreakdownSection — Card rendered only when `state.breakdown.isEmpty == false`; header Button toggles `state.activityCollapsed` (UserDefaults `LocalOCR.accounts.activity.collapsed`); body iterates `state.breakdown` from GET /plaid/transaction-breakdown (month filter inherited from Transactions toolbar) | ✅ |
| F-1231 | Activity breakdown — per-account row | `.accounts-breakdown-row` div with `onclick="pickAccountsBreakdownRow(id)"` | Activity by Account card | — | Shows: account name/mask, 💳 purchases, ⚡ autopay, 💰 interest, ↩ refunds, total | BreakdownRow — label = nickname ?? name, ····mask appended; 4 cells "💳 N · ⚡ N · 💰 N · ↩ N" dimmed when zero, trailing `= total`. Each cell has tooltip; whole row is Button (.plain) | ✅ |
| F-1232 | Activity breakdown — filter-by-account via row click | Row click toggles `accounts-tx-account-filter`; calls `resetAccountsTxOffsetAndReload()` | Activity by Account card | — | Active row gets `is-active` class; second click clears filter | Row Button → `AccountsState.pickBreakdownRow(plaidAccountId)` — toggles `state.txAccountFilter` (sets to id OR clears if already that id) then `resetTransactionsOffsetAndReload()` (parallel reload of transactions + staged + breakdown). Active row gets accentDim background + accent border | ✅ |
| F-1233 | Spend by Person card | Card `#dash-spend-by-person-card` (hidden until authorised) | — | GET `/analytics/spend-by-person?month=<YM>` | Only shown for admin or users with explicit `spend-by-person` grant; collapse toggle | SpendByPersonSection — renders only when `!state.spendByPersonForbidden` (auto-flipped by AccountsState when backend returns 403); collapse Button toggles `state.spendByPersonCollapsed` (UserDefaults `LocalOCR.accounts.spendByPerson.collapsed`) | ✅ |
| F-1234 | Spend by Person — month nav previous | Button `‹` `onclick="shiftSpendByPersonMonth(-1)"` | Spend by Person card | GET `/analytics/spend-by-person?month=<YM>` | Steps selected month back by 1 | "chevron.left" Button → `AccountsState.stepSpendByPersonMonth(-1)` — decrements YYYY-MM, handles year wrap, refires loadSpendByPerson | ✅ |
| F-1235 | Spend by Person — month label | Span `#dash-spend-by-person-month` | Spend by Person card | — | "Jan" (current month) or "Jan 2025" (other) | Text with monthLabel(ym) — DateFormatter "MMM" when matches current YM, "MMM yyyy" otherwise; monospacedDigit so chevrons don't jump | ✅ |
| F-1236 | Spend by Person — month nav next | Button `›` `onclick="shiftSpendByPersonMonth(1)"` | Spend by Person card | GET `/analytics/spend-by-person?month=<YM>` | Steps selected month forward by 1 | "chevron.right" Button → `AccountsState.stepSpendByPersonMonth(1)` | ✅ |
| F-1237 | Spend by Person — body | Div `#dash-spend-by-person` | Spend by Person card | GET `/analytics/spend-by-person?month=<YM>` | Renders per-person rows + household + untagged; empty state "Tag receipts to see per-person spend." | SpendByPersonSection.contents VStack — ProgressView loading state, error state, empty-state "Tag receipts to see per-person spend.", or VStack of moneyRow entries | ✅ |
| F-1238 | Spend by Person — per-person row | Row with "👤 Name" label + formatted money | Spend by Person card | — | From `data.per_person[]` | ForEach(people) → moneyRow(label: "👤 \(name)\(isSelf ? ' (me)' : '')", amount:) | ✅ |
| F-1239 | Spend by Person — household shared row | Row "🏠 Household (shared)" | Spend by Person card | — | Only shown when `data.household_total !== 0` | moneyRow "🏠 Household (shared)" — rendered only when `householdTotal != 0` | ✅ |
| F-1240 | Spend by Person — untagged row | Row "— Untagged" | Spend by Person card | — | Only shown when `data.unset_total !== 0` | moneyRow "— Untagged" with dim=true — rendered only when `unsetTotal != 0` | ✅ |
| F-1241 | Spend by Person — empty state | "No spend this month — or nothing tagged yet." | Spend by Person card | — | Shown when no rows | Text "Tag receipts to see per-person spend." when people empty AND household == 0 AND unset == 0 (web's literal copy is the placeholder div text, same intent) | ✅ |
| F-1242 | Spend by Person — error state | "Could not load per-person spend." | Spend by Person card | — | Shown when API returns non-OK | Text `state.spendByPersonError` in DesignTokens.error when set by APIError path; 403 routed to `spendByPersonForbidden` flag instead (hides whole card per web behavior) | ✅ |
| F-1243 | Transactions card — collapse/expand toggle | Card title `onclick="toggleAccountsSection('accounts-transactions-card')"` | — | — | `toggleAccountsSection()` | TransactionsSection.header Button toggles `state.txCollapsed` (UserDefaults `LocalOCR.accounts.tx.collapsed`) | ✅ |
| F-1244 | Transactions tab — All Spending | Tab button `data-accounts-tab="spending"` `onclick="setAccountsTransactionsTab('spending')"` | Transactions card | GET `/plaid/transactions?kind=spending` | Default tab; resets offset and reloads | Segmented Picker tag `.spending` → `AccountsState.setTxTab(.spending)` (resets offset + triple-reload tx/staged/breakdown) | ✅ |
| F-1245 | Transactions tab — Transfers & Bills | Tab button `data-accounts-tab="transfers"` | Transactions card | GET `/plaid/transactions?kind=transfers` | TRANSFER_CATEGORIES set drives server-side filter | Segmented Picker tag `.transfers` → `AccountsState.setTxTab(.transfers)` — server-side filter via `kind=transfers` query param | ✅ |
| F-1246 | Transactions — account filter select | Select `#accounts-tx-account-filter` `onchange="resetAccountsTxOffsetAndReload()"` | Transactions card | GET `/plaid/transactions?account_id=<id>` | "All accounts" default; options populated on load | Picker bound to `state.txAccountFilter` (empty == all). Options derived from `state.breakdown` (per-account rows) since they carry nickname + name + mask in one place. Selecting row → `setTxAccountFilter` resets offset and triple-reloads | ✅ |
| F-1247 | Transactions — month filter | Input `#accounts-tx-month` type=month `onchange="resetAccountsTxOffsetAndReload()"` | Transactions card | GET `/plaid/transactions?start=<date>&end=<date>` | Converts YYYY-MM to first/last day of month | MonthFilterField — TextField "YYYY-MM" with auto-commit on valid format or empty; calls `setTxMonth` which converts to `start=YYYY-MM-01` + `end=YYYY-MM-DD` (last day computed via Calendar). Native `.month` `type` input not used (SwiftUI lacks a built-in equivalent; text input + validation is the closest match) | 🔄 |
| F-1248 | Transactions — refresh button | Button `.accounts-toolbar__icon` 🔄 `onclick="loadPlaidTransactionsList()"` | Transactions card | GET `/plaid/transactions` | Force-reload | "arrow.clockwise" icon Button → `AccountsState.loadTransactions` | ✅ |
| F-1249 | Pending Review queue wrapper | Div `#accounts-review-wrap` (hidden when empty) | Transactions card | GET `/plaid/staged-transactions` | Warning-tinted section shown when staged transactions exist | PendingReviewQueue — renders only when `state.stagedRows.isEmpty == false` OR `stagedCounts.duplicateFlagged > 0`; warning-tinted card (DesignTokens.warningDim background) | ✅ |
| F-1250 | Pending Review — summary label | Span `#accounts-review-summary` | Pending Review | — | "N staged · $M total" or similar summary text | "N awaiting confirm" caption next to title when rows present; "N possible duplicate(s) flagged" when ready-queue empty but duplicates lurk | ✅ |
| F-1251 | Pending Review — Confirm All button | Button `#accounts-review-confirm-all` `onclick="confirmAllPlaidStaged()"` | Pending Review | POST `/plaid/staged-transactions/bulk-confirm` | Confirms all visible staged transactions at once | "Confirm all" PrimaryButton → confirmationDialog "Confirm every pending transaction? This turns them all into receipts at once." → `AccountsState.bulkConfirmStaged` (body `{all_ready: true}`). Toast shows "Confirmed N · skipped M" with severity warning when skipped > 0 | ✅ |
| F-1252 | Pending Review — review body | Div `#accounts-review-body` | Pending Review | GET `/plaid/staged-transactions` | Rendered by `loadAccountsReviewQueue()`; per-transaction rows | ForEach(state.stagedRows) → StagedRowView | ✅ |
| F-1253 | Per-staged-tx — Confirm button | Per-row button `onclick="confirmStagedTransaction(id)"` | Pending Review | POST `/plaid/staged-transactions/<id>/confirm` | Creates a purchase receipt from the staged record | "Confirm" PrimaryButton → `AccountsState.confirmStaged(row)`. Toast distinguishes "Linked to existing receipt" (matchedExisting=true) vs "Confirmed → receipt created" | ✅ |
| F-1254 | Per-staged-tx — Dismiss button | Per-row button `onclick="dismissStagedTransaction(id)"` | Pending Review | POST `/plaid/staged-transactions/<id>/dismiss` | Drops staged record without creating a receipt | "Dismiss" GhostButton → `AccountsState.dismissStaged` (optimistic remove + POST) | ✅ |
| F-1255 | Per-staged-tx — Flag Duplicate button | Per-row button `onclick="flagStagedDuplicate(id)"` | Pending Review | POST `/plaid/staged-transactions/<id>/flag-duplicate` | Marks as duplicate; suppresses from future import | "Duplicate" GhostButton → `AccountsState.flagDuplicateStaged(row)`. Web's optional `duplicate_purchase_id` body field plumbed through state but UI doesn't prompt for it (web doesn't either — just flags as suspected duplicate) | ✅ |
| F-1256 | Per-staged-tx — Match Candidates panel | Per-row expand `onclick="loadStagedMatchCandidates(id)"` | Pending Review | GET `/plaid/staged-transactions/<id>/match-candidates` | Shows existing receipts that could be linked to this Plaid transaction | "Link…" GhostButton opens `LinkPickerSheet` (modal) — calls `loadMatchCandidates(for:)` on appear; candidates rendered as rows with merchant-match Badge (success/secondary), Δ$ delta, Nd off, store, total, date | ✅ |
| F-1257 | Per-staged-tx — Link to Existing Receipt | Button in match-candidates panel | Pending Review | POST `/plaid/staged-transactions/<id>/link-receipt` | Associates Plaid transaction with a pre-existing receipt | Per-candidate "Link" PrimaryButton → `linkStagedToReceipt(row, purchaseId:)`. Also a manual TextField at the bottom of the sheet: "Or paste a Purchase ID:" + "Link by ID" GhostButton with Int validation | ✅ |
| F-1258 | Per-staged-tx — Attach Upload | Button triggering file pick | Pending Review | POST `/plaid/staged-transactions/<id>/attach-upload` | Uploads a receipt image and links it to this staged transaction | "Attach…" GhostButton → SwiftUI `.fileImporter` (allowedContentTypes: .image, .pdf) — supports jpg/png/webp/heic/pdf via mimeType helper. Multipart upload via `APIClient.multipartRequest` field `image=<file>`. Toast picks "linked" vs "saved manually" copy based on `purchase_id` presence in response | ✅ |
| F-1259 | Transactions — summary line | Div `#accounts-tx-summary` | Transactions card | GET `/plaid/transactions` | "Showing N of M transactions for this tab." | summaryLine Text — "Showing K of N transactions for this tab." OR "N transactions in this tab — nothing matches the current filter." | ✅ |
| F-1260 | Transactions — body | Div `#accounts-tx-body` | Transactions card | GET `/plaid/transactions` | Loading state; rendered by `renderAccountsTransactionRows()`; empty state per tab | txBody VStack — loading state, error state, empty state per-tab, or VStack of TransactionRowView rows | ✅ |
| F-1261 | Transactions — loading/error state | `<div class="empty-state"><p>Loading transactions…</p></div>` or "Could not load" | Transactions card | — | Loading default; error on non-OK response | "Loading transactions…" ProgressView when `isLoadingTransactions && rows empty`; Text `state.txError` in DesignTokens.error when set | ✅ |
| F-1262 | Transactions — empty state (spending tab) | "No spending transactions found for this filter." | Transactions card | — | Shown when filtered result is empty | "No spending transactions found for this filter." Text shown when rows empty AND txTab == .spending | ✅ |
| F-1263 | Transactions — empty state (transfers tab) | "No transfers or bill payments found for this filter." | Transactions card | — | Shown when filtered result is empty | "No transfers or bill payments found for this filter." Text shown when rows empty AND txTab == .transfers | ✅ |
| F-1264 | Transaction row — merchant + amount + date + category | `.summary-row` with title, value, meta | Transactions body | — | Amount in warning colour for refunds; formatted date + category (underscores → spaces) | TransactionRowView — merchant (semibold) + amount (monospaced, warning color when isRefund); meta row: formatted date · category (underscores→spaces, capitalized) · account label "<name> ····<mask>" if matched to known PlaidAccount | ✅ |
| F-1265 | Transaction row — "Open in Receipts" button | `btn-ghost btn-sm` per row | Transactions body | — | `onclick="jumpToReceipt(t.purchase_id)"`; only shown when `t.purchase_id` exists | "Open in Receipts" GhostButton (always shown since rows always carry a purchaseId) → `NotificationCenter.post(.jumpToReceipt, userInfo:["purchaseId":pid])`. Receipts tab listener wiring deferred to Receipts polish phase | 🔄 |
| F-1266 | Transactions — pagination bar | Div `#accounts-tx-pagination` | Transactions card | — | Prev / Next buttons; page N · rows X–Y of Z label; buttons disabled at boundaries | paginationBar HStack — "Page K · rows X–Y of Z" caption + Prev/Next GhostButtons at trailing edge | ✅ |
| F-1267 | Transactions — Prev page button | Button `← Prev` `onclick="changeAccountsTxPage(-1)"` | Pagination bar | GET `/plaid/transactions?offset=<n>` | Disabled when `accountsTxOffset === 0` | "Prev" Button → `AccountsState.prevTxPage()` (offset -= limit); `.disabled(txOffset == 0)` | ✅ |
| F-1268 | Transactions — Next page button | Button `Next →` `onclick="changeAccountsTxPage(1)"` | Pagination bar | GET `/plaid/transactions?offset=<n>` | Disabled when `offset + limit >= total` | "Next" Button → `AccountsState.nextTxPage()` (offset += limit); `.disabled(txOffset + limit >= txTotal)` | ✅ |
| F-1269 | Spending Trends card — collapse/expand toggle | Card title `onclick="toggleAccountsSection('accounts-trends-card')"` | — | — | `toggleAccountsSection()` | SpendingTrendsSection.header Button toggles `state.trendsCollapsed` (UserDefaults `LocalOCR.accounts.trends.collapsed`) | ✅ |
| F-1270 | Spending Trends — window select | Select `#accounts-trends-window` `onchange="loadPlaidSpendingTrends()"` | Spending Trends card | GET `/plaid/spending-trends?months=<n>` | Options: Last 3 / 6 / 12 months (12 default) | Picker bound to `state.trendsMonths` (Int 3/6/12) → `setTrendsMonths(clamped 1..24)` refires loadSpendingTrends; default 12 persisted to UserDefaults `LocalOCR.accounts.trends.months` | ✅ |
| F-1271 | Spending Trends — refresh button | Button `.accounts-toolbar__icon` 🔄 `onclick="loadPlaidSpendingTrends()"` | Spending Trends card | GET `/plaid/spending-trends?months=<n>` | Force-reload | "arrow.clockwise" icon Button → `AccountsState.loadSpendingTrends` | ✅ |
| F-1272 | Spending Trends — summary line | Div `#accounts-trends-summary` | Spending Trends card | — | "N months · $M total across K categories" | summaryLine Text — "K months · $X total across N categories" computed from series aggregations | ✅ |
| F-1273 | Spending Trends — SVG bar chart (by month) | `<svg>` in `#accounts-trends-body` rendered by `renderSpendingTrendsChart()` | Spending Trends card | GET `/plaid/spending-trends?months=<n>` | Horizontal scrollable; each bar = one month total; bar values labelled above | SpendingTrendsChart — horizontal ScrollView wrapping Canvas-drawn bars (barW 36, gap 14, height 220). Per-bar: rounded Path (accent color), value label "$X" above using ctx.resolve + measure, MM x-axis label below baseline (drawn via ctx.resolve + center alignment) | ✅ |
| F-1274 | Spending Trends — category breakdown table | `<table>` below bar chart with Category / Amount / % columns | Spending Trends card | — | Top 12 categories; "…and N more" note when > 12 | categoryTable — Grid layout with Category / Total / Share columns; top 12 by total desc; "…and N more categories." caption when cats.count > 12 | ✅ |
| F-1275 | Spending Trends — loading state | `<div class="empty-state"><p>Loading spending trends…</p></div>` | Spending Trends card | — | Default content before fetch | HStack with ProgressView + "Loading spending trends…" when `isLoadingTrends && trends == nil` | ✅ |
| F-1276 | Spending Trends — empty state | "No Plaid-imported purchases in this window yet. Confirm some transactions and check back." | Spending Trends card | — | Shown when `series.length === 0` | Text caption shown when `trends != nil && trends.series.isEmpty` | ✅ |
| F-1277 | Spending Trends — error state | "Could not load spending trends." | Spending Trends card | — | Shown on non-OK response or network error | Text `state.trendsError` in DesignTokens.error when set | ✅ |
| F-1278 | Plaid Link flow — link-token creation | — (JS) | — | POST `/plaid/link-token` | Called before opening Plaid Link modal; token passed to `Plaid.create()` | `AccountsState.beginPlaidLink(itemId:)` posts to /plaid/link-token, stores returned `link_token` in `pendingLinkToken` → AccountsView sheet binding presents PlaidLinkSheet | ✅ |
| F-1279 | Plaid Link flow — public-token exchange | — (JS Plaid callback) | — | POST `/plaid/exchange-public-token` | Called in `onSuccess` callback; creates/updates PlaidItem in DB | PlaidLinkWebView Coordinator handles `plaidSuccess` JS message → `AccountsState.completePlaidLink(publicToken:metadata:)` posts to /plaid/exchange-public-token with metadata + reloads connections | ✅ |

#### Screen: Analytics (Spending by Category)
Source: `src/frontend/index.html` lines 4633–4702
Backend: `calculate_spending_analytics.py` (url_prefix `/analytics`)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1300 | Page header — "Analytics" title + subtitle | H1 + subtitle paragraph | — | — | Subtitle "Understand spending patterns by module." | SpendingByCategoryView.pageHeader Text(appTitle1) "Analytics" + subtitle "Understand spending patterns by module." | ✅ |
| F-1301 | Spending Overview card | `.card` wrapping `#analytics-body` | — | GET `/analytics/spending?period=<p>&domain=<d>` | Loaded on page enter via `loadAnalytics()` | spendingOverviewCard Card wrapping toolbar + refund summary + analytics body; `AnalyticsState.loadSpending` fires on .task and on every Picker change | ✅ |
| F-1302 | Period filter — Monthly | Select `#analytics-period` option "monthly" (default) | Spending Overview card | GET `/analytics/spending?period=monthly&domain=<d>` | `onchange="loadAnalytics()"` | Period Picker tag `.monthly` — default; didSet on `state.period` persists to UserDefaults + reloadSpending | ✅ |
| F-1303 | Period filter — Weekly | Select `#analytics-period` option "weekly" | Spending Overview card | GET `/analytics/spending?period=weekly&domain=<d>` | `onchange="loadAnalytics()"` | Period Picker tag `.weekly` → server returns "YYYY-W##" keys; prettyPeriod renders "YYYY · week ##" | ✅ |
| F-1304 | Domain filter — Grocery | Select `#analytics-domain` option "grocery" (default) | Spending Overview card | GET `/analytics/spending?period=<p>&domain=grocery` | `onchange="loadAnalytics()"` | Domain Picker tag `.grocery` — default; sends `domain=grocery` query | ✅ |
| F-1305 | Domain filter — Restaurant | Select `#analytics-domain` option "restaurant" | Spending Overview card | GET `/analytics/spending?period=<p>&domain=restaurant` | `onchange="loadAnalytics()"` | Domain Picker tag `.restaurant` — sends `domain=restaurant`; deals body switched to domain notice | ✅ |
| F-1306 | Domain filter — General Expense | Select `#analytics-domain` option "general_expense" | Spending Overview card | GET `/analytics/spending?period=<p>&domain=general_expense` | `onchange="loadAnalytics()"` | Domain Picker tag `.generalExpense` (raw="general_expense") | ✅ |
| F-1307 | Domain filter — All Modules | Select `#analytics-domain` option "" (empty value) | Spending Overview card | GET `/analytics/spending?period=<p>` (no domain param) | `onchange="loadAnalytics()"` | Domain Picker tag `.all` (raw=""); `queryValue` is nil so endpoint omits `domain` param | ✅ |
| F-1308 | Sort — Newest Period | Select `#analytics-sort` option "period_desc" (default) | Spending Overview card | — | `onchange="setAnalyticsSort(this.value)"`; client-side sort of rendered rows | Sort Picker tag `.periodDesc` — default; `AnalyticsState.sortedRows` sorts by `id` desc | ✅ |
| F-1309 | Sort — Oldest Period | Select `#analytics-sort` option "period_asc" | Spending Overview card | — | `setAnalyticsSort('period_asc')` | Sort Picker tag `.periodAsc` — sorts by id asc | ✅ |
| F-1310 | Sort — Highest Total | Select `#analytics-sort` option "total_desc" | Spending Overview card | — | `setAnalyticsSort('total_desc')` | Sort Picker tag `.totalDesc` — sorts by `net` desc | ✅ |
| F-1311 | Sort — Most Items | Select `#analytics-sort` option "count_desc" | Spending Overview card | — | `setAnalyticsSort('count_desc')` | Sort Picker tag `.countDesc` — sorts by `receiptCount` desc | ✅ |
| F-1312 | Review Refunds button | Button `btn-ghost btn-sm` `onclick="openRefundReceipts()"` | Spending Overview card toolbar | GET `/receipts?...` (with refund filter) | Navigates to Receipts page with refund filter applied | "Review Refunds" GhostButton → posts `.openRefundReceipts` notification. Receipts-side listener wiring deferred to Receipts polish phase (same pattern as F-1265 jumpToReceipt) | 🔄 |
| F-1313 | Refund summary strip | Div `#analytics-refund-summary` | Spending Overview card | GET `/analytics/spending` | `renderAnalyticsRefundSummary()`; shown when refund count > 0 in any period | refundSummary HStack "↩ N refunds · $X returned" in DesignTokens.warning over warningDim background; only rendered when `state.totalRefundCount > 0` (sum of refundCount over all periods) | ✅ |
| F-1314 | Analytics body — desktop table view | `<table>` with Period / Net / Purchases / Refunds / Receipts columns | `#analytics-body` | GET `/analytics/spending` | Rendered when `!isCompactMobileReceiptView()` | periodTable — header HStack (PERIOD / NET / PURCHASES / REFUNDS / RECEIPTS) + Divider + ForEach periodRow; per-row counts only (purchaseTotal / refundTotal surfaced via .help() tooltips to match web simplicity) | ✅ |
| F-1315 | Analytics body — mobile list view | `<ul class="summary-row-list">` with period + spend meta chips | `#analytics-body` | GET `/analytics/spending` | Rendered when `isCompactMobileReceiptView()`; title + purchase/refund count + net amount | Mobile-list adaptation skipped — macOS app always renders the table form. Web's compact list is mobile-only fallback that doesn't apply on desktop | 🚫 |
| F-1316 | Analytics body — empty state | `<div class="empty-state">📊 No spending data yet. Upload a receipt to get started.</div>` | `#analytics-body` | — | Shown when `periods.length === 0` | analyticsBody empty branch — "📊" + "No spending data yet. Upload a receipt to get started." caption shown when `state.sortedRows.isEmpty` | ✅ |
| F-1317 | Analytics body — error state | Empty analytics-refund-summary cleared; body shows fallback | `#analytics-body` | — | Shown when `spendRes.ok` is false | analyticsBody error branch — `state.spendingError` Text in DesignTokens.error when APIError surfaces | ✅ |
| F-1318 | Deals Captured card | `.card` wrapping `#deals-body` | — | GET `/analytics/deals-captured?months=1` | Card always visible; deals only loaded for Grocery domain | dealsCapturedCard Card always rendered; `AnalyticsState.loadDeals` fires on `.task` and on domain change (refresh button reloads both) | ✅ |
| F-1319 | Deals Captured — body | Div `#deals-body` | Deals Captured card | GET `/analytics/deals-captured?months=1` | Rendered when domain = "grocery" or "all modules"; empty state: "No deals tracked yet." | Body shows "$X saved" headline (success color, appTitle2) + "in the last month from N deals" subline + collapsible DisclosureGroup "Item breakdown" listing top 20 deal rows with paid / avg / saved. Empty state: "No deals tracked yet." | ✅ |
| F-1320 | Deals Captured — domain exclusion notice | Static text: "Restaurant/General expense spend is tracked separately from grocery deal detection." | `#deals-body` | — | Shown when domain = "restaurant" or "general_expense" | When `!state.showsDealsBody` (domain is restaurant or general_expense), body renders "Restaurant / general-expense spend is tracked separately from grocery deal detection." caption only | ✅ |
| F-1321 | Analytics `analyticsSort` state variable | JS `let analyticsSort = 'period_desc'` | — | — | Persists sort order across `loadAnalytics()` reloads; synced to select element | `AnalyticsState.sort` `@Published` with didSet persisting to UserDefaults `LocalOCR.analytics.sort`; survives app restarts (web only persists for the session) | ✅ |

---

### GROUP: Household
#### Screen: Budget
Source: `src/frontend/index.html` lines 4105–4219
Backend: `manage_household_budget.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1400 | Page header — "Budget" title + subtitle | H1 + subtitle paragraph | — | — | Subtitle "Track monthly budgets by category." | BudgetView.pageHeader Text(appTitle1) "Budget" + subtitle "Track monthly budgets by category." | ✅ |
| F-1401 | Action bar — Budget editor toggle | Div `budget-editor-header budget-action-bar` `onclick="toggleBudgetEditor()"` | Budget Editor card | — | Collapses/expands the editor form; `toggleBudgetEditor()` | editorHeader Button toggles `state.editorCollapsed` (UserDefaults `LocalOCR.budget.editor.collapsed`); chevron + "⚙ Budget" label | ✅ |
| F-1402 | Action bar — Budget ⚙ button | Button `btn-ghost budget-action-btn` `onclick="toggleBudgetEditor(true)"` | Budget Editor card | — | Force-opens editor panel | Web's dedicated "force-open" duplicate button merged into the F-1401 toggle; macOS UX is single chevron Button (no value in a second control with identical semantics) | 🔄 |
| F-1403 | Action bar — Manual Entry ✍️ button | Button `btn-ghost budget-action-btn` `onclick="openManualEntryModal()"` | Budget Editor card | — | Opens manual receipt entry modal | "Manual Entry" GhostButton — toasts "Manual entry deferred to v1.1 — use the web app for now." (full receipt-entry modal infra is large, reuses Upload Receipt fan-out — defer to v1.1) | 🔄 |
| F-1404 | Action bar — Log Cash 💸 button | Button `btn-primary budget-action-btn` `onclick="openCashTransactionModal()"` | Budget Editor card | — | Opens cash transaction modal | "Log Cash" PrimaryButton → `Router.shared.activeSheet = .cashTransaction` opens existing CashTransactionsView as a sheet | ✅ |
| F-1405 | Budget Editor — Month input | Input `#budget-month` type=month `onchange="loadBudgetStatus()"` | Budget Editor form | GET `/budget/status?month=<m>&budget_category=<c>` | Initialised to current month; triggers full status reload | TextField bound to `draftMonth` with YYYY-MM validation on submit → `state.setMonth(...)` refreshes summary + targets/history. macOS lacks SwiftUI `type="month"` native picker; text input + format validation is closest match | 🔄 |
| F-1406 | Budget Editor — Budget Category select | Select `#budget-category` `onchange="loadBudgetEditorDefaults()"` | Budget Editor form | GET `/budget/status?month=<m>&budget_category=<c>` | Options populated by `populateBudgetCategoryOptions()`; selecting pre-fills amount | Picker bound to `state.selectedCategory` with all 14 entries from `BudgetCategoryCatalog.all` (mirrors src/backend/budgeting_domains.py). Picker change didSet calls `seedAmountFromSelection` → pre-fills draftAmount from current target | ✅ |
| F-1407 | Budget Editor — Budget Amount ($) input | Input `#budget-amt` type=number min=0 step=10 placeholder="500" | Budget Editor form | — | Value pre-filled from `loadBudgetEditorDefaults()` | TextField bound to `state.draftAmount` placeholder "500"; parsedAmount validates Double ≥ 0 (canSave gate) | ✅ |
| F-1408 | Budget Editor — Save Budget button | Button `#budget-save-btn btn-primary` `onclick="setBudget()"` | Budget Editor form | POST `/budget/set-monthly` `{month, budget_category, budget_amount}` | Admin-only guard: toast "Only admins can update budgets" if non-admin; toast success on save | "Save Budget" PrimaryButton `.disabled(!canSave)` (canSave = isAdmin && parsedAmount != nil). Save → `AccountsState.saveBudget` (oh — actually `BudgetState.saveBudget(amount:)`) → POST /budget/set-monthly; toast on success or 403 from backend | ✅ |
| F-1409 | Budget Editor — explanation note | Div `.budget-editor-note` | Budget Editor form | — | Static "Receipt defaults and line-item overrides decide how spending rolls into each budget category." | Static Text caption in editorForm: "Receipt defaults and line-item overrides decide how spending rolls into each budget category." | ✅ |
| F-1410 | Budget Status card — "This Month" header | Span `.card-title` "This Month" | Budget Status card | GET `/budget/category-summary?month=<m>` | Loaded by `loadBudgetStatus()` on page enter | statusHeader Text "This Month" (appHeadline) | ✅ |
| F-1411 | Budget Status — total spent display | Span `#budget-total-spent` | Budget Status card header | GET `/budget/category-summary?month=<m>` | `formatMoney(totalSpent)` summed from all categories | Trailing Text in statusHeader formatted from `state.totalSpent` (sum of all categories' spent) | ✅ |
| F-1412 | Budget Status — refresh button | Button `restaurant-refresh-btn` 🔄 `onclick="loadBudgetStatus()"` | Budget Status card header | GET `/budget/category-summary?month=<m>` | Force-reload | "arrow.clockwise" icon Button → `BudgetState.loadSummary` | ✅ |
| F-1413 | Budget Status — Active Categories section | `<div>` "Active Categories" grid | Budget Status body | GET `/budget/category-summary?month=<m>` | Shown when any category has `budget_amount > 0` or `spent > 0` | "Active Categories" Text label + VStack of categoryRow for `state.activeCategories` (filter on `budgetAmount > 0 OR spent > 0`) | ✅ |
| F-1414 | Budget Status — Other Categories collapsible | `<details>` "Other Categories" with count | Budget Status body | — | Collapsed by default; shows categories with no budget and no spend | DisclosureGroup "Other Categories · N" with `state.inactiveCategories`. Collapsed by default. | ✅ |
| F-1415 | Budget Status — category row | `<details class="budget-cat-row">` per category | Budget Status body | — | `onclick="syncBudgetCategorySelection(category)"`; syncs editor category select | categoryRow Button — tap sets `state.selectedCategory = row.budgetCategory` (syncs editor + seeds amount), toggles `expandedCategory` to reveal contributions list. Selected row gets accentDim background + accent border | ✅ |
| F-1416 | Budget category row — progress bar | `.progress-bar` with class `ok`/`warn`/`danger` (70%/90% thresholds) | Category row | — | Width = `min(pct, 100)%` | ProgressBar GeometryReader Capsule — fill width = pctClamped/100; `BudgetSeverity` enum → success/warning/error tints matching web's ok/warn/danger thresholds (70% / 90%) | ✅ |
| F-1417 | Budget category row — spent + remaining meta | Span "N% · $X left" / "$X over" | Category row | — | Remaining < 0 → "over" label | "N% · $X left" or "$X over" Text via `row.remainingLabel` helper on BudgetCategoryStatus; foregroundStyle DesignTokens.error when `isOver` | ✅ |
| F-1418 | Budget category row — target vs no-target | "of $Y" or "No target" | Category row | — | Shown based on `target > 0` | `row.targetLabel` helper returns "of $X" when budgetAmount > 0 else "No target"; rendered in DesignTokens.tertiaryLabel | ✅ |
| F-1419 | Budget category row — contributing receipts | Compact receipt rows inside `<details>` expand | Category row | — | `renderCompactReceiptRows(contributions)`; empty state "No contributing receipts yet." | contributionsList VStack — per contribution row: store · date · amount (warning color when refund). Empty state: "No contributing receipts yet." | ✅ |
| F-1420 | Budget Status — empty state (no budget data) | `<div class="empty-state"><p>No budget set for this month.</p></div>` | Budget Status card | — | Shown when `/budget/category-summary` returns non-OK | Text "No budget set for this month." (caption, secondary) when `state.summary == nil && state.summaryError != nil` | ✅ |
| F-1421 | Current Budget Targets card | Card `#budget-targets-shell` with collapse toggle | — | GET `/budget/target-history?month=<m>` | `toggleBudgetSection('budget-targets-shell')`; collapsed by default; chevron ▶ | targetsCard Button toggles `state.targetsCollapsed` (UserDefaults default: true). Trailing "N set" count caption | ✅ |
| F-1422 | Current Budget Targets — targets body | Div `#budget-targets-body` | Budget Targets card | GET `/budget/target-history?month=<m>` | Rendered by `renderBudgetTargetRows(payload.current_targets)` | targetsBody VStack of rows: category label + amount + relative time. "No category targets set for this month." empty state | ✅ |
| F-1423 | Current Budget Targets — error state | "Could not load current budget targets." | Budget Targets card | — | Shown when `/budget/target-history` returns non-OK | Text "Could not load current budget targets." in DesignTokens.error when `historyError` is set AND targets empty; full error text exposed via .help() tooltip | ✅ |
| F-1424 | Budget Change History card | Card `#budget-history-shell` with collapse toggle | — | GET `/budget/target-history?month=<m>` | `toggleBudgetSection('budget-history-shell')`; collapsed by default | historyCard Button toggles `state.historyCollapsed` (UserDefaults default: true). Trailing "N change(s)" caption | ✅ |
| F-1425 | Budget Change History — history body | Div `#budget-history-body` | Budget History card | GET `/budget/target-history?month=<m>` | Rendered by `renderBudgetHistoryRows(payload.history)` | historyBody VStack — per change: category label + deltaLabel ("$prev ↑/↓ $new" or "Set to $X") + relative time | ✅ |
| F-1426 | Budget Change History — error state | "Could not load budget history." | Budget History card | — | Shown when `/budget/target-history` returns non-OK | Text "Could not load budget history." in DesignTokens.error when `historyError` is set AND history empty | ✅ |
| F-1427 | `updateBudgetEditorAccess()` admin guard | Disables `#budget-save-btn` for non-admins | Budget Editor | — | Called at start of `loadBudgetStatus()`; visual cue for read-only users | `canSave` = `appState.currentUser?.isAdmin == true && parsedAmount != nil`. Save Budget Button `.disabled(!canSave)` with .help() tooltip "Only admins can update budgets" for non-admin users | ✅ |

#### Screen: Contributions
Source: `src/frontend/index.html` lines 4703–4792
Backend: `manage_contributions.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1500 | Page header — "Contribution" title + subtitle | H1 + subtitle paragraph | — | — | Subtitle "See how scoring works, what you earned, and how you can help the system stay sharp." | ContributionsView.pageHeader Text(appTitle1) "Contribution" + subtitle exact copy | ✅ |
| F-1501 | How Low-Stock Validation Works explainer card | Static `.card` | — | — | 4-step numbered explanation of the pending→validated contribution lifecycle | explainerCard Card with "How Low-Stock Validation Works" heading + 4 step() rows (numbered Circle badge + step text) | ✅ |
| F-1502 | Explainer step 1 — Mark item Low | Static step text | Explainer card | — | "Mark an item Low. This starts a pending contribution, but it does not score yet." | step(1, "Mark an item Low. This starts a pending contribution, but it does not score yet.") | ✅ |
| F-1503 | Explainer step 2 — Move to Shopping List | Static step text | Explainer card | — | "Move that item into the Shopping List." | step(2, "Move that item into the Shopping List.") | ✅ |
| F-1504 | Explainer step 3 — Receipt validation | Static step text | Explainer card | — | "When a later receipt confirms the item was bought, the low-stock action is validated." | step(3, "When a later receipt confirms the item was bought, the low-stock action is validated.") | ✅ |
| F-1505 | Explainer step 4 — False-positive rule | Static step text | Explainer card | — | "If the low flag is quickly cleared or wrong, the pending contribution does not count." | step(4, "If the low flag is quickly cleared or wrong, the pending contribution does not count.") | ✅ |
| F-1506 | Summary stats grid | Div `#contrib-summary-cards` `.contrib-summary-grid` | — | GET `/contributions/summary` | Rendered by `loadContributions()`; 5 stat cards | summaryStatsGrid LazyVGrid (adaptive minimum 180px) with 5 Card stat cells | ✅ |
| F-1507 | Summary stat — Total Score | `.contrib-stat` "Total Score" | Summary stats grid | GET `/contributions/summary` | `summary.total_score`; "Your current household contribution score" | statCard "TOTAL SCORE" / `summary.totalScore` / "Your current household contribution score" subline | ✅ |
| F-1508 | Summary stat — Receipts | `.contrib-stat` "Receipts" | Summary stats grid | GET `/contributions/summary` | `summary.receipts_processed`; sub-text `receipt_points` | statCard "RECEIPTS" / `receiptsProcessed` / "+\(receiptPoints) pts" subline | ✅ |
| F-1509 | Summary stat — OCR Fixes | `.contrib-stat` "OCR Fixes" | Summary stats grid | GET `/contributions/summary` | `summary.ocr_fixes`; sub-text `ocr_points` | statCard "OCR FIXES" / `ocrFixes` / "+\(ocrPoints) pts" subline | ✅ |
| F-1510 | Summary stat — System Help (bonus) | `.contrib-stat` "System Help" | Summary stats grid | GET `/contributions/summary` | `summary.bonus_points`; low-stock, location, shopping upkeep | statCard "SYSTEM HELP" / "+\(bonusPoints)" / "low-stock · location · shopping upkeep" subline | ✅ |
| F-1511 | Summary stat — Floating points | `.contrib-stat` "Floating" | Summary stats grid | GET `/contributions/summary` | `summary.floating_points`; actions awaiting real-world follow-through | statCard "FLOATING" / "+\(floatingPoints)" / "awaiting real-world follow-through" subline | ✅ |
| F-1512 | Recent Score Activity card | `.card` with title "Recent Score Activity" and 🔄 button | — | GET `/contributions/summary` | Left card of lower contrib-grid | recentActivityCard Card with title + refresh icon Button + body | ✅ |
| F-1513 | Recent Score Activity — refresh button | `btn-ghost btn-sm` 🔄 `onclick="loadContributions()"` | Recent Score Activity card | GET `/contributions/summary` | Force-reload | arrow.clockwise icon Button → `ContributionsState.refresh` | ✅ |
| F-1514 | Recent Score Activity — event list | Div `#contrib-recent-list` `.contrib-list` | Recent Score Activity card | GET `/contributions/summary` | Renders `data.recent_events[]`; empty state "No contribution history yet." | ForEach over `state.summary.recentEvents` → eventRow. Empty state Text "No contribution history yet." | ✅ |
| F-1515 | Recent Score Activity — event row | `.contrib-list-item` per event | Recent Score Activity card | — | Description + "+N points" + event_type · status · datetime meta | eventRow VStack — description + trailing "+N pts" (success tinted when positive); meta HStack: eventType (capitalized, underscores→spaces) · ContributionStatusLabel.format(status) · formatted createdAtDate | ✅ |
| F-1516 | Recent Score Activity — error state | "Could not load contribution details." in `#contrib-recent-list` | Recent Score Activity card | — | Shown when `/contributions/summary` returns non-OK | Text "Could not load contribution details." in DesignTokens.error when `lastError != nil && summary == nil`; full APIError text on .help() tooltip | ✅ |
| F-1517 | Ways to Help Right Now card | `.card` with title "Ways To Help Right Now" | — | GET `/contributions/summary` | Right card of lower contrib-grid | opportunitiesCard Card; renders in same LazyVGrid row as recent activity (adaptive minimum 360 — collapses to single column on narrow window) | ✅ |
| F-1518 | Ways to Help — opportunities list | Div `#contrib-opportunities` `.contrib-list` | Ways to Help card | GET `/contributions/summary` | Renders `data.opportunities[]`; empty state "No suggested actions right now." | ForEach over `state.summary.opportunities` → opportunityRow. Empty state Text "No suggested actions right now." | ✅ |
| F-1519 | Opportunity row — title + count + description | `.contrib-opportunity` div | Ways to Help card | — | `item.title`; `item.count` available; `item.description` | opportunityRow VStack — title (semibold) + optional count capsule (accentDim) + description caption | ✅ |
| F-1520 | Opportunity row — CTA button | `btn-ghost btn-sm` `onclick="goToPage(item.page)"` | Ways to Help card | — | `item.cta` as button label; navigates to relevant page | CTA GhostButton → `navigate(to: page)` switches `Router.activeTab` to receipts/inventory/shopping/kitchen/products/dashboard; unknown pages (e.g. "settings") surface a toast since Settings tab opens in v1.1 | ✅ |
| F-1521 | How Points Are Earned card | `.card` with title "How Points Are Earned" | — | GET `/contributions/summary` | Upper card of second contrib-grid row | pointsRulesCard Card with title + rules ForEach | ✅ |
| F-1522 | Points rules list | Div `#contrib-rules` `.contrib-rule-list` | How Points Are Earned card | GET `/contributions/summary` | Renders `data.rules[]`; each: title + "+N points" + description | ruleRow VStack per rule — title + "+N pts" (success when > 0) + description caption | ✅ |
| F-1523 | Fair Scoring Rules card | `.card` with title "Fair Scoring Rules" | — | GET `/contributions/summary` | Paired with How Points card in second grid row | fairScoringCard Card; rendered in same LazyVGrid row as pointsRulesCard | ✅ |
| F-1524 | Scoring notes list | Div `#contrib-notes` `.contrib-note-list` | Fair Scoring Rules card | GET `/contributions/summary` | Renders `data.notes[]`; each note as `.contrib-note-item` | ForEach over `state.summary.notes` → bullet "•" + note caption | ✅ |
| F-1525 | `contributionStatusLabel()` helper | JS function mapping event status enum to human label | — | — | Used in event row meta rendering | `ContributionStatusLabel.format(_:)` enum-static helper — maps pending/validated/void/floating/finalized to human labels; capitalizes unknowns | ✅ |
| F-1526 | `contributionSummary` module-level cache | JS `let contributionSummary = null` | — | GET `/contributions/summary` | Populated by `loadContributions()`; read by other modules for leaderboard / nudge logic | `ContributionsState.shared.summary` `@Published` plays the equivalent role; observed by other views via @ObservedObject on the shared singleton (leaderboard / nudge integration follows when those screens land) | ✅ |

#### Screen: Medications
Source: `src/frontend/index.html` lines 2847–2867
Backend: `manage_medications.py`

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1600 | Page header — "Medicine Cabinet" title + subtitle | H1 + subtitle paragraph | — | — | Subtitle "Track household medications, expiry dates, and members" | `MedicationsView.header` — appTitle2 + subheadline copy verbatim. Added 2026-05-19. | ✅ |
| F-1601 | Add Medication button | Button `btn-primary` `onclick="openAddMedicationSheet()"` | — | POST `/medications` | Opens bottom-sheet form | Toolbar Button (`plus` icon) + ⌘N → `state.openAdd()` → `MedicationEditSheet` via `.sheet(item: $state.editing)`. Added 2026-05-19. | ✅ |
| F-1602 | Members button | Button `btn-ghost` `onclick="openMembersSheet()"` | — | GET `/household-members` | Opens Members bottom-sheet to add/delete non-user household members | Toolbar Button (`person.2`) → `state.membersSheetOpen = true` → `MembersSheet`. Backend has only GET; mac roster is read-only. Added 2026-05-19. | 🔄 |
| F-1603 | Status filter select | Select `#medicine-filter-status` `onchange="loadMedicineCabinet()"` | — | GET `/medications?status=<v>` | Options: active (default), all, expired, finished | Segmented Picker (Active/All/Expired/Finished) → `state.setStatusFilter(_:)` → reloads list. Defaults to `active`. Added 2026-05-19. | ✅ |
| F-1604 | Member filter chips row | Div `#medicine-member-chips` `.filter-chip-row` | — | GET `/medications?user_id=<id>` or `?member_id=<id>` | Chips for each user / household-member + "All" + "🏠 Household"; rendered by `_renderMedicineMemberChips()` | `MemberChipRow` — All + per-user chips from `/auth/household-members` + 🏠 Household. Tap → `state.setMemberFilter(_:)` → reload with `user_id=` or `member_id=none`. Backend only returns User rows (no separate HouseholdMember table). Added 2026-05-19. | 🔄 |
| F-1605 | Cabinet body grid | Div `#medicine-cabinet-body` auto-fill grid | — | GET `/medications` | Rendered by `_renderMedicineCabinet()`; empty state "No medications. Tap + Add Medication to get started." | `MedicationsListBody` single-column VStack of `MedicationRow` (matches web `inv-tile` flex-column). Loading + empty branches. Added 2026-05-19. | ✅ |
| F-1606 | Medication tile — name + strength | `.inv-tile-name` text | Cabinet grid | — | `med.name + " · " + med.strength` | `MedicationRow` Text — `med.name + (strength.map { " · \($0)" })`. Added 2026-05-19. | ✅ |
| F-1607 | Medication tile — age-group badge | Span `.inv-tile-days` | Cabinet grid | — | 👶 Kids / 🧑 Adult / 👪 All from `med.age_group` | `ageLabel` computed: child→👶 Kids, adult→🧑 Adult, default→👪 All. Rendered in head row. Added 2026-05-19. | ✅ |
| F-1608 | Medication tile — quantity + unit | Span `.inv-tile-qty` | Cabinet grid | — | `"×N unit"` — hidden when quantity is null | `qtyText` "×\(Int)\( unit)" (skips "count" unit suffix). Hidden when `med.quantity` nil. Added 2026-05-19. | ✅ |
| F-1609 | Medication tile — expiry date meta | Div in `.inv-tile-meta` "🍂 Exp: YYYY-MM-DD" | Cabinet grid | — | Shown only when `med.expiry_date` is set | Conditional Text "🍂 Exp: \(date)" colored error when isExpired. Added 2026-05-19. | ✅ |
| F-1610 | Medication tile — member label | Div in `.inv-tile-meta` | Cabinet grid | — | Shows assigned user/member name + emoji, or "🏠 Household" | `state.memberLabel(for:)` — matches `med.userId` to a `MedicineMember`, else returns "🏠 Household" when `belongsTo == "household"`. Added 2026-05-19. | ✅ |
| F-1611 | Medication tile — AI warning | Div in `.inv-tile-meta` "⚠️ …" | Cabinet grid | — | Shows first warning from `med.ai_warnings[]`; truncated with ellipsis | Conditional Text "⚠️ \(first)" in warning color, line-limit 1 + truncation tail. Added 2026-05-19. | ✅ |
| F-1612 | Medication tile — "Expired" badge | Span `badge-low` | Cabinet grid | — | Shown when `med.is_expired` or `med.status === "expired"`; tile gets `exp-expired` class | `statusChip("Expired", ...)` rendered when `isExpired`; tile also gets error-tinted border. Added 2026-05-19. | ✅ |
| F-1613 | Medication tile — "Low" badge | Span `badge-low` | Cabinet grid | — | Shown when `med.is_low`; tile gets `exp-soon` class | `statusChip("Low", warning, warningDim)` rendered when `isLow` (and not expired); tile gets warning-tinted border. Added 2026-05-19. | ✅ |
| F-1614 | Medication tile — photo (admin-only) | `<img class="inv-tile-img">` | Cabinet grid | GET `/medications/<id>/photo` | Only shown when `med.image_path` and current user is admin | Backend exposes ONLY `POST /medications/<id>/photo` (upload) — there is no GET route. The web `<img src="/medications/<id>/photo">` returns 405. Skipped on mac to avoid noisy 405 logs. Added 2026-05-19. | 🚫 |
| F-1615 | Medication tile — Edit button | `inv-btn-edit` "✎" | Cabinet grid | — | `onclick="openEditMedicationSheet(med)"` | iconButton (pencil) → `state.openEdit(_:)` → `MedicationEditSheet` pre-filled. Also context menu "Edit". Added 2026-05-19. | ✅ |
| F-1616 | Medication tile — Mark Done button | `inv-btn-used` "✓ Done" | Cabinet grid | PUT `/medications/<id>` `{status:"finished"}` | Only shown when `med.status === "active"` | iconButton (checkmark.circle.fill) rendered only when `med.status == "active"` → `state.markDone(_:)` → PUT body `{status:"finished"}` + reload. Added 2026-05-19. | ✅ |
| F-1617 | Medication tile — Delete button | `inv-btn-danger` "🗑" | Cabinet grid | DELETE `/medications/<id>` | Confirm dialog before delete | iconButton (trash, error tint) → `state.delete(_:)` → DELETE + toast + reload. Context menu "Delete" destructive role also wired. Added 2026-05-19. | ✅ |
| F-1618 | Add/Edit Medication bottom-sheet | `_openMedicationSheet()` overlay + sheet element | — | POST `/medications` or PUT `/medications/<id>` | Animated sheet from bottom; body scroll locked; form pre-filled in edit mode | `MedicationEditSheet` via `.sheet(item: $state.editing)` — sticky header + scroll body + sticky footer. `MedicationEditState` holds draft fields, pre-filled from `existing` Medication. Added 2026-05-19. | ✅ |
| F-1619 | Sheet — Name field | Input type=text, placeholder "e.g. Ibuprofen" | Add/Edit sheet | — | Required; toast error if blank | TextField "$draft.name" placeholder "e.g. Ibuprofen". Save button disabled until non-blank; `state.save()` re-checks + toast. Added 2026-05-19. | ✅ |
| F-1620 | Sheet — Active Ingredient field | Input type=text, placeholder "e.g. Ibuprofen" | Add/Edit sheet | — | Optional | TextField "$draft.activeIngredient". Added 2026-05-19. | ✅ |
| F-1621 | Sheet — Brand / Manufacturer field | Input type=text, placeholder "e.g. Advil" | Add/Edit sheet | — | Optional | TextField "$draft.brand". Added 2026-05-19. | ✅ |
| F-1622 | Sheet — Strength field | Input type=text, placeholder "e.g. 200mg" | Add/Edit sheet | — | Optional | TextField "$draft.strength". Added 2026-05-19. | ✅ |
| F-1623 | Sheet — Dosage Form select | Select with Tablet / Capsule / Liquid / Cream / Spray / Patch / Other | Add/Edit sheet | — | Default "tablet" | Picker bound to "$draft.dosageForm" with all 7 options from `MedicationOptions.dosageForms`. Defaults to "tablet". Added 2026-05-19. | ✅ |
| F-1624 | Sheet — Age Group (For) select | Select: Everyone / Adults only / Kids only | Add/Edit sheet | — | Default "both" | Picker bound to "$draft.ageGroup" with 👪 Everyone / 🧑 Adults only / 👶 Kids only. Defaults to "both". Added 2026-05-19. | ✅ |
| F-1625 | Sheet — Belongs To select | Select: Household + per-user + per-member options | Add/Edit sheet | — | Options built from `__medicinePeople`; default "household" | Picker "$draft.memberKey" — 🏠 Household (shared) + per-user chips. memberKey encodes "household"/"user_<id>"/"member_<id>"; `toBody()` splits into userId/memberId/belongsTo. Added 2026-05-19. | ✅ |
| F-1626 | Sheet — Quantity input + Unit select | Input type=number + select (Tablets/Capsules/ml/oz/Count/Doses) | Add/Edit sheet | — | Side-by-side row | HStack — TextField "$draft.quantityText" + Picker "$draft.unit" (6 unit options). Added 2026-05-19. | ✅ |
| F-1627 | Sheet — Expiry Date field | Input type=date | Add/Edit sheet | — | Optional | TextField "$draft.expiryDate" placeholder "YYYY-MM-DD". (SwiftUI `DatePicker` would be richer; v1.1 may swap.) Added 2026-05-19. | 🔄 |
| F-1628 | Sheet — Manufacture Date field | Input type=date | Add/Edit sheet | — | Optional | TextField "$draft.manufactureDate" placeholder "YYYY-MM-DD". Same v1.1 DatePicker note. Added 2026-05-19. | 🔄 |
| F-1629 | Sheet — Barcode field | Input type=text, placeholder "UPC/NDC" | Add/Edit sheet | POST `/medications/barcode-lookup` | Optional; populated by scan or manual entry | TextField "$draft.barcode" — value also fed into Lookup button (F-1633). Added 2026-05-19. | ✅ |
| F-1630 | Sheet — Notes field | Input type=text, placeholder "e.g. Take with food" | Add/Edit sheet | — | Optional | TextField "$draft.notes" placeholder "e.g. Take with food". Added 2026-05-19. | ✅ |
| F-1631 | Sheet — Camera barcode scan button | `<label>` for hidden `<input type=file capture=environment>` "📷 Camera" | Add/Edit sheet (add mode) | POST `/medications/barcode-lookup` `{barcode}` | Add mode only; uses Html5Qrcode.scanFile(); fills fields on match | "📷 Photo" Button opens NSOpenPanel for an image; mac has no in-process barcode decoder for v1.0 (would need Vision framework + barcode-detection plumbing). Toast directs user to manual entry + Lookup. Added 2026-05-19. | 🔄 |
| F-1632 | Sheet — Gallery barcode scan button | `<label>` for hidden `<input type=file>` "🖼 Gallery" | Add/Edit sheet (add mode) | POST `/medications/barcode-lookup` `{barcode}` | Add mode only; reads barcode from photo file | Same v1.1 deferral as F-1631. Added 2026-05-19. | 🔄 |
| F-1633 | Sheet — Name Lookup button | Button "🔍 Lookup" | Add/Edit sheet (add mode) | POST `/medications/barcode-lookup` `{name}` | Looks up by name; fills fields from drug database | "🔍 Lookup" GhostButton → `state.barcodeLookup(name:barcode:)` (sends whichever is non-empty; backend prefers barcode > name) → `draft.apply(lookup:)` fills fields on match. Added 2026-05-19. | ✅ |
| F-1634 | Sheet — Save / Cancel footer buttons | Buttons "Save" (primary) + "Cancel" (ghost) in sticky footer | Add/Edit sheet | POST or PUT `/medications` | Save disabled during request; toast on success; reloads cabinet | Sticky footer HStack — Cancel GhostButton + Add/Save PrimaryButton (uses ⌘↩ via `.defaultAction`). Save disabled while name blank; toast + reload + close on success. Added 2026-05-19. | ✅ |
| F-1635 | Members bottom-sheet | `openMembersSheet()` — list + add form | — | GET `/household-members`, POST `/household-members`, DELETE `/household-members/<id>` | Shows all `__medicineMembers`; add requires name; delete with confirm | `MembersSheet` shows read-only roster from `/auth/household-members`. Web's POST + DELETE routes don't exist server-side (verified by Rule 1 grep — only GET is registered). Added 2026-05-19. | 🚫 |
| F-1636 | Members sheet — member row | Row with avatar-emoji + name + age label + Delete button | Members sheet | DELETE `/household-members/<id>` | Empty state "No members yet." | Rows render emoji + name + role + "You" marker. No delete button (no backend route). Empty state with `person.2` icon. Added 2026-05-19. | 🚫 |
| F-1637 | Members sheet — Add Member form | Name input + Age select (Adult/Child) + Add button | Members sheet | POST `/household-members` `{name, age_group}` | Appends to `__medicineMembers`; re-renders chip row | Skipped — backend `/household-members` is GET-only. Sheet copy notes household users are managed via Settings. Added 2026-05-19. | 🚫 |
| F-1638 | `__medicineMembers` / `__medicineMemberFilter` module state | JS `let __medicineMembers = []` + `let __medicineMemberFilter = null` | — | — | Persists member list and active chip filter across renders | `MedicationsState.members: [MedicineMember]` + `memberFilter: String?` (nil = All). Persisted in-memory across navigation via @MainActor singleton. Added 2026-05-19. | ✅ |

#### Screen: Settings
Source: `src/frontend/index.html` lines 4793–5765
Backend: many (`manage_authentication.py`, `manage_ai_models.py`, `configure_telegram_webhook.py`, etc.)

| ID | Feature | UI Element | Sub-tab / Modal | API Call | Business Rule | macOS Impl | Status |
|----|---------|-----------|-----------------|----------|---------------|------------|--------|
| F-1700 | Page header — "Settings" title + subtitle | H1 + subtitle "Configure your Grocery Manager" | — | — | Static | TBD | ❌ |
| F-1701 | Session card — avatar bubble | Div `#settings-avatar-preview` `.avatar-bubble` | Session card | — | Shows current user's avatar emoji; default 🦊 | TBD | ❌ |
| F-1702 | Session card — user name display | Div `#session-summary-name` | Session card | — | "Not signed in" until authenticated | TBD | ❌ |
| F-1703 | Session card — user sub-line (role/email) | Div `#session-summary-sub` | Session card | — | Muted secondary info | TBD | ❌ |
| F-1704 | Session card — Change Avatar button | Button `btn-ghost btn-sm` `onclick="toggleSessionAvatar()"` | Session card | — | Toggles `#session-avatar-wrap` visibility | TBD | ❌ |
| F-1705 | Session card — Details toggle button | Button `btn-ghost btn-sm` `onclick="toggleSessionDetails()"` | Session card | — | Shows/hides `#session-details-wrap`; label toggles "▼ Details" / "▲ Hide details" | TBD | ❌ |
| F-1706 | Session card — Sign Out button | Button `btn-ghost btn-sm` `onclick="logout()"` | Session card | POST `/auth/logout` | Ends session | TBD | ❌ |
| F-1707 | Avatar editor — Profile Avatar input | Input `#settings-avatar` maxlength=4 placeholder="😀" | Session card avatar editor | — | Shown when `#session-avatar-wrap` is visible | TBD | ❌ |
| F-1708 | Avatar editor — Save Avatar button | Button `btn-primary btn-sm` `onclick="saveMyAvatar()"` | Session card avatar editor | PATCH `/auth/users/<id>` `{avatar_emoji}` | Updates current user's avatar | TBD | ❌ |
| F-1709 | Avatar editor — Cancel button | Button `btn-ghost btn-sm` `onclick="toggleSessionAvatar()"` | Session card avatar editor | — | Hides avatar editor panel | TBD | ❌ |
| F-1710 | Session details — Current browser login | Input `#settings-user` disabled | Session details panel | — | Shows current username | TBD | ❌ |
| F-1711 | Session details — Auth Source | Input `#settings-auth-source` disabled | Session details panel | — | "Signed out" / "password" / "google" / etc. | TBD | ❌ |
| F-1712 | Session details — Trusted Device | Input `#settings-auth-device` disabled | Session details panel | — | Trusted device name or "—" | TBD | ❌ |
| F-1713 | Session details — Current Host | Input `#settings-current-host` disabled | Session details panel | — | Current window.location.host | TBD | ❌ |
| F-1714 | Session details — Default Pairing Host | Input `#settings-pairing-host` disabled | Session details panel | — | API base URL for pairing | TBD | ❌ |
| F-1715 | My Activity card | Card `#settings-my-activity-card` | — | — | Rendered by `renderMyActivityCard()`; uses `currentUser` data | TBD | ❌ |
| F-1716 | My Activity — last login line | Div "🕒 Last login: … ago" | My Activity card | — | `currentUser.last_login_at`; "first recorded session" if null | TBD | ❌ |
| F-1717 | My Activity — current session line | Div "💻 Signed in X ago · device description" | My Activity card | — | `currentUser.current_session_started_at` + `_describeUserAgent(ua)` | TBD | ❌ |
| F-1718 | My Activity — stats line | Div "✨ N receipts · M shopping items · $X tracked" | My Activity card | — | From `currentUserStats`; "No recent activity recorded yet." when all zero | TBD | ❌ |
| F-1719 | My Activity — sign-in empty state | `<div class="empty-state"><p>Sign in to see your activity.</p></div>` | My Activity card | — | Shown when `!currentUser` | TBD | ❌ |
| F-1720 | Appearance card — Theme select | Select `#theme-picker` `onchange="setTheme(this.value)"` | Appearance card | — | Options: Apple Light / Apple Dark / Clay Light / Clay Dark / Notion Paper / Notion Night | TBD | ❌ |
| F-1721 | Appearance card — theme description note | Div below theme select | Appearance card | — | Static explanatory text | TBD | ❌ |
| F-1722 | Navigation card — Edge-pull page navigation toggle | Checkbox `#settings-overscroll-nav` `onchange="setOverscrollNavEnabled(this.checked)"` | Navigation card | — | Off by default; persisted to localStorage | TBD | ❌ |
| F-1723 | Navigation card — keyboard shortcut hint | Static text about Alt+← / Alt+→ | Navigation card | — | Static | TBD | ❌ |
| F-1724 | Manage Stores card | Card `#settings-manage-stores` | — | GET `/api/stores` | Loaded by `loadManageStores()` on Settings page enter | TBD | ❌ |
| F-1725 | Manage Stores — All filter chip | Button `pill active` `data-filter="all"` `onclick="setManageStoresFilter('all')"` | Manage Stores card | GET `/api/stores` | Default active filter | TBD | ❌ |
| F-1726 | Manage Stores — Frequent filter chip | Button `pill` `data-filter="frequent"` | Manage Stores card | GET `/api/stores` (client-side filter) | Filters table to frequent stores | TBD | ❌ |
| F-1727 | Manage Stores — Rarely Used filter chip | Button `pill` `data-filter="low_freq"` | Manage Stores card | GET `/api/stores` (client-side filter) | Filters table to low-frequency stores | TBD | ❌ |
| F-1728 | Manage Stores — Hidden filter chip | Button `pill` `data-filter="hidden"` | Manage Stores card | GET `/api/stores` (client-side filter) | Filters table to hidden stores | TBD | ❌ |
| F-1729 | Manage Stores — stores table | Div `#manage-stores-table` | Manage Stores card | GET `/api/stores` | Table with store name + visibility picker per row | TBD | ❌ |
| F-1730 | Manage Stores — per-store visibility change | Dropdown/select per row | Manage Stores table | PATCH `/api/stores/<id>/visibility` `{visibility}` | Sets store to frequent / low_freq / hidden / auto | TBD | ❌ |
| F-1731 | Manage Stores — loading state | `<div class="empty-state"><p>Loading…</p></div>` | Manage Stores card | — | Default before fetch resolves | TBD | ❌ |
| F-1732 | Household Users card — + Add User button (admin) | Button `#user-add-btn btn-ghost btn-sm` `onclick="openHouseholdUserForm()"` | — | — | Admin-only; `display:none` until admin confirmed | TBD | ❌ |
| F-1733 | Household Users card — + Service Account button (admin) | Button `#service-account-add-btn btn-ghost btn-sm` `onclick="openServiceAccountForm()"` | — | POST `/auth/service-accounts` | Admin-only | TBD | ❌ |
| F-1734 | Household Users card — Sort select | Select `#users-sort` `onchange="setUsersSort(this.value)"` | — | — | Options: Name A-Z / Role / Status / Newest; client-side sort | TBD | ❌ |
| F-1735 | Household Users card — Refresh button | Button `btn-ghost btn-sm` `onclick="loadUsers()"` | — | GET `/auth/users` | Force-reload | TBD | ❌ |
| F-1736 | Household Users — admin-only note | Div `#user-admin-note` "Only admins can manage household users." | — | — | Shown for non-admins | TBD | ❌ |
| F-1737 | Household Users — Invite via Google section | Div `#invite-user-section` (admin-only) | User form | POST `/auth/invites` | Email + Name + Role fields; `display:none` for non-admins | TBD | ❌ |
| F-1738 | Invite form — Email field | Input `#invite-email` type=email | User form / invite section | — | Required | TBD | ❌ |
| F-1739 | Invite form — Name field | Input `#invite-name` placeholder "Jane" | User form / invite section | — | Optional | TBD | ❌ |
| F-1740 | Invite form — Role select | Select `#invite-role` User/Admin | User form / invite section | — | Default "user" | TBD | ❌ |
| F-1741 | Invite form — Send Invite Link button | Button `btn-primary` `onclick="createInvite()"` | User form / invite section | POST `/auth/invites` | Creates invite; shows result URL in `#invite-result` | TBD | ❌ |
| F-1742 | Invite form — result URL panel | Div `#invite-result` with readonly input `#invite-result-url` + Copy button | User form | — | Hidden until invite created; Copy calls `copyInviteLink()` | TBD | ❌ |
| F-1743 | Classic user form — Name field | Input `#user-name` placeholder "e.g. Alex" | User form / classic section | — | Required | TBD | ❌ |
| F-1744 | Classic user form — Email field | Input `#user-email` type=email | User form / classic section | — | Required | TBD | ❌ |
| F-1745 | Classic user form — Password field | Input `#user-password` type=password | User form / classic section | — | ≥8 characters | TBD | ❌ |
| F-1746 | Classic user form — Avatar field | Input `#user-avatar` maxlength=4 placeholder "😎" | User form / classic section | — | Optional | TBD | ❌ |
| F-1747 | Classic user form — Role select | Select `#user-role` User/Admin | User form / classic section | — | Default "user" | TBD | ❌ |
| F-1748 | Classic user form — Add Household User button | Button `btn-primary` `onclick="createUser()"` | User form / classic section | POST `/auth/users` | Creates password-based user | TBD | ❌ |
| F-1749 | Classic user form — Cancel button | Button `btn-ghost` `onclick="hideHouseholdUserForm()"` | User form / classic section | — | Hides form | TBD | ❌ |
| F-1750 | Pending Invites section | Div `#pending-invites-section` + body `#pending-invites-body` | — | GET `/auth/invites` | Shown admin-only; loaded by `loadUsers()`; per-invite rows with revoke action | TBD | ❌ |
| F-1751 | Users table body | Div `#users-table-body` | — | GET `/auth/users` | Rendered by `loadUsers()`; two layouts: admin (with actions) vs read-only | TBD | ❌ |
| F-1752 | User row — role badge | Span `badge-ok` (admin) / `badge-pantry` (user) | Users table | — | From `user.role` | TBD | ❌ |
| F-1753 | User row — Edit button (admin) | `btn-ghost btn-sm` `onclick="editUser(id)"` | Users table | PATCH `/auth/users/<id>` | Prompts for name / email / avatar / role; updates user | TBD | ❌ |
| F-1754 | User row — Pages button (admin) | `btn-ghost btn-sm` `onclick="editUserPages(id)"` | Users table | PATCH `/auth/users/<id>` | Shows per-page access picker; `ALL_SIDEBAR_PAGES` list | TBD | ❌ |
| F-1755 | User row — Promote/Demote button (admin) | `btn-ghost btn-sm` `onclick="toggleUserRole(id)"` | Users table | PATCH `/auth/users/<id>` `{role}` | Confirm dialog; toggles admin ↔ user | TBD | ❌ |
| F-1756 | User row — Delete button (admin) | `btn-danger btn-sm` | Users table | DELETE `/auth/users/<id>` | Confirm dialog | TBD | ❌ |
| F-1757 | Service Account — rotate token | Button per service account row | Users table | POST `/auth/service-accounts/<id>/rotate` | Generates new API token for service account | TBD | ❌ |
| F-1758 | Service Account — delete | Button per service account row | Users table | DELETE `/auth/service-accounts/<id>` | Removes service account | TBD | ❌ |
| F-1759 | Trusted Devices card — Pair New Device button | Button `btn-ghost btn-sm` `onclick="openDevicePairingModal()"` | — | POST `/auth/device-pairing/start` | Opens device-pairing modal; generates QR + link | TBD | ❌ |
| F-1760 | Trusted Devices card — Refresh button | Button `btn-ghost btn-sm` `onclick="loadTrustedDevices()"` | — | GET `/auth/trusted-devices` | Force-reload | TBD | ❌ |
| F-1761 | Trusted Devices — admin-only note | Div `#trusted-devices-note` | — | — | Shown for non-admins | TBD | ❌ |
| F-1762 | Trusted Devices body | Div `#trusted-devices-body` | — | GET `/auth/trusted-devices` | Rendered by `loadTrustedDevices()`; per-device cards | TBD | ❌ |
| F-1763 | Device card — name + status badge | `<strong>` + `badge-ok`/`badge-low` | Trusted Devices body | — | Status: active (green) / other (low) | TBD | ❌ |
| F-1764 | Device card — scope + linked user meta | Div "Linked to X · Added by Y" | Trusted Devices body | — | `device.linked_user_name`; `device.created_by_name` | TBD | ❌ |
| F-1765 | Device card — last seen | Div with relative time | Trusted Devices body | — | `formatDateTime(device.last_seen_at)` | TBD | ❌ |
| F-1766 | Device card — Rename button | `btn-ghost btn-sm` `onclick="renameTrustedDevice(id, name)"` | Trusted Devices body | PATCH `/auth/trusted-devices/<id>` `{name}` | Prompt dialog | TBD | ❌ |
| F-1767 | Device card — Scope button | `btn-ghost btn-sm` `onclick="changeTrustedDeviceScope(id, scope)"` | Trusted Devices body | PATCH `/auth/trusted-devices/<id>` `{scope}` | Changes device scope | TBD | ❌ |
| F-1768 | Device card — Pages button | `btn-ghost btn-sm` `onclick="editTrustedDevicePages(id)"` | Trusted Devices body | PATCH `/auth/trusted-devices/<id>` | Per-page access picker for trusted device | TBD | ❌ |
| F-1769 | Device card — Revoke button | `btn-danger btn-sm` `onclick="revokeTrustedDevice(id)"` | Trusted Devices body | POST `/auth/trusted-devices/<id>/revoke` | Revokes (soft-disable) device | TBD | ❌ |
| F-1770 | Device card — Delete button | `btn-danger btn-sm` `onclick="deleteTrustedDevice(id)"` | Trusted Devices body | DELETE `/auth/trusted-devices/<id>` | Permanently removes device record | TBD | ❌ |
| F-1771 | Device Pairing modal | Overlay `#device-pairing-overlay` | — | POST `/auth/device-pairing/start` + GET `/auth/device-pairing/status/<token>` | Name + Scope fields; Start button generates QR + link; polls status every 2.5s | TBD | ❌ |
| F-1772 | Device Pairing modal — Name input | Input `#device-pairing-name` placeholder "e.g. Kitchen Fridge" | Pairing modal | — | Required | TBD | ❌ |
| F-1773 | Device Pairing modal — Scope select | Select `#device-pairing-scope` | Pairing modal | — | Drives scope-help text | TBD | ❌ |
| F-1774 | Device Pairing modal — QR code | Div `#device-pairing-qr` | Pairing modal | — | Rendered after pairing starts | TBD | ❌ |
| F-1775 | Device Pairing modal — pairing link | Div `#device-pairing-link` | Pairing modal | — | Copyable URL for non-QR pairing | TBD | ❌ |
| F-1776 | Device Pairing modal — status label | Div `#device-pairing-status` | Pairing modal | GET `/auth/device-pairing/status/<token>` | Polls until approved / rejected / timeout | TBD | ❌ |
| F-1777 | Device Pairing modal — linked-user select | Select `#device-approval-linked-user` | Pairing modal | POST `/auth/device-pairing/approve` | Admin chooses which user to link device to | TBD | ❌ |
| F-1778 | Snapshot Review card — Refresh button | Button `btn-ghost btn-sm` `onclick="loadSnapshotReviewQueue()"` | — | GET `/product-snapshots/review-queue?status=pending&limit=50` | Force-reload | TBD | ❌ |
| F-1779 | Snapshot Review — admin-only note | Div `#snapshot-review-note` | — | — | Shown for non-admins | TBD | ❌ |
| F-1780 | Snapshot Review body | Div `#snapshot-review-body` | — | GET `/product-snapshots/review-queue` | Rendered by `loadSnapshotReviewQueue()`; per-snapshot rows | TBD | ❌ |
| F-1781 | Snapshot row — product name input | Input `#snapshot-review-name-<id>` | Snapshot Review body | — | Pre-filled from linked product name or shopping item name | TBD | ❌ |
| F-1782 | Snapshot row — category select | Select `#snapshot-review-category-<id>` | Snapshot Review body | — | Populated by `renderProductCategoryOptionTags()` | TBD | ❌ |
| F-1783 | Snapshot row — Link Product button | `btn-primary btn-sm` `onclick="saveSnapshotReview(id)"` | Snapshot Review body | POST `/product-snapshots/<id>/review` | Links snapshot to product by name+category | TBD | ❌ |
| F-1784 | Snapshot row — Dismiss button | `btn-ghost btn-sm` | Snapshot Review body | POST `/product-snapshots/<id>/review` `{action:"dismiss"}` | Dismisses the snapshot from queue | TBD | ❌ |
| F-1785 | Environment Backup & Restore card — Create Backup button | Button `btn-ghost btn-sm` `onclick="createEnvironmentBackup()"` | — | POST `/system/backups/create` | Admin-only; creates .tar.gz bundle | TBD | ❌ |
| F-1786 | Environment Backup — Upload Backup button | Button `btn-ghost btn-sm` `onclick="triggerEnvironmentBackupUpload()"` | — | POST `/system/backups/upload` | Triggers hidden file input `#environment-backup-upload` | TBD | ❌ |
| F-1787 | Environment Backup — hidden file input | Input `#environment-backup-upload` type=file accept=".tar.gz" | — | POST `/system/backups/upload` | `onchange="uploadEnvironmentBackup(this)"` | TBD | ❌ |
| F-1788 | Environment Backup — Verify button | Button `btn-ghost btn-sm` `onclick="verifyEnvironmentBackup()"` | — | POST `/system/backups/verify` | Checks current environment integrity | TBD | ❌ |
| F-1789 | Environment Backup — Refresh button | Button `btn-ghost btn-sm` `onclick="loadEnvironmentBackups()"` | — | GET `/system/backups` | Force-reload backup list | TBD | ❌ |
| F-1790 | Environment Backup — admin-only note | Div `#environment-backup-note` | — | — | Shown for non-admins | TBD | ❌ |
| F-1791 | Environment Backup hero — Backup Count stat | Strong `#environment-backup-count` | Backup hero | — | Count of available backups | TBD | ❌ |
| F-1792 | Environment Backup hero — Latest Size stat | Strong `#environment-backup-latest-size` | Backup hero | — | Human-readable size of newest backup | TBD | ❌ |
| F-1793 | Environment Backup hero — Receipt Files stat | Strong `#environment-backup-latest-receipts` | Backup hero | — | File count in latest backup | TBD | ❌ |
| F-1794 | Environment Backup hero — Purchases stat | Strong `#environment-backup-latest-purchases` | Backup hero | — | Purchase record count in latest backup | TBD | ❌ |
| F-1795 | Environment Backup — latest summary stat grid | Div `#environment-backup-latest-summary` | Backup panel | — | Additional per-field summary from latest backup | TBD | ❌ |
| F-1796 | Environment Backup — Restore Source select | Select `#environment-backup-select` `onchange="updateEnvironmentRestoreSelectedSummary()"` | Backup panel | — | Options populated by `loadEnvironmentBackups()` | TBD | ❌ |
| F-1797 | Environment Backup — Restore Selected Backup button | Button `btn-danger` `onclick="openEnvironmentRestoreModal()"` | Backup panel | POST `/system/backups/restore` | Opens confirm modal; replaces current state + restarts backend | TBD | ❌ |
| F-1798 | Environment Backup — restore progress panel | Div `#environment-backup-restore-progress` | Backup panel | — | Hidden until restore initiated; shows stage label, percent, animated bar | TBD | ❌ |
| F-1799 | Environment Backup — restore progress bar | Div `#environment-backup-restore-bar` width transitions | Restore progress | — | CSS width 0%→100% with gradient | TBD | ❌ |
| F-1800 | Environment Backup — restore stage label | Div `#environment-backup-restore-stage` | Restore progress | — | "Preparing restore…" → stage messages | TBD | ❌ |
| F-1801 | Environment Backup — restore percent label | Div `#environment-backup-restore-percent` | Restore progress | — | "0%" → "100%" | TBD | ❌ |
| F-1802 | Environment Backup — restore detail | Div `#environment-backup-restore-detail` | Restore progress | — | "Waiting to start." → status messages | TBD | ❌ |
| F-1803 | Environment Backup — backup list table | Div `#environment-backup-list .table-wrap` | Backup panel | GET `/system/backups` | Per-backup rows; each row has Download + Restore buttons | TBD | ❌ |
| F-1804 | Environment Backup — per-backup Download button | Button in backup list row | Backup list | GET `/system/backups/download/<filename>` | Direct download of .tar.gz | TBD | ❌ |
| F-1805 | Environment Backup — per-backup Restore button | Button `btn-danger` `onclick="openEnvironmentRestoreModal(filename)"` | Backup list | POST `/system/backups/restore` | Pre-selects that backup in restore modal | TBD | ❌ |
| F-1806 | Environment Backup — verification report panel | Div `.environment-backup-report-card` with `#environment-backup-report` | Backup panel | — | Shows latest verify/restore report; default "Run Verify or create a backup…" | TBD | ❌ |
| F-1807 | Catalog Review card — Status filter select | Select `#review-status-filter` `onchange="loadReviewQueue()"` | — | GET `/products/review-queue?status=<v>` | Options: Pending / Dismissed / Resolved / All | TBD | ❌ |
| F-1808 | Catalog Review — Run Gemini button | Button `btn-ghost btn-sm` `onclick="runBulkEnhancement()"` | — | POST `/products/review-queue/enhance` | Admin-only; bulk AI enhancement of pending items | TBD | ❌ |
| F-1809 | Catalog Review — Refresh button | Button `btn-ghost btn-sm` `onclick="loadReviewQueue()"` | — | GET `/products/review-queue` | Force-reload | TBD | ❌ |
| F-1810 | Catalog Review — admin-only note | Div `#review-admin-note` | — | — | "Only admins can review and clean up OCR-heavy product names." | TBD | ❌ |
| F-1811 | Catalog Review body | Div `#review-queue-body` | — | GET `/products/review-queue` | Rendered by `loadReviewQueue()`; per-item rows with approve/dismiss | TBD | ❌ |
| F-1812 | Catalog Review — per-item approve | Button in review row | Catalog Review body | POST `/products/<id>/review-status` `{status:"resolved"}` | Marks item as resolved | TBD | ❌ |
| F-1813 | Catalog Review — per-item dismiss | Button in review row | Catalog Review body | POST `/products/<id>/review-status` `{status:"dismissed"}` | Marks item as dismissed | TBD | ❌ |
| F-1814 | AI Model Registry card — + New Model button | Button `btn-ghost btn-sm` `onclick="openAdminAiModelEditorForNew()"` | — | — | Opens Model Editor form (admin-only) | TBD | ❌ |
| F-1815 | AI Model Registry — Refresh button | Button `btn-ghost btn-sm` `onclick="loadAdminAiModels()"` | — | GET `/api/admin/models` | Force-reload | TBD | ❌ |
| F-1816 | AI Model Registry — admin-only note | Div `#admin-ai-models-note` | — | — | "Only admins can manage the OCR model catalog." | TBD | ❌ |
| F-1817 | AI Model Registry body | Div `#admin-ai-models-body` | — | GET `/api/admin/models` | Rendered by `renderAdminAiModels()`; per-model rows; empty state "Could not load AI models." | TBD | ❌ |
| F-1818 | Model Editor — editor panel | Div `#admin-ai-model-editor-wrap` (hidden until opened) | AI Model Registry | — | Shown by `openAdminAiModelEditorForNew()` or row edit click | TBD | ❌ |
| F-1819 | Model Editor — "Editing existing model" pill | Div `#admin-ai-model-editing-pill badge-info` | Model Editor | — | Shown only in edit mode | TBD | ❌ |
| F-1820 | Model Editor — Name field | Input `#admin-ai-model-name` placeholder "Gemini Flash" | Model Editor | — | Required | TBD | ❌ |
| F-1821 | Model Editor — Provider select | Select `#admin-ai-model-provider` | Model Editor | — | Options: gemini / openai / openrouter / ollama / anthropic | TBD | ❌ |
| F-1822 | Model Editor — Model String field | Input `#admin-ai-model-string` | Model Editor | — | Provider-native model ID | TBD | ❌ |
| F-1823 | Model Editor — Price Tier select | Select `#admin-ai-model-price-tier` | Model Editor | — | Options: free / premium / pro / enterprise | TBD | ❌ |
| F-1824 | Model Editor — Credential Mode select | Select `#admin-ai-model-credential-mode` `onchange="handleAdminAiModelKeyInput()"` | Model Editor | — | env / stored_key / no_key_required | TBD | ❌ |
| F-1825 | Model Editor — Base URL field | Input `#admin-ai-model-base-url` placeholder "Optional" | Model Editor | — | Optional; for self-hosted endpoints | TBD | ❌ |
| F-1826 | Model Editor — Description field | Input `#admin-ai-model-description` | Model Editor | — | Short label shown in model picker | TBD | ❌ |
| F-1827 | Model Editor — Stored API Key field | Input `#admin-ai-model-api-key` type=password `oninput="handleAdminAiModelKeyInput()"` | Model Editor | — | Leave blank to keep current key; key hint shown dynamically | TBD | ❌ |
| F-1828 | Model Editor — Sort Order field | Input `#admin-ai-model-sort-order` type=number | Model Editor | — | Default 100 | TBD | ❌ |
| F-1829 | Model Editor — Input $/1M Tokens field | Input `#admin-ai-model-input-cost` type=number step=0.000001 | Model Editor | — | Optional pricing metadata | TBD | ❌ |
| F-1830 | Model Editor — Output $/1M Tokens field | Input `#admin-ai-model-output-cost` type=number step=0.000001 | Model Editor | — | Optional pricing metadata | TBD | ❌ |
| F-1831 | Model Editor — Enabled checkbox | Checkbox `#admin-ai-model-enabled` | Model Editor | — | Default checked | TBD | ❌ |
| F-1832 | Model Editor — Visible checkbox | Checkbox `#admin-ai-model-visible` | Model Editor | — | Default checked | TBD | ❌ |
| F-1833 | Model Editor — Vision checkbox | Checkbox `#admin-ai-model-supports-vision` | Model Editor | — | Default checked | TBD | ❌ |
| F-1834 | Model Editor — PDF checkbox | Checkbox `#admin-ai-model-supports-pdf` | Model Editor | — | Default unchecked | TBD | ❌ |
| F-1835 | Model Editor — JSON checkbox | Checkbox `#admin-ai-model-supports-json` | Model Editor | — | Default unchecked | TBD | ❌ |
| F-1836 | Model Editor — Image Input checkbox | Checkbox `#admin-ai-model-supports-image` | Model Editor | — | Default checked | TBD | ❌ |
| F-1837 | Model Editor — Clear stored key checkbox | Checkbox `#admin-ai-model-clear-key` | Model Editor | — | Explicitly wipes stored key on save | TBD | ❌ |
| F-1838 | Model Editor — Save Model button | Button `btn-primary` `onclick="saveAdminAiModel()"` | Model Editor | POST `/api/admin/models` or PUT `/api/admin/models/<id>` | Creates or updates model | TBD | ❌ |
| F-1839 | Model Editor — Clear button | Button `btn-ghost` `onclick="resetAdminAiModelForm()"` | Model Editor | — | Resets all fields | TBD | ❌ |
| F-1840 | Model Editor — Cancel button | Button `btn-ghost` `onclick="hideAdminAiModelEditor()"` | Model Editor | — | Hides editor panel | TBD | ❌ |
| F-1841 | AI Usage sub-card — days select | Select `#admin-ai-usage-days` `onchange="loadAdminAiUsage()"` | AI Model Registry | GET `/api/admin/models/usage?days=<n>` | Options: Last 7 / 14 / 30 days | TBD | ❌ |
| F-1842 | AI Usage sub-card — Refresh button | Button `btn-ghost btn-sm` `onclick="loadAdminAiUsage()"` | AI Model Registry | GET `/api/admin/models/usage` | Force-reload | TBD | ❌ |
| F-1843 | AI Usage — summary stat grid | Div with Requests / Tokens / Estimated Cost totals | AI Usage card | GET `/api/admin/models/usage` | From `adminAiUsage.totals` | TBD | ❌ |
| F-1844 | AI Usage — per-model table | `<table>` with Model / Requests / Tokens / Avg Latency / Est. Cost / Last Used | AI Usage card | GET `/api/admin/models/usage` | Admin-only; empty state "No AI model usage recorded yet." | TBD | ❌ |
| F-1845 | AI Usage — error state | "Could not load AI usage." | AI Usage card | — | Shown on non-OK response | TBD | ❌ |
| F-1846 | AI Usage — admin access note | "Admin access required to view AI usage." | AI Usage card | — | Shown for non-admins | TBD | ❌ |
| F-1847 | Image Backfill card — provider select | Select `#image-backfill-provider` | — | GET `/api/admin/image-backfill/schedule` | Options: Auto / Gemini Flash / OpenAI gpt-image-1 | TBD | ❌ |
| F-1848 | Image Backfill — Refresh candidates button | Button `btn-ghost btn-sm` `onclick="loadImageBackfillCandidates()"` | — | GET (image backfill candidates API) | Force-reload | TBD | ❌ |
| F-1849 | Image Backfill — Run button | Button `#image-backfill-run-btn btn-primary btn-sm` `onclick="runImageBackfill()"` | — | POST `/api/admin/image-backfill/run` | Disabled until candidates selected | TBD | ❌ |
| F-1850 | Image Backfill — History window select | Select `#image-backfill-history-window` `onchange="loadImageBackfillHistory()"` | History fieldset | GET (backfill history API) | Options: Last 7 / 30 / 90 / 365 days; 30 default | TBD | ❌ |
| F-1851 | Image Backfill — History refresh button | Button `btn-ghost btn-sm` `onclick="loadImageBackfillHistory()"` | History fieldset | — | Force-reload | TBD | ❌ |
| F-1852 | Image Backfill — History totals span | Span `#image-backfill-history-totals` | History fieldset | — | "N images · $X total cost" | TBD | ❌ |
| F-1853 | Image Backfill — History body | Div `#image-backfill-history-body` | History fieldset | GET (backfill history API) | Rendered by `loadImageBackfillHistory()` | TBD | ❌ |
| F-1854 | Image Backfill — note | Div `#image-backfill-note` | — | — | "Products with no image. Pick rows + provider → Run." | TBD | ❌ |
| F-1855 | Image Backfill — Schedule enabled toggle | Checkbox `#image-backfill-schedule-enabled` | Nightly schedule fieldset | GET/POST `/api/admin/image-backfill/schedule` | Loaded by `loadImageBackfillSchedule()`; saved by `saveImageBackfillSchedule()` | TBD | ❌ |
| F-1856 | Image Backfill — Schedule hour input | Input `#image-backfill-schedule-hour` type=number 0-23 | Nightly schedule fieldset | POST `/api/admin/image-backfill/schedule` | Default 4; server-time 24h | TBD | ❌ |
| F-1857 | Image Backfill — Schedule minute input | Input `#image-backfill-schedule-minute` type=number 0-59 | Nightly schedule fieldset | POST `/api/admin/image-backfill/schedule` | Default 0 | TBD | ❌ |
| F-1858 | Image Backfill — Save schedule button | Button `btn-ghost btn-sm` `onclick="saveImageBackfillSchedule()"` | Nightly schedule fieldset | POST `/api/admin/image-backfill/schedule` | Saves enabled flag + hour + minute | TBD | ❌ |
| F-1859 | Image Backfill — Next run label | Div `#image-backfill-next-run` | Nightly schedule fieldset | — | Shows next scheduled run time | TBD | ❌ |
| F-1860 | Image Backfill — status div | Div `#image-backfill-status` | — | — | Shows run status / progress messages | TBD | ❌ |
| F-1861 | Image Backfill — candidates table body | Div `#image-backfill-body .table-wrap` | — | GET (backfill candidates API) | Per-product rows with checkbox; Run button enabled when selections made | TBD | ❌ |
| F-1862 | Chat Audit card — limit select | Select `#chat-audit-limit` `onchange="loadChatAudit()"` | — | GET `/chat/audit?limit=<n>` | Options: Last 20 / 50 / 100 | TBD | ❌ |
| F-1863 | Chat Audit — Refresh button | Button `btn-ghost btn-sm` `onclick="loadChatAudit()"` | — | GET `/chat/audit` | Force-reload | TBD | ❌ |
| F-1864 | Chat Audit body | Div `#chat-audit-body` | — | GET `/chat/audit?limit=<n>` | Rendered by `loadChatAudit()`; per-message audit rows | TBD | ❌ |
| F-1865 | API Token card — token input | Input `#settings-token` type=password placeholder "Paste your token here" | — | — | Optional bearer token for integrations | TBD | ❌ |
| F-1866 | API Token — Save button | Button `btn-primary` `onclick="saveToken()"` | — | — | Persists token to localStorage | TBD | ❌ |
| F-1867 | API Token — Show/Hide toggle | Button `btn-ghost` 👁 `onclick="toggleTokenVis()"` | — | — | Toggles input type password ↔ text | TBD | ❌ |
| F-1868 | API Token — API Base URL field | Input `#settings-url` | — | — | Base URL for API calls; persisted via `saveSettings()` | TBD | ❌ |
| F-1869 | API Token — Save Settings button | Button `btn-success` `onclick="saveSettings()"` | — | — | Saves both token and base URL to localStorage | TBD | ❌ |

---

## VERIFICATION

### Check A — fetch() coverage
_Pending — populated after all per-page passes complete._

### Check B — features-data.js coverage
_Pending._

### Check C — Plan gap detection
_Pending._

### Check D — Business rule coverage
_Pending._

---

## AUDIT STATUS

| Page | Audit pass | Rows |
|---|---|---|
| Dashboard | ✅ done | 45 |
| Inventory | ✅ done | 45 |
| Shopping List | ✅ done | 58 |
| Kitchen | ✅ done | 39 |
| Products | ✅ done | 31 |
| Upload Receipt | ✅ done | 36 |
| Receipts List | ✅ done | 61 |
| Restaurant | ✅ done | 30 |
| Balances | ✅ done | 18 |
| Contacts | ✅ done | 22 |
| Expenses | ✅ done | 51 |
| Fixed Bills | ✅ done | 92 |
| Accounts | ✅ done | 80 |
| Analytics | ✅ done | 22 |
| Budget | ✅ done | 28 |
| Contributions | ✅ done | 27 |
| Medications | ✅ done | 39 |
| Settings | ✅ done | 170 |

**Total items: 894** (Dashboard 45, Inventory 45, Shopping List 58, Kitchen 39, Products 31, Upload Receipt 36, Receipts List 61, Restaurant 30, Balances 18, Contacts 22, Expenses 51, Fixed Bills 92, Accounts 80, Analytics 22, Budget 28, Contributions 27, Medications 39, Settings 170)

---

✅ Pass 5 complete — Medications, Settings
✅ AUDIT COMPLETE — all 18 screens audited

📊 Total registry items: 894

✅ Pass 1 complete — Dashboard, Inventory, Shopping List, Kitchen

✅ Pass 2 complete — Products, Upload Receipt, Receipts List, Restaurant
✅ Pass 3 complete — Balances, Contacts, Expenses, Fixed Bills
✅ Pass 4 complete — Accounts, Analytics, Budget, Contributions
